# CLAUDE.md — プロジェクト引き継ぎ

このファイルは claude.ai 上のチャットで行った要件定義・プロトタイプ開発の引き継ぎ書です。
Claude Code はまずこのファイルと requirements.md(要件定義書v0.2)を読んでから作業してください。

## プロジェクト概要

ライブで約20曲を、iPadに表示したChordWikiのコード譜を見ながら演奏するための
自作PWA「ChordWikiライブビューア」を開発する。

核心要件は「ChordWikiと同等の操作感の自動スクロール再現」。
既存の楽譜アプリは複数試したが、速度調整が粗い・スクロール中に画像描画が乱れる、
という理由ですべて不合格だった。メモ機能はその上に載せる二次的機能。

## これまでの経緯と決定事項

1. 当初はSafari拡張機能でChordWiki実ページにメモを重ねる方式(v0.1)を検討
2. 以下の理由でPWA方式(v0.2)に転換した:
   - 開発者はMacを持たない(Safari拡張はXcodeビルドが必須で不可能)
   - 本番会場はネット接続が保証されない
   - SafariのITP(7日未訪問でサイトデータ自動削除)によるデータ消失リスク
3. コンテンツは「事前にChordWikiを移調済み状態でフルページスクショ(画像/PDF)し、
   PWAに登録して表示する」方式。本番中の移調は非対応(キーは事前確定が前提)
4. スクロールエンジンのプロトタイプ(scroll-prototype.html → index.html)を作成済み
   - rAF + translate3d による一定速度スクロール
   - 縦1024pxタイル分割Canvas描画(iPad Safariの巨大画像描画問題対策)
   - 1px/秒刻みの速度調整(長押しで刻み加速)、localStorage保存
   - 即時停止・同位置再開、末尾自動停止
   - 手動ドラッグ(再生中はドラッグ中一時停止、離すと自動再開)
   - PDF読み込み(pdf.jsをCDNから遅延読込。本実装では同梱してオフライン化する)
   - Wake Lock、進捗バー、再生中ツールバー自動非表示
5. PC(ブラウザ)での動作確認は合格。iPad実機検証は未実施

## 現在の状態

- リポジトリには index.html(プロトタイプ)と本ファイル、requirements.md がある想定
- GitHub Pages で公開済み(のはず。未公開なら公開設定から行う)
- 次のマイルストーン: iPad実機検証 → 合格ならMVP実装

## iPad実機検証のチェック項目

1. 実際のChordWikiフルページスクショで、スクロールが滑らかか
2. 1px/秒刻みの速度粒度が適切か(実際に使う速度域もメモする)
3. スクロール中に画像の白抜け・描画乱れが出ないか(長い曲で)
4. ドラッグ→自動再開の挙動が演奏時の感覚に合うか

## MVP実装のスコープ(requirements.md 15章)

プロトタイプをベースに以下を追加する:

- Service Worker によるオフライン起動(pdf.js同梱含む)
- IndexedDB への曲保存(画像、曲名、スクロール速度、メモ、BPM)
- 曲ごとのスクロール速度保存(現在はグローバル1値)
- テキストメモ・画像メモの配置(譜面画像上の相対座標、編集モードで操作)
- 演奏モード(デフォルト)/編集モードの切り替え(鍵アイコン長押し等の誤操作対策)
- 全データのZIP書き出し/読み込み(バックアップ必須。最終バックアップ日時の表示と警告)

MVP後: 曲一覧、セットリスト、前後曲切り替え、メトロノーム(Web Audio、曲ごとBPM)、
Windows(Chrome/Edge)編集対応。詳細は requirements.md の14章。

## 技術方針・制約

- 素のHTML/CSS/JS。フレームワークなし。ビルド工程なし(誰でも触れる状態を維持)
- 外部依存は pdf.js のみ。本実装ではリポジトリに同梱する
- 対象: iPad Safari(ホーム画面追加PWA)+ Windows Chrome/Edge(事前編集用)
- ホーム画面追加PWAはITPの7日削除の対象外(WebKit公式見解)だが、
  仕様変更リスクに備えZIPバックアップを必須機能とする
- localStorage は画像を扱えないため使わない(IndexedDB を使う)
- 受け入れ条件は requirements.md の16章。特に
  「機内モード(完全オフライン)で全機能動作」が最重要

## 検証が必要な未決事項(requirements.md 17章)

- 本番中に移調が必要な場面が本当にないか(本方式では不可)
- 自動スクロール中の手動操作の挙動(プロトタイプ実装案の是非を実機で確認)
- メトロノーム音色
- 譜面画像の推奨解像度・圧縮率(iPad実機で実測)
- ホーム画面追加PWAが7日放置でデータ保持されるかの実測(開発と並行して放置試験)
