# 追加機能 要件定義(2026-08-20 夜)

対象は #1 リアルタイム録音記譜 / #2 ピアノロール / #5 スタンプ追加。
(#4 ChordWiki取り込みは 2026-08-20 に「対応不要」と判断され、取り下げ)

**この文書は確認をもらうためのもので、承認前に実装はしない。**
(2026-08-20の夜、#1と#5を先に作ってしまったが、これは私の読み違い。要否は判断を仰ぐ)

各項目の末尾に「判断した点」を書いてある。違っていたら直してほしい。

---

## #1 リアルタイム録音記譜(MIDI + クリック) ★最優先

### 目的

クリックに合わせてMIDIキーボードで弾くと、そのまま楽譜になる。
自動採譜(音源解析)と違い**音高もタイミングも誤差ゼロ**なので、実用になるのはこちら。

### 使い方

1. 楽譜エディタのツールバー「⏺ 録音」を押す → 録音パネルが開く
2. BPM・カウントイン・量子化を確認して「録音開始」
3. カウントインのクリックが鳴り、そのあと録音が始まる
4. 弾く。クリックは鳴り続ける
5. 「停止」を押すと、その場で譜面に変換される

### パネルの項目

| 項目 | 既定値 | 備考 |
|---|---|---|
| BPM | 曲のBPM。無ければ100 | 録音中は変えられない |
| カウントイン | 1小節 | なし / 1小節 / 2小節 |
| 量子化 | 16分 | 4分 / 8分 / 16分 / 32分 |
| 入れ方 | 末尾に追記 | 追記 / 全部置き換え |

### 記譜化のルール

1. **カウントインが終わった瞬間を音楽的時刻0**とする
2. 各打鍵の時刻を拍に変換 → 一番近いグリッドに吸着(前後どちらにもズレを許す)
3. **同時発音**: 吸着後の開始位置が同じ音は、1つの音符(和音)にまとめる
4. **音価**: 離した時刻 − 押した時刻 を吸着。ただし
   - 0になったらグリッド1つぶん(短すぎる打鍵の救済)
   - 次の打鍵より長くなったら、次の打鍵の直前で切る(レガートで弾いた時の吸収)
5. **休符**: 音符と音符の間がグリッド1つ以上空いていたら休符を入れる
6. **末尾**: 最後の音符の終わりまで。小節の残りは埋めない(既存の自動処理に任せる)

変換後は既存の `scoreElements` になるので、**小節分割・タイ・連桁・移調は全部そのまま効く**。

### 技術的な要点

- **時計合わせが肝**。メトロノームは `AudioContext.currentTime`、MIDIイベントは
  `event.timeStamp`(`performance.now()`基準)と**別の時計**。録音開始時に両方を
  記録して換算する。ここを雑にすると全体が一定量ズレる
- 楽譜エディタはフレーズ集からも開ける(曲が無い)ので、**エディタ内に専用のBPMを持つ**。
  既存のビューア側メトロノーム(`currentBpm`必須)には依存しない
- クリック音は既存の `scheduleClick()` を使い回す

### 3連符の扱い(2026-08-20 追記。当初スコープ外にしていたのを撤回)

**1拍ごとに、直線グリッドと3連グリッドのどちらで解釈するかを自動で決める。**

- その拍に入っている打鍵を、直線グリッド(16分等)と3連グリッド(8分3連・16分3連)の
  両方に吸着させてみて、**元の時刻とのズレの合計が小さい方**を採用する
- 3連と判定した拍の音符には `triplet: true` と共通の `tripletGroup` を振る
  (`tripletFirst` は先頭のみ)。既存の3連符データ構造にそのまま乗る
- **拍単位で判定する**ので、曲の途中で3連と直線が混ざっても正しく書き分けられる
- どちらのズレも大きい場合(明らかに外している)は直線を採る。3連は「積極的に
  3連らしく弾けている時だけ」選ばれるべきで、迷ったら普通の解釈にする方が事故が少ない

**これは #1 の実装済み分に対する追加作業**(現状は直線グリッドのみで動いている)。

### 複合拍子(6/8・9/8・12/8)でのクリック(2026-08-20 追記)

**現状の実装には不具合がある。** クリックの間隔を `60/BPM` 秒、1拍の長さを
「拍子の下の数字ぶんの音符」として計算しているため、6/8ではクリックが**8分音符**を
刻む(1小節に6回)。

6/8でBPMと言えば普通は**付点4分**(1小節に2回)なので、このままだと
「BPM=120」と入れた時に体感の3倍速で刻んでしまう。

- 複合拍子(下が8で拍数が3の倍数)では、**BPMは付点4分を指すものとして扱う**
- クリックも付点4分ごとに鳴らす(1小節に2回/3回/4回)
- 量子化のグリッド(8分・16分など)は音価そのものなので、こちらは変更不要

なお **6/8そのものは3連符が無くても書ける**。6/8の「3つ組」は拍子が持っている
区切りであって、8分3つは**ただの8分音符**(3連符ではない)。3連符が要るのは
4/4などで「2つに割れるところに3つ詰める」時だけ。
記譜・連桁側の6/8対応は既に動いている(8分6つが3つずつ2組で連桁されることを確認済み)。

### スコープ外(v1でやらないこと)

- MIDIキーボードが無い場合のマイク録音。精度が別次元に落ちるので分ける
- ベロシティからの強弱記号の自動付与
- 5連符・7連符などの3連以外の連符

### 受け入れ条件

- 4/4・120BPMで4分音符を4つ弾く → 4分音符4つの1小節になる
- 8分音符8つ → 8分8つ、連桁は拍ごとに2つずつ
- 3つ同時に押す → 和音1つ(`pitches`が3要素)になる
- 1拍休んで弾く → 4分休符が入る
- 多少走っても/遅れてもグリッドに吸着する
- 録音を止めても、それまでの譜面(追記モードなら)が壊れない

### 判断した点

- **音価は「押している長さ」から取る**。「次の音まで」にすると休符が一切出ず、
  実際の演奏と違う譜面になるため
- **既定は追記**。置換を既定にすると、録り直しのつもりが既存の打ち込みを消す事故になる

---

## #5 スタンプ追加(小・すぐ終わる)

### 目的

実際のバンド譜で使う記号を足す。定義テーブルに1行足すだけで増える構造は既にできている。

### 追加するもの

こちらの判断で、ライブ用コード譜で頻出のものを入れる。要らないものは朝に言ってもらえれば消す。

| 分類 | 追加 |
|---|---|
| 強弱 | `fff` `sfz` `fp` |
| 奏法 | フェルマータ / ブレス(V) / `Fill` `Break` `Stop` |
| 進行 | `Intro` `A` `B` `Sabi` `Solo` `Outro` |
| 反復 | `D.S.` `D.C.` `Coda` `Fine` |
| ノリ | `Shuffle` `Swing` `4Beat` `Bossa` |
| 楽器 | `Cho` `Syn` `Per` を only/in に追加 |

### 判断した点

- **進行の札は既存のMC/TCと同じ橙**にする。同じ「進行の話」なので色を分ける理由がない
- **反復記号(D.S.等)は黒インク**にする。これは記譜の記号であって注釈ではないため

---

## #2 ピアノロールエディタ(大)

### 目的

五線譜が読みにくい場面でも譜面を作れるようにする。**#1の録音結果を直す場所**として実質必須。

### 使い方

- 楽譜エディタに「⊞ ピアノロール」トグル。オンにすると五線譜の代わりにピアノロールが出る
- 横軸=時間(グリッド)、縦軸=音高。左端に鍵盤
- 空きをタップで音符を追加、音符をタップで選択、ドラッグで移動、右端を掴んで長さ変更

### データの扱い

既存の `scoreElements` と**双方向に変換**する。ピアノロール専用のデータは持たない
(2つの真実を持つと必ず食い違うため)。

- `scoreElements` → ロール: 先頭からの累積音価で開始位置、`scoreElementDurationUnits` で長さ
- ロール → `scoreElements`: 時間順に並べ、隙間を休符で埋め、同時発音を和音にまとめる

### グリッドと3連符(2026-08-20 追記。読み取り専用にする案は撤回)

DAWと同じく**グリッドを選べるようにする**。

- 選択肢: 4分 / 8分 / 16分 / 32分 / **8分3連 / 16分3連**
- 選んだグリッドに吸着する。3連グリッドを選んでいる間は3連の位置に乗る
- 書き戻す時、3連グリッド上の音符には `triplet` / `tripletGroup` を付ける
- 直線グリッドを選んでいる時でも、**既にある3連符はその位置のまま表示する**
  (見えなくなったり勝手に直線へ吸着したりはしない)。動かした時だけ吸着する

### スコープ外(v1)

- コード名・強弱・スラー等の装飾。五線譜側で付ける
- 5連符・7連符などの3連以外の連符

### 夜間の到達目標

**表示 + タップで追加 + タップで削除**まで。ドラッグ移動・長さ変更はその次。
中途半端な状態で終わらせないよう、動く単位で区切って進める。

---

## 決まったこと(2026-08-20 確認済み)

1. **#1・#5 は残す。** マニュアル(MANUAL.md / manual.html)にも反映する
2. **ピアノロールは五線譜と切り替え**(同時表示にしない)。画面を広く使えるので
   スマホ・タブレットでも操作しやすい方を採る
3. **改行の「＋−」は A(その段に入る小節数を増減)**。「2段目だけ短い」を直すのが目的
4. **着手順**: 6/8クリック修正 → 3連符 → ピアノロール
5. #4 ChordWiki取り込みは取り下げ

### こちらの判断で決めたこと(確認不要とした分)

- 6/8のBPM基準は**付点4分**(記譜の慣習どおり)
- 3連符と判定するのは「3連の方がズレが明確に小さい時だけ」。迷ったら直線にする
- 録音の既定値(カウントイン1小節・量子化16分・末尾に追記)は現状のまま

---

## 追加項目: 改行時の「＋−」(A案)

段(五線の行)の切れ目に「＋」「−」を出し、**その段に入る小節数**を増減する。

- **＋**: 次の段の先頭の小節を、この段の末尾に引き上げる
- **−**: この段の末尾の小節を、次の段へ送る
- 指定は小節に持たせる(既存の `lineBreak` と同じ考え方)。自動折り返しは残し、
  手動指定があった段だけそれに従う
- 幅が足りなくなる(＋しすぎ)場合は、それ以上増やせないようにする

## 進め方について

- 各機能は**動く単位ごとに検証**してから次へ行く(いつもどおりヘッドレスChrome+CDP)
- 途中で「これは仕様を決め直した方がいい」と判断したら、**勝手に広げずここに追記して止める**

---

## ツアー(初回の案内) ※未着手・2026-08-20 追加

### なぜ要るか

初アクセス者の動線が想定できていない。実測すると、まっさらな状態で開いた人はこうなる:

1. 開いた瞬間に `confirm()` で設定を促される
   — アプリを一度も見ていない人に**知らない言葉**を突きつけている
   (2026-08-20に「同期キー」という言い方はやめ、「あなた用のURL」に改めた。
    ただし**開いた瞬間に出る**という問題そのものは残っている)
2. 曲0件なのに `編集モード` `セットリスト` `練習ToDo` `リンク集` `フレーズ集` `＋曲を追加`
   が同じ重さで並ぶ。**このうち5つは曲が無いと使えない**
3. 画面の8割が空白で、「まだ曲がありません」の1行だけ。
   **その譜面をどこから持ってくるのか(ChordWikiのスクショ)が書かれていない**

### 方針: 用途を選ばせて分岐する

このアプリは用途の幅が広い(本番で見る / 練習する / 譜面を作る / 端末間で共有する)。
全部を最初に説明しても読まれないし、今やりたいこと以外は邪魔になる。
**「何をしたいか」をボタンで選ばせて、その道だけを案内する。**

### 最初の質問(たたき台)

- 🎤 **ライブ本番でコード譜を見ながら演奏したい**
- 🎧 **曲を覚えたい・練習したい**
- ✍️ **譜面に書き込みたい・楽譜を作りたい**
- 📱 **他の端末でも同じデータを使いたい**

### 各ルートの中身(たたき台)

**🎤 本番で見る**
1. ChordWikiで曲を開いてスクリーンショットを撮る
2. 「＋ 曲を追加」で取り込む
3. 曲を開いて「▶ 再生」→ 自動スクロールが始まる
4. 速さは「− 速度 +」で調整。BPMを入れると「速度計算」で自動で合わせられる

**🎧 練習する**
1. 曲を開く
2. 「🎵 音源」で音源を入れる(端末内のみ)
3. A-Bリピートで難所を繰り返す・速さを落とす
4. 「🥁 クリック」でメトロノーム

**✍️ 書き込む・楽譜を作る**
1. 「＋ テキスト」「＋ スタンプ」「ペン」で譜面に直接書く
2. 「＋ 楽譜」で五線譜を書いて貼る(MIDIキーボードがあれば「⏺ 録音」)
3. ピアノロールでも直せる

**📱 端末間で共有**
1. あなた用のURLに切り替える(URLの末尾に `?key=...` が付く。他人に教えない)
2. 「今すぐ同期」
3. 「あなた用のURLをコピー」したURLを、もう片方の端末で開くだけ

### 未確定(着手前に決めること)

- **初回の確認ダイアログをツアーに置き換えるか。**
  置き換えるのが自然だと思うが、クラウド保存の設定率が下がる可能性がある
  (ただし今は「意味が分からず全員キャンセル」になっている可能性が高い)
- **形式**: 画面にオーバーレイを重ねて実際のボタンを指し示す形か、
  説明ページ(start.htmlのような別ページ)形式か
- **出すきっかけ**: 初回訪問時に自動か、ホームの「使い方」の隣に「ツアー」ボタンを置くだけか
- 曲が0件のあいだ、使えない機能のボタンを畳むかどうか

### 関連する決定

- **招待機能は廃止した(2026-08-20)。** 「友達を招待」「はじめに(招待された方へ)」を
  アプリから削除。ツアーは「招待された人向け」ではなく**すべての初アクセス者向け**に作る
- **「同期キー」という概念は廃止した(2026-08-20)。** 下の「あなた用のURL」を参照。
  ツアーでも「キー」「合言葉」といった言い方はしない

---

## フレーズ集をディグリーで管理する(ジャズのリック辞書) ※未着手・2026-08-20 追加

### やりたいこと

- `IIm` が出てくるフレーズを探す
- `2-5-1` のフレーズを探す
- 索引付きの辞書のように引ける
- ディグリーで持てば移調が一瞬
- コメントを付けたい
- YouTubeのリンクと秒数を入れたい

**「どうやって紐づけてどう管理するか」が肝**、という認識で合っている。以下は設計案。

### 今あるもの(確認済み)

フレーズは既にこの形で保存されている:

```js
{ id, name, tags: [],
  elements: [...],   // 音符。コード名は el.chord に文字列("Dm7"等)
  timeSigIdx, keyFifths, clef, pickupUnits, staffType,
  preview,           // 一覧用のPNG
  createdAt, updatedAt }
```

**`keyFifths`(調号)が既に入っている**のが大きい。コード名と調が揃っているので、
**ディグリーは計算で出せる**。移調(`scoreTransposeAll`)もコード名を正しく書き換える形で既にある。

### 設計案: ディグリーは「導出値」にする

保存の形は3案ある:

- (a) コード名だけ保存し、検索のたびに毎回ディグリーを計算する
- (b) ディグリーを本体にして、表示時にキーから実コード名を作る
- **(c) コード名+キーを本体とし、ディグリーは索引用に計算して一緒に保存する** ←これを推す

**(b)を採らない理由**: ディグリーを一次データにすると綴りの情報が落ちる。
`A7` を `VI7` と持つか `bVII7` と持つかは文脈次第で、元のコード名に戻せなくなる。
入力もコード名(ChordWikiの略記)のままが自然。

**(a)でなく(c)にする理由**: 検索のたびに全フレーズを計算し直すのは無駄。
ただし**真実はあくまでコード名+キー**で、ディグリーはそこから作り直せるキャッシュ。
キーを変えたり移調したら作り直す。

```js
// 追加するフィールド
degrees: ['IIm7', 'V7', 'IM7'],   // 索引用。コード名+keyFifthsから計算
degreeKey: 'IIm7 V7 IM7',          // 部分一致検索用に連結した文字列
comment: '',                       // 自由記述
youtube: { url: '', startSec: 0, endSec: null },
```

### ディグリー表記の決め方

**根音**: キーの主音からの半音差で12種類
`I bII II bIII III IV #IV V bVI VI bVII VII`

**品質**: コード名から根音を取り除いた残りをそのまま使う
(`m7` `7` `M7` `m7b5` `dim` `sus4` `6` `add9` `aug` `9` `13` ...)

**分数コード**: ベースも度数にする。`G7/B`(キーC)→ `V7/VII`

例(キーC): `Dm7 G7 CM7` → `IIm7 V7 IM7`

### 検索

- **単体**: `IIm` を含むフレーズ(品質は前方一致。`IIm7` も `IIm9` も引っかかる)
- **進行**: `IIm7 V7 IM7` の**連続した並び**を含むフレーズ
- **ゆるい進行**: `2-5-1` / `II V I` のように度数だけ指定。品質は問わない
- 入力は「2-5-1」でも「IIm7 V7 IM7」でも受ける(内部で正規化)
- 一覧では、出現するディグリーをチップで並べて押して絞る(今のタグ絞り込みと同じ形)

### 決まったこと(2026-08-20 確認済み)

1. **本命の使い方は「コードからフレーズを逆引き」すること。**
   「なんのコードの時になんのフレーズが使えるのか」をディグリー単位で洗い出したい。
   つまり索引の向きは **ディグリー → フレーズ**。
   - 例: 今 `Dm7` が来た(キーC) → 度数は `IIm7` → `IIm7` で使えるフレーズ一覧
   - **出てきたフレーズは今のキーに移調して見せる**。そのまま弾けないと意味がない
   - 進行でも同じ: `2-5-1` を指定 → その並びで使えるフレーズ一覧
2. **キーはフレーズ単位で固定**(今の `keyFifths` のまま。途中転調は扱わない)
3. **検索の入力は文字とチップの両方**
4. 着手順は任せる → **範囲選択(スマホ) → ディグリー化** の順で進める
   (フレーズを切り出す入口が塞がっていると、辞書を作っても中身が増えないため)

### 逆引きの画面(たたき台)

フレーズ集に「コードから探す」を用意する:

- キーを選ぶ(既定は最後に使ったキー)
- コード名を打つ(`Dm7`)か、ディグリーのチップを押す(`IIm7`)
- 一致するフレーズが**選んだキーに移調された状態**で並ぶ
- 進行で探す時は `2-5-1` / `IIm7 V7 IM7` のように入力

### 併せて: 楽譜の範囲選択がスマホでできない

複数選択・コピー・切り取り・貼り付けは実装済みだが、**範囲選択が Shift+タップ**
(`drag.shiftKey`)なので、**物理キーボードの無いスマホ・タブレットでは範囲を選べない**。
フレーズを切り出して集める使い方をするなら、ここは先に直した方がよい。

案:
- **「範囲選択」トグルボタン**を出し、オンの間は「始点をタップ→終点をタップ」で範囲になる
- (別案)音符を長押しで範囲選択を開始

トグルの方が今のツールバーの作りに素直で、押している間だけの状態も分かりやすい。
**こちらを先に実装する。**

---

## 「同期キー」をやめて「あなた用のURL」にした(2026-08-20 実施)

### なぜ

「同期キー」は**初見殺し**だった。アプリを一度も見ていない人に、開いた瞬間
「同期キーがまだ設定されていません」と出していた。キーという言葉から
「パスワードを発行してもらう手続き」を想像させるが、実態は
**URLの末尾に `?key=...` が付いているだけ**で、手続きは何もない。

### やったこと

言い方を、実態そのままの「**あなた用のURL**」に統一した。

| 前 | 後 |
| --- | --- |
| ☁ クラウド同期 | ☁ クラウド保存(あなた用のURL) |
| 同期キーを設定 | 別のURLに切り替える |
| 他の端末用リンクをコピー | あなた用のURLをコピー |
| 「同期キーを入力してください」 | 「あなた用のURLを貼り付けてください」 |
| 「同期キーがまだ設定されていません」 | 「このURLは、まだあなた用のURLになっていません」 |

- **URLを丸ごと貼っても、`key=` の後ろだけを貼っても、どちらでも通る**
  (`extractSyncKey()`)。利用者に見えているのはURLなので、URLを貼るのが
  自然な操作になる。これが概念の置き換えの実体
- 解除時の文言も「解除しました」から「この端末だけの利用に戻しました。
  データは端末内に残っています」に変えた。消えると誤解されないように
- 「普通に開いた場合は端末の中だけに保存され、それでも全機能が使える」ことを
  マニュアルの冒頭に明記した。クラウドは**必須ではない**

### まだ決めていない

- **新規の人が自分のURLを手に入れる手段が無い。** 許可済みのURL(`allowed_keys`)は
  サーバー側(D1)で追加する運用のままなので、いま一般公開すると新規の人は
  全員「端末内だけ」で使うことになる。
  Workerには `generateSpaceKey()` を残してあるが、**まだどこからも呼んでいない**
  - 自動発行できるようにすると、見ず知らずの人が自分のD1/R2に書き込めることになる。
    容量と費用の話なので、**やるかどうかは持ち主の判断**
  - やらない場合でも、一般公開する以上は「クラウド保存は今のところ作者用」と
    どこかに書いておいた方がよさそう

---

## みんなのフレーズ(公開フレーズ集) ※未着手・2026-08-20 追加

### 要望

「フレーズは公開できてもいいかもね。みんなのフレーズで良いのを取り込んで
取得していく感じ。」

### なぜこれは筋がいいか

- **ディグリー索引が既にある。**「II-V-I のフレーズ」を**全員の投稿を横断して**
  引けるようになる。これが公開する意味そのもの。1人分だと索引はありがたみが薄いが、
  集まると辞書になる
- **初アクセス者に見せるものができる。**いま曲0件で開いた人は空白の画面しか無い
  (→ 上の「ツアー」の節)。「みんなのフレーズ」は**登録も設定も無しで今すぐ役に立つ**
  数少ない入口。ツアーの着地点として強い
- **譜面の共有より риск が低い。**中身は利用者が自分で書いた音符・コード名・コメントと
  リンクで、ChordWikiのスクショのような取り込み画像ではない

### ただし気をつける点

- **リックは元をたどれば誰かの演奏の採譜。** 2〜4小節の抜粋＋自分のコメント、という
  形なら引用として説明しやすいが、コメント無しでリックだけを大量に並べると
  そうは言いにくくなる。**コメント欄を「おまけ」ではなく主役に置く**設計にしたい
  (「どこで使うか」「何が効いているか」を書かせる)。YouTubeリンク＋秒数は
  リンクを張っているだけなので問題にならない
- 元ネタの曲名・演奏者を書く欄があった方がいい(出典が書いてある方が引用として自然)

### v1の方針: **読むのは全員、出すのは持ち主だけ**

いきなり誰でも投稿できるようにすると、**荒らし対応の当番が発生する**。
数十人規模でも、報告を受けて消す人が要る。一方で「読む・取り込む」は
取り込み先が端末内(IndexedDB)なので、**あなた用のURLが無くても成立する**。

そこで v1 は非対称にする:

| | 誰ができるか | 置き場所 |
| --- | --- | --- |
| 見る | **全員**(URL不要) | サーバー |
| 取り込む | **全員**(URL不要) | 自分の端末内 |
| 公開する | **今のところ持ち主だけ** | サーバー |

- 当番が要らない。自分で入れた物しか載らないので、消す判断も自分だけ
- **データの持ち方は開放しても変わらない**ので、後から投稿を開ける
- 最初は自分のリックを何十件か入れておく。それだけでも辞書として成立する

### 開放する時に最低限そろえるもの

- **通報ボタン**(既にある `POST /api/feedback` に `kind: 'report'` を足せば足りる)
- 持ち主が任意の公開フレーズを消せる手段
- 公開の連投制限(フィードバックと同じIPハッシュ方式を使い回す)
- 利用者が書いた文字は**絶対に innerHTML で入れない**。
  既存のフレーズ表示は確認済みで、名前・コメント・タグはすべて `textContent`
  経由なので問題無し(`innerHTML` は一覧を空にする `= ''` にしか使っていない)。
  公開フレーズを描くコードでもこの書き方を崩さないこと

### 作りの要点

- **公開時も取り込み時も「複製」にする。参照にしない。**
  - 自分の手元のフレーズを消したり直したりしても、他人が取り込んだ物は変わらない
  - 取り込んだ側はどうせ転調したりコメントを足したりして自分の物にする
  - ただし `sourceId` は残す。出どころの表示と、「取り込み済み」の目印に使う
- **プレビューPNGは公開側に持たせない。**フレーズは一覧用に
  PNGのdata URLを1枚持っているが、これを公開プールでD1のTEXTに溜めると
  すぐ重くなる(`004_music_phrases.sql` に書いた「件数が増えたらR2へ」が
  ここで効いてくる)。**公開側は `elements` だけ持ち、表示は端末側で
  VexFlowに描かせる**(VexFlowは同梱済みなので追加の取得は要らない)
- 公開するもの: 名前・タグ・`elements`・拍子/調号/音部記号/アウフタクト/譜表・
  `degrees`・コメント・出典(曲名/演奏者/リンク+秒数)
- 公開しないもの: 個人が分かるものは一切。**投稿者名は v1 では持たない**
  (名乗る欄を作ると、そこが荒らしの入口になる。必要になってから足す)
- 検索は**手元のフレーズ集と同じディグリー検索をそのまま使う**。
  スケールでの束ね方(`SCORE_FAMILY_LINKS`)も同じ物を使えば、
  「手元では出るのに、みんなのフレーズでは出ない」というズレが起きない

### 未確定

- **一覧の並び順。**新着順で十分か、「取り込まれた回数」を数えるか
  (数えるなら取り込み時にpingが要る。使用状況の記録と同じ作りでできる)
- 取り込む単位。1件ずつか、「II-V-I のフレーズ20件をまとめて」もありか
- 手元のフレーズ集と混ぜて表示するか、別画面にするか
  (混ぜると「これは自分のか、拾ってきたのか」が分からなくなるので、
   別画面 + 取り込んだ物にだけ印、が無難だと思う)
