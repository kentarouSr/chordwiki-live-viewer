# 楽譜エディタ(スコアエディタ)の記譜法についての考え方

このファイルは `index.html` 内の「+ 楽譜」機能(`openScoreEditor` 以下)が
何を正しい記譜法として採用し、何を意図的に簡略化しているかを記録する開発メモ。
コード自体を読めば分かることは書かず、「なぜそうしたか」「どこまでが正式なルールで
どこからが割り切りか」を残す。

2026-08、ユーザーから「簡略化しないで、ちゃんとした楽譜を自動で作れるようにして」
という指示を受け、拍子・調号・小節線・段の自動折り返しに対応する大幅な書き換えを行った
(それ以前のv1は拍子・小節・調号を持たない単一行の五線だった)。

その後、複数段対応のタップ判定バグを修正した直後、ユーザーから「その他にもダメな
ところが多い、まず楽譜のルールを調べて資料に起こしてほしい」という指示を受け、
連桁(混在音価)・タイによる小節線をまたぐ分割・休符の特例・ト音記号の正しい形について
出典付きでリサーチした。**そのリサーチ結果(正しいルールの定義)は `SCORE_ENGRAVING_RULES.md`
にまとめてある。実装をこのファイルの内容と食い違う形に変更する時は、まずそちらを
確認すること。**

## 【最重要】2026-08-15: 描画エンジンをVexFlow(記譜ライブラリ)へ全面移行

Bravuraフォント直描画への移行後も、「加線音符の符幹延長」「臨時記号の横幅未考慮」
「未完成の小節にも小節線が出る」等、記譜法の細かい不具合が次々見つかった。ユーザーが
「記譜のルールをAIで実装するのむずそうだな。ネットで流用出来そうなものないか探して」
と提案し、調査の結果 **[VexFlow](https://github.com/0xfe/vexflow)**(MITライセンス、
2010年開始・16年の実績・週4万DL、`vendor/vexflow/`に同梱)へ全面移行した。

**これ以降、このファイルの「◯◯を自前で描画する」「◯◯のバグを直した」系の記述の
大半は歴史的経緯として残しているだけで、現在の実装には当てはまらない。** 現在の実装は
下記の「現在のアーキテクチャ」を参照すること。

### なぜVexFlowか

- 連桁のグループ化・傾き、符幹の向きと長さ(加線での延長含む)、臨時記号の表示要否、
  レジャー線、小節線、3連符ブラケット、タイの曲線 — このセッションで延々と手で
  直し続けていた項目を**全部ライブラリ側が正しく処理する**
- MITライセンスで無料・改変・同梱可。pdf.js(Apache 2.0)・Bravura単体(SIL OFL)と
  同じ「vendor/に固定バージョンを同梱する」パターンがそのまま使え、
  「完全オフライン動作必須」という要件も維持できる
- 候補比較(abcjs/OpenSheetMusicDisplay/Verovio/alphaTab)の結果、VexFlowは
  オブジェクトを組み立てるAPIで`scoreElements`からの変換が自然、Verovioは
  WASM込み約7MBと重すぎる、Flat.io/Soundslice等はオンライン専用SaaSでオフライン
  要件に反する、という判断だった

### 現在のアーキテクチャ

- `vendor/vexflow/vexflow.js`(約1.1MB、UMDビルド。プレーンな`<script>`タグ読み込みで
  `window.VexFlow`が使える)を`loadVexFlow()`で遅延読み込みする(スコアエディタを
  開くまでは読み込まない)。
- VexFlow 5は内部でBravura(記譜フォント)+Academico(補助テキスト用フォント)を
  必要とするが、CDNからの`loadFonts()`は使わずオフライン対応のため
  `vendor/vexflow/fonts/`に自前でホスティングし、`@font-face`+`VexFlow.setFonts()`
  (同期・fetchなし)で読み込む。フォント自体はどちらもSIL OFL 1.1
  (`@vexflow-fonts/bravura`・`@vexflow-fonts/academico`パッケージから取得)。
- **このアプリ側が引き続き自前で担当する部分**(VexFlowが自動でやってくれない):
  - タップ入力→`scoreElements`(step・duration・accidental等)の蓄積は従来どおり
  - 小節線をまたぐ音価の自動タイ分割(`buildScoreRenderQueue`、`scoreDecomposeToNotatable`)
    はVexFlowの範囲外なので継続して自前で行う(VexFlowは「小節ごとに正しい音価の
    音符を渡してもらう」ことを前提にしたAPIで、勝手にタイ分割はしてくれない)
  - 実効臨時記号(調号+その小節内の直前の臨時記号を考慮した「実際に鳴っている音」)の
    計算(`computeScoreLayout`内)。**表示するかどうか**(調号どおりなら省略、等)は
    `VF.Accidental.applyAccidentals(voices, keyName)`が自動判定するので、こちらは
    「正しい音を渡す」ことだけ考えればよい
  - 複数段(システム)への折り返し(`scoreComputeSystems`)。VexFlowは1段の幅計算
    (`Formatter.preCalculateMinTotalWidth`・`Stave.getNoteStartX`)はしてくれるが、
    どこで折り返すかの判断はしない
  - タップ入力のY座標→音高(step)変換。VexFlowが実際に描画した`Stave`の
    `getYForLine()`/`getSpacingBetweenLines()`から逆算する(`currentSystemTapBand()`)
- **VexFlowに完全に任せている部分**: 五線・ト音記号・調号・拍子記号・小節線・符頭・
  休符・臨時記号の表示要否と位置・付点・符幹の向きと長さ・連桁のグループ化と傾き・
  3連符のブラケット・タイの曲線・コード名注記(`VF.Annotation`)

### 実装時にハマった不具合(2026-08-15)

- **`ctx.fillStyle`の汚染でVexFlowの文字・符頭が透明に近い色で描かれる**: 「今タップ
  すると音符が入る段」を示す薄い水色ハイライト(`ctx.fillStyle='rgba(108,204,255,0.12)'`)
  を描いた後、リセットせずに`VF.CanvasContext`を作って渡していたため、VexFlowの
  `fillText`ベースの描画(符頭・ト音記号・数字・注記等、フォントに依存する全て)が
  この薄い色を継承してしまい、ほぼ見えない状態になっていた(符幹・連桁・タイ・
  ブラケットのようなベクター線画だけは`strokeStyle`を使うため影響を受けず、結果的に
  「符頭が消えて棒だけ残る」という分かりにくい壊れ方になった)。**教訓: Canvas 2D
  contextの`fillStyle`/`strokeStyle`はグローバルな状態なので、外部ライブラリに
  contextを渡す前に必ず既知の状態にリセットする。**
  修正: ハイライト矩形を描いた直後に`ctx.fillStyle = '#000'`へ戻してから
  `new VF.CanvasContext(ctx)`を作るようにした。
- **Formatterインスタンスの使い回しで描画が二重に崩れる**: 段への折り返し幅を見積もる
  段階で`new VF.Formatter().joinVoices([voice]).preCalculateMinTotalWidth([voice])`を
  呼んだ後、同じFormatterインスタンスを実際の描画用`format()`にも使い回していたところ、
  符頭・臨時記号・注記が二重に配置されたような崩れた見た目になった。
  修正: 幅の見積もり専用のFormatterと、実際の描画用のFormatterを完全に分離した
  (`drawScoreFromLayout`側で毎回`new VF.Formatter()`を作り直す)。
- **音符が1つも無い(入力前・全消去直後)状態でタップが一切効かない**: 段への折り返し
  ロジックが「小節が1つも無い時は空のシステム(`[]`)を1つ入れる」という形で五線の
  存在自体は保証していたつもりだったが、実際の描画ループ(`measureIdxs.forEach`)は
  小節数が0だと1回も実行されず、`Stave`オブジェクトが一切作られない
  (`scoreLastStaves`に`null`が入る)ままだった。タップ処理は`currentSystemTapBand()`
  が返す`Stave`を前提にしているため、空の状態では永久にタップが無視され、
  最初の1音すら入力できないという致命的な不具合だった。ヘッドレスブラウザで実際に
  タップをシミュレートして`computeScoreLayout().queue`が空のままなことに気づき発覚。
  修正: 小節が0個の段では、音符無しの「空のStave」(ト音記号・調号・拍子記号だけ)を
  明示的に作って`scoreLastStaves`に入れるようにした。
- **VexFlowのAPIはバージョンで大きく変わる**: `StaveTie`のプロパティ名は
  `first_note`/`last_note`(v3/v4系)ではなく`firstNote`/`lastNote`(v5、キャメルケース)。
  幅の事前計算は`Formatter`の**静的メソッドではなくインスタンスメソッド**
  (`new Formatter().joinVoices([voice]).preCalculateMinTotalWidth([voice])`)。
  フォントは`VexFlow.loadFonts()`(CDN、非同期)と`VexFlow.setFonts()`
  (既に`@font-face`で読み込み済みのフォント名を渡すだけ、同期)の2通りがある。
  **教訓: ライブラリのAPIは記憶や一般的な思い込みに頼らず、実際にヘッドレスブラウザで
  小さいコード片を動かして検証してから本実装に組み込む**(このセッションを通して
  何度も出てきた教訓だが、外部ライブラリ導入時は特に重要だった)。

### 続報: 調号表示の簡略化・コード名表記の改善・既存音符のタップ編集(2026-08-15)

VexFlow移行を見せた後、ユーザーから3点の指摘・要望。

1. **調号の表示ラベルを簡略化**: それまで`SCORE_KEY_OPTIONS`の`label`は
   「ト長調(G) ♯1」のような長い日本語だったが、「キーはBbやCのみの記載で良い。
   シャープの数くらい分かるっしょ」との指摘で、`label`を単純にキー文字("G"等)
   だけにした。実装済みだった`vexName`フィールドをそのまま流用できたので変更は
   配列リテラルの書き換えのみ。
2. **コード名の表記を実際のコード譜スタイルに**: 添付されたリズム譜(マスター譜)
   の画像で「E♭Δ9」のようにルート音は通常サイズ、コードの種類・テンションは
   小さく上付き、という表記を見せられ「添付画像の感じになるといいかも」と要望。
   `VF.Annotation`(単純な1サイズのテキスト)から`VF.ChordSymbol`に切り替えた。
   フルパーサーは組まず、正規表現でルート音(A〜G+任意で#/♭)だけ切り出して
   通常サイズで`addText`、残り全部(m7・△9・7♭9等)を`addTextSuperscript`で
   上付きにするだけの単純な分割で、実際のコード譜にかなり近い見た目になることを
   ヘッドレスブラウザで確認済み(△や♭のようなUnicode記号もそのまま上付きテキストとして
   綺麗に描画されるため、専用グリフへの変換は不要だった)。
   **ここで実装ミスを1つ踏んだ**: `ChordSymbol`にも`Annotation`と同じ
   `setVerticalJustification()`があるものと思い込んで書いたところ実際には存在せず
   (`ChordSymbol`は`setVertical()`)、コード名付きの音符がある譜面の描画が丸ごと
   例外で止まっていた(タップで音符を置く処理自体は例外の外なので動くが、
   直後の`renderScoreCanvas()`が全部失敗するため画面には反映されない、という
   分かりにくい壊れ方をした)。ヘッドレスブラウザでのタップ再現テストで発覚・修正。
   VexFlowの各クラスは似ているようで持っているメソッド名が微妙に違うため、
   毎回`Object.getOwnPropertyNames(Class.prototype)`で実際のメソッド一覧を
   確認してから使うべきだった、という教訓の再確認。
3. **既存の音符をタップして選び直し、その場で直せる機能を追加**: それまでは
   「↩ 直前を削除」で末尾の1個を消すことしかできず、「直前の音しか編集できない
   仕様が微妙」「タップした音が直せるようにしたい」との指摘。実装:
   - 描画時(`drawScoreFromLayout`)に、各音符(`scoreElements`のインデックス基準、
     タイ分割前の元の音符単位)の画面X座標と段番号を`scoreLastNotePositions`に
     記録しておく(Y座標は毎回`step`から計算するので保存不要)。
   - タップ処理(`pointerdown`)を3分岐にした: ①タップ位置が既存の音符に近ければ
     (`scoreHitTestNote()`、当たり判定半径は`SCORE_STAVE_LINE_SPACING*1.6`)、
     その音符を選択(同じ音符を再タップすると選択解除)。②既に音符を選択中なら、
     今回のタップ位置をその音符の新しい音高として反映し、選択解除。③どちらでも
     なければ、これまでどおり「今の段」の末尾に新しい音符を追加。
   - 選択中は音価ボタン・♯♭♮ボタン・コード名入力欄が「次に置く音符の設定」では
     なく「選択中の音符をその場で直す」操作になるよう`updateScoreToolbarUI()`と
     各ボタンのイベントハンドラを選択状態で分岐させた。「3連」ボタンだけは、
     複数音符にまたがるグルーピングの組み替えが必要になり複雑になるため既存音符への
     適用は非対応のままにした(次に置く音符にのみ効く、という従来の挙動のまま)。
   - 選択中の音符は`note.setStyle({fillStyle:'#2b6cb0', strokeStyle:'#2b6cb0'})`で
     青く色付けして視覚的に分かるようにした。「↩」ボタンは選択中だけ
     「✖ 選択中の音符を削除」に表示を変え、末尾ではなく選択中の音符自体を
     `scoreElements.splice()`で削除するようにした。
   - ヘッドレスブラウザで実際にタップ→選択→音価変更→別の場所を再タップして
     音高変更、という一連の流れを検証済み(スクリーンショットで選択中の音符が
     青く表示されることも確認)。

## スコープの前提

- 単音メロディー一本のみ(和音進行やポリフォニーは非対応)。コードネームは
  音符の上にテキストとして添えるだけで、和音として鳴らしたり複数符頭を積んだりはしない。
- 演奏(音再生)はしない。あくまで「後で人間が読んで弾ける・歌える」ための視覚表現。
- 1つの楽譜(1回のエディタ起動〜完了)につき、拍子・調号はそれぞれ1つだけ。
  曲の途中で拍子・調号が変わるような曲には対応しない(UIの複雑化を避けるための
  意図的な割り切り。単音メロディーの手早いメモという用途では十分と判断した)。
- 目的は彫刻譜(プロの版下)のような美しさではなく、素早く書けて誤読されない明瞭さ。
  そのため装飾的な要素(スラー、強弱記号との統合レイアウトなど)は非対応。

## データモデル

- 音高は MIDI 番号ではなく `step`(五線上の段階、整数、**E4 = 0**)で持つ。
  再生機能が無いため「実音高」ではなく「五線上のどこに描くか」だけが必要で、
  step だと `Y = baseY - step * 半行間隔` の単純な一次式でピクセル変換できる
  (`baseY` は段(システム)ごとのE4のY座標。段が複数あるので固定値ではなくなった)。
- 臨時記号(`accidental: 'sharp'|'flat'|'natural'|null`)は `step` とは独立に持つ。
  実際の記譜でも♯♭♮はそのstepの音を修飾するだけで、段位置自体は動かさない。
  `natural` は「調号で♯/♭が付く音を、この音だけ本位に戻す」ためのもの。
- 付点(`dotted`)・3連符(`triplet`/`tripletGroup`/`tripletFirst`)・音価(`duration`)は
  それぞれ独立フィールド。`tripletFirst` はグループの先頭要素だけに立てるフラグで、
  「3」ラベルを音符ごとではなくグループに1回だけ描くために使う。
- 拍子(`scoreTimeSigIdx` → `SCORE_TIME_SIGS`)と調号(`scoreKeyFifths`、五度圏の
  fifths値。+が♯系、-が♭系)はエディタ全体で1つずつのグローバル状態。

## レイアウト計算は `computeScoreLayout()` に一本化

小節分割・臨時記号の要否・連桁のグループ化・段(システム)割りは、バラバラに
その場で計算すると前提がズレて事故る(実際、旧v1はビームと小節を別々に考えていて
拍をまたぐビームが放置されていた)ので、1つの関数で順番に計算して1つのlayout
オブジェクトを返し、`drawScore()` はそれを描くだけにした。

1. **小節分割**: 拍子から「1小節の容量」(全音符=1単位として `beats/unit`)を求め、
   音価の合計がこの容量に達する/超えるまで貪欲に音符・休符を1小節に詰める。
   達した/超えた音符の直後で区切る。
2. **臨時記号の表示要否**: 明示的に♯♭♮を指定した音符は必ず表示し、以後その小節内で
   同じ段位置(＝同じ音)に効く。無指定の音符は、その小節内で初出であれば
   (＝調号どおりであれば)何も描かない。臨時記号は小節線を越えて持ち越さない
   (実際の記譜法どおり)。
3. **連桁のグループ化**: 「拍のまとまり」の境界(単純拍子は1拍=1/unitごと、
   6/8等の複合拍子は付点4分=3/8ごと)を`scoreBeatBoundaries()`で求め、これを
   跨がない・同音価が連続する8分/16分音符だけを連桁でまとめる。
4. **段(システム)割り**: 各小節の必要幅(音価比例幅の合計)を求め、段の内容幅
   (`scoreSystemContentWidth()`、コンテナ幅からト音記号・調号のぶんを引いた値)に
   収まるだけ貪欲に小節を詰めていく。段が変わるごとにト音記号・調号を描き直す
   (実際の楽譜と同じ)。

## 描画で守っている記譜法ルール

- **符幹の向き**: 五線中央(第3線, step=4)より下の音は符幹を上向き・右側に付け、
  上の音は下向き・左側に付ける。全音符には符幹を付けない。
- **連桁(beam)**: 上記の拍のまとまりを跨がない範囲で、連続する8分音符・16分音符は
  個別の旗ではなく符幹を繋ぐ横棒(連桁)でまとめる。16分音符は連桁2本。
- **休符の縦位置**: 全休符は第4線からぶら下がる、2分休符は第3線(中央線)の上に乗る。
  4分休符以下は独自のくねり形+旗で表現。
- **音価に比例した横幅**: `SCORE_DURATION_WIDTH_UNITS`で音価ごとの相対幅を持ち、
  `scoreElementWidth()`で実ピクセル幅に変換して各要素・各小節の幅を決める。
- **調号**: 五線冒頭にト音記号の右に描く。♯系は F,C,G,D,A,E,B の順、♭系は
  B,E,A,D,G,C,F の順で増える(五度圏どおり)。各文字をどの段位置(線/間)に描くかは
  実際の記譜法の慣習で決まっている固定パターンなので、計算式ではなく
  `SCORE_SHARP_STEPS`/`SCORE_FLAT_STEPS` にそのまま配列で持たせている。
- **小節線**: 小節の終わりに自動で挿入。常に普通の単線のみで、終止線(二重線)は
  一切描かない(理由は後述の「直したバグ: 終止線(二重線)を描いていた」参照)。
- **臨時記号は符頭の左**、**コード名は五線の外側(上)**、**3連符ラベルはグループ先頭のみ**。

## タップ入力と複数段

エディタは常に末尾に音符を追記していく方式。当初(初回リリース時)は「タップされた
段(システム)がどれであるかは無視して良く、タップY座標を段の高さ+段間隔の周期で
割った余りだけ見れば音の高さが求まる」という設計(`scoreYToStepAnySystem()`)に
していたが、**これは実際に長いメロディーを入力すると壊れることが判明した**
(詳細は次項)。現在は、常に「今から音符を追記する対象である最後の段」の実際の帯域
(`currentSystemTapBand()`、その段の上のレジャー線余白〜下のレジャー線余白の範囲)
だけをタップ受付の対象とし、それ以外(既に確定した前の段・段と段の間の余白)への
タップは無視する。加えて、今タップすると音符が追加される段には薄い水色の背景を
敷いて視覚的に分かるようにしてある(`drawScore()`冒頭)。

### 直したバグ: 複数段になった状態でタップすると全然関係ない音高の音符が紛れ込む(2026-08-15)

ユーザーから「全然楽譜がちゃんと書けていない」とスクショ付きで報告があった。
画像では、五線から完全に外れた場所に音符・連桁の塊が浮いているように見えた。

**原因**: 上記の「どの段をタップしても周期の余りで音高を決める」設計は、数式としては
正しかった(実際、あるタップのcanvas上のY座標が分かれば、そこから求まる音高は
どの段にいたとしても一貫していた)。しかし、これは「ユーザーは常に画面に表示されて
いる『今の段』だけをタップする」という前提の上に成り立つ設計であり、**段が複数
できて画面をスクロールする必要が出てきた瞬間から前提が崩れる**: 五線譜エリアが
スクロール可能になると、画面には「既に確定した前の段」と「今の段」が同時に見えたり、
オートスクロールの影響で「さっきタップしたのと同じ画面上の位置」が実は別の段を
指すようになったりする。ユーザーがそれと気づかずに前の段や段の間の余白をタップして
しまうと、アプリはそれを普通に受理し「今の段のその相対位置」として解釈して音符を
追記していた(常に末尾に追記、という仕様のため、どこをタップしても機械的に音符が
1つ増える)。その結果、意図しない極端に高い/低い音高の音符が本来の続きに混ざり込み、
それが8分/16分音符で連続すると連桁で繋がって「五線から浮いた塊」に見えていた。

**再現方法**: ヘッドレスブラウザで、実際のユーザーがやりそうな「画面上のだいたい
同じ高さを繰り返しタップする」操作をシミュレートしたところ、段が1つの間は正常な
音高(3〜7の範囲)が入るが、段が増えてスクロールが必要になった瞬間から明らかに
おかしい音高(-7〜-6等)が混入することを`computeScoreLayout()`の生データで確認できた
(スクリーンショットに写っていた「浮いた塊」と一致する視覚的症状も再現できた)。

**修正**: `scoreYToStepAnySystem()`を廃止し、`currentSystemTapBand()`(最後の段の
実際のY範囲を返す)を新設。タップ時にこの帯域の外なら何もしない(音符を追加しない)
ようにした。あわせて、今タップが効く段が視覚的に分かるよう薄い水色のハイライトを
追加した(ユーザーが「ここをタップすればいい」と一目で分かるようにするための予防策。
修正しても『間違った場所をタップして無視される』こと自体は起こり得るので、そもそも
間違えにくくする方が良いと判断した)。修正後、同じ「同じ画面位置を連打」シナリオで
段外のタップが正しく無視され、変な音高が混入しなくなったことを確認済み。

## 記譜フォント(Bravura)による描画への移行(2026-08-15)

「連桁をもっと詳しく調べてケースと回答を作って」「五線譜や音符の見た目がしょぼい」
という指摘を受け、`SCORE_ENGRAVING_RULES.md`のリサーチ内容を実装した。手描きCanvas
(ベジエ曲線で符頭・ト音記号・休符を自前で描く方式)をやめ、SMuFL準拠の記譜フォント
`Bravura`(`vendor/bravura/BravuraText.woff2`、SIL Open Font License、`vendor/pdfjs/`
と同じ「外部CDNに頼らず同梱する」パターン)を`@font-face`で読み込み、符頭・ト音記号・
休符・臨時記号・旗は全て`ctx.fillText()`でこのフォントのグリフを描画する方式に
変更した。コードポイントは`SCORE_GLYPH_CODE`定数にまとめてある(W3C SMuFL公式の
`glyphnames.json`で確認済み)。符幹・連桁・小節線・レジャー線・タイの曲線・3連符
ブラケットは音価/音高で長さや形が変わるため引き続き自前で描く。

寸法比率もSMuFL標準(`SCORE_STEM_LEN = SCORE_LINE_SPACING * 3.5`等、詳細は
`SCORE_ENGRAVING_RULES.md`の表を参照)に揃えた。符幹が約2倍長くなった分、段の
上下余白(`SCORE_SYSTEM_TOP_PAD`/`BOTTOM_PAD`)も広げてある。

同じタイミングで、以下の記譜ルールも実装した(詳しいルールと出典は
`SCORE_ENGRAVING_RULES.md`参照):

- **混在音価の連桁(部分連桁・フック)**: `computeBeamGroups`(現`computeScoreLayout`
  内)は、もう「同じ音価が連続する区間」だけでなく、8分/16分が混ざっていても拍の
  まとまりを跨がない限り1つの連桁グループにするようになった。`drawBeamGroup()`が
  グループ内で主連桁(常に全体を1本)+副連桁(16分どうしが隣接する区間だけ、
  孤立した16分はグループ中心向きの短いフック)を描き分ける。
- **連桁の傾き(スラント)**: `drawBeamGroup()`内で、グループ最初と最後の音符の
  高さの差から傾きを求める(`SCORE_MAX_BEAM_SLANT`で誇張しすぎないよう上限あり)。
  同じ高さなら水平、上昇形なら右上がり、下降形なら右下がりになる。
- **3連符のブラケット**: 3連符のメンバー(`tripletGroup`が同じ音符群)が、4分音符
  混入等の理由で1つの連桁グループにまとまらなかった場合、`drawTripletBracket()`が
  連桁の代わりに角カッコ+「3」を描く。全メンバーが1つの連桁で繋がる場合は
  (実際の記譜どおり)従来の「グループ先頭に「3」を1つだけ」表示で足りるため
  ブラケットは描かない。
- **1小節まるごと休みは全休符記号**: `computeScoreLayout()`が各小節を見て
  「1つの休符だけで小節の容量ぴったり埋まっている」場合を`fullMeasureRest`として
  マークし、`drawScoreRest()`はそのフラグが立っていれば実際に選ばれた音価
  (付点2分等)に関わらず全休符のグリフを描く(拍子に関わらない実際の記譜法の慣習)。
- **小節線をまたぐ音価の自動タイ分割**: `buildScoreRenderQueue()`が、ユーザーが
  入力した`scoreElements`(手を加えない)を元に、小節の容量を超える音符を
  「小節末までの長さ」+「残りの長さ」に分割した描画用の列(queue)を作る。半端な
  残りは`scoreDecomposeToNotatable()`で記譜可能な音価(付点付き1つ、またはタイの
  連鎖)に分解する。`computeScoreLayout()`以降の全処理(小節分割・臨時記号・連桁・
  段割り)はこのqueueに対して行う。タイの曲線(`drawTie()`)は同じ段の中でだけ描く
  (段をまたぐタイの弧はv1では非描画。音価自体は正しく分割されているので音楽的な
  正しさは保たれるが、見た目の弧が出ない、という制約)。

### 直したバグ: 符幹と符頭の間に隙間ができる(2026-08-15、フォント切り替え直後)

ユーザーが実機(iPad)のスクショで「棒とオタマジャクシがもう少し近くにならない？」と
指摘。**原因**: 符頭の描画原点・符幹の接続位置を決める`SCORE_NOTEHEAD_HALF_WIDTH`
(符頭の見た目の半分の幅の目分量、`SCORE_LINE_SPACING * 0.52`固定)が、実際の
Bravuraフォントの符頭グリフの形状と合っていなかった。手描き図形と違い、フォントの
グリフは中心対称とは限らず、符幹の接続位置も目視の「符頭の端」とは微妙にズレた
場所に設計されている。**修正**: Bravuraの公式メタデータ(`redist/Bravura.json`の
`glyphsWithAnchors`)を取得し、`noteheadBlack`/`noteheadHalf`の実際のアンカー座標
(`cutOutNW`/`cutOutSE`=符頭の見た目の輪郭、`stemUpSE`/`stemDownNW`=符幹接続点、
いずれもスタッフスペース単位)を確認した上で、符頭の描画原点(`SCORE_NOTEHEAD_CENTER`
/`SCORE_NOTEHEAD_WHOLE_CENTER`)と符幹接続オフセット(`SCORE_STEM_ATTACH_UP`/
`_DOWN`、上下で非対称)を実データから算出し直した。**教訓**: SMuFLフォントのグリフを
Canvasに`fillText`で描く時、接続点(符幹・連桁・臨時記号の位置など)は目分量ではなく
フォント配布元が提供するメタデータ(アンカー座標)を当たるのが確実。今回は最初
目分量で実装してユーザーの実機確認で気づいたが、次に同種の調整(付点の位置など)が
必要になった時も、まず`Bravura.json`のアンカーを確認してから数値を決めること。

### 直したバグ: 符頭がレジャー線・五線の位置とズレる(2026-08-15、同日続報)

ユーザーが「見た目どこおかしいか分かる？認識併せしよ」と、今度は具体的な指摘無しで
自己診断を求めてきた。実際に自分でアプリをヘッドレスブラウザで開いて確認したところ、
**レジャー線が必要な低い/高い音符で、符頭の位置がレジャー線や本来あるべき五線の
位置と数px〜1段ぶんズレて見える**ことを発見した(ユーザーからの指摘を待たずに
自分で見つけた)。

**原因**: 符頭・休符・臨時記号の描画に使っていた`ctx.textBaseline = 'middle'`が、
SMuFLフォント(Bravura)のグリフを正確に中央揃えしてくれていなかった。`middle`は
フォント全体のascent/descent(文字全般のための汎用的な指標)を基準にした中央であって、
個々のグリフの実際のインクの中心ではない。**最初「alphabetic基準に直せば正しいはず」
という理論的な仮説で修正を試みたが、実際にヘッドレスブラウザで検証し直したところ
むしろ逆方向にズレて悪化した**(理論だけで判断せず、必ず実測すべきだったという教訓)。
最終的に、Canvasの`getImageData`で実際に描画したグリフの暗いピクセルの範囲を
スキャンし、`middle`基準での実際のズレ量(符頭で約7.5px、フォントサイズ72px時)を
直接測定した上で、その分だけYに補正を加える`scoreGlyphYFix()`ヘルパーを追加して
解決した(補正量はフォントサイズに比例してスケールする)。修正後、単独のレジャー線
(1本・2本とも)・複数音符を含む一連のフレーズの両方で、`getImageData`によるピクセル
スキャンで符頭の重心が目標のY座標(レジャー線・五線の位置)と一致することを数値で
確認済み。

**教訓**:
- SMuFLフォントをCanvasで正確に位置合わせするには、`textBaseline`の標準オプション
  (`middle`/`alphabetic`等)のどれも「理論上こうなるはず」を信用せず、実際に
  `getImageData`でピクセルスキャンして実測するのが最も確実(フォントメタデータの
  アンカー座標だけでは、テキストベースラインという別レイヤーの挙動までは分からない)。
- スクリーンショットを縮小・圧縮した状態で目視するだけだと、数px〜1段ぶんのズレを
  見誤ることがある(実際、修正後に「まだズレているように見える」と一瞬誤読しかけたが、
  ピクセルスキャンの数値で「実は正しく直っている」と確認できた)。見た目の微妙な
  位置ズレを検証する時は、スクリーンショットの目視だけでなくピクセル単位の数値測定を
  併用すること。

### 直したバグ: 終止線(二重線)を描いていた(2026-08-15、続報)

上記の修正を踏まえてスクショ2枚を再確認していた際、最初は「小節の最後を太線+
細線の二重線(終止線)にする」実装(`btnScoreDone`押下=画像化する最終出力の時だけ
`isFinal=true`で二重線にする、という条件分岐)になっていた。ユーザーから明確な
指摘: 「終止線はかかないで良い。なぜなら、コード譜に貼り付ける前提だから。
メロディラインを取らなきゃ行けないところだけ楽譜を貼るの。」

**理解**: このアプリの楽譜スニペットは、常に既存のコード譜の特定箇所に貼り付ける
「メロディーの断片」であり、独立した「完結した1曲」として完成させるものではない。
そのため「曲の終わり」を示す終止線という記号自体が意味を持たない。「最終出力の時
だけ二重線にする」という当初の実装は、記譜法として一見自然に見えたが、この
アプリの実際の用途(断片の貼り付け)に対しては誤りだった。

**修正**: `drawBarline()`の呼び出しを常に`final=false`(普通の単線)に固定。
`drawScore()`/`renderScoreCanvas()`に残っていた`isFinal`引数も、もう使われないので
削除した(`btnScoreDone`のハンドラも`renderScoreCanvas(true)`→`renderScoreCanvas()`
に修正)。

### 直したバグ: 上向き符幹と符頭の間に隙間が復活していた(2026-08-15、続報)

終止線の件とは別に、ユーザーから2枚のスクショを見せられ「これは認識できてる？」
「問題はそこ以外の箇所。まずは先程の画像を確認して何が問題なのか端的に回答して」
と、終止線以外にもう一つ問題があると指摘された。

**原因**: 上記「符幹と符頭の間に隙間ができる」バグの初回修正時、`SCORE_STEM_ATTACH_UP`
/`_DOWN`をBravuraの公式アンカー`stemUpSE`/`stemDownNW`の座標そのまま
(それぞれ中心から約10.8px/10.44px)に設定していた。しかしこれらのアンカーは
SMuFL上「複数の符頭が縦に重なるクラスタ配置」のための余白込みの座標であり、
単独の符頭に対しては実際の見た目のインク端(`cutOutNW`/`cutOutSE`、中心から
約6.5px)よりも外側に位置する。その結果、符幹の接続点が符頭のインクから浮いて
しまい、特に上向き符幹(低い音)側で約4pxの隙間が生じていた(ヘッドレスブラウザで
実際にscore editorに2つの4分音符を入力し、`ctx.getImageData()`で該当座標の
ピクセル濃淡を1行ずつスキャンして確認。符頭の右端(x≈158)と符幹(x≈162-163)の間に
確かに数px分の空白があることを数値で確認した)。

**修正**: `SCORE_STEM_ATTACH_UP`/`_DOWN`を、公式アンカー(`stemUpSE`/`stemDownNW`)
ではなく`cutOutNW`/`cutOutSE`(実際のインク端、中心から約6.5px)を基準にした値
(`SCORE_LINE_SPACING * 0.30`、インク端よりわずかに内側へ重なるよう調整)に変更。
修正後、`getImageData`のflood-fillで符頭と符幹が1つの連結図形になっている
(隙間が無い)ことをピクセル単位で確認済み。

**教訓**: SMuFLのアンカー座標には複数の種類があり(`cutOut*`=見た目のインク境界、
`stemUpSE`/`stemDownNW`=符幹の理想的な接続点で、複数符頭が重なる場合の余白を
考慮した座標)、単純に「アンカーだから正確なはず」と全種類を同列に扱うと、
用途によっては視覚的に不自然な結果(今回のような隙間)になる。単独の符頭に
符幹をしっかり接続させたい場合は`cutOut*`(インクの実際の輪郭)を基準にする方が
適切。

### 符幹の接続点の縦方向のズレを修正(2026-08-15、続報2)

ユーザーから「棒と●の位置がおかしいのはわかる？」と、さらなる確認。上記の横方向の
隙間修正は`SCORE_STEM_ATTACH_UP`/`_DOWN`という**横方向のオフセットだけ**を実データ
(cutOutNW/SE)に基づかせたものだったが、符幹の描画開始Y座標は`drawScoreElement()`
(符幹なし・単独音符)でも`drawBeamGroup()`(連桁でまとめた音符)でも、常に音符の
「符頭の重心の高さ」(`y`/`ys[i]`)そのままで、縦方向の補正が一切無かった。

**原因**: 符頭グリフはわずかに傾いており、Bravuraの公式アンカー
(`stemUpSE=[1.18,0.168]`・`stemDownNW=[0.0,-0.168]`、`cutOutNW=[0.208,0.3]`・
`cutOutSE=[0.94,-0.296]`)には横方向だけでなく縦方向の成分もある。横だけ実データに
直して縦だけ据え置き、という非対称な状態になっていた。

**修正**: `SCORE_STEM_ATTACH_Y_OFFSET = SCORE_LINE_SPACING * 0.168`
(`stemUpSE`/`stemDownNW`のY成分)を新設。SMuFL座標は上方向が正、Canvasは下方向が
正なので符号を反転し、上向き符幹は`y - オフセット`、下向き符幹は`y + オフセット`を
符幹の開始Y(`stemStartY`/`stemStartYs`)として使うよう`drawScoreElement()`と
`drawBeamGroup()`の両方を修正した。連桁の傾き自体は(音の高さの見た目を保つため)
引き続き符頭の生のY座標(`ys`)から計算し、符幹の描画開始点だけこの補正を適用する形
にした。**この縦方向のオフセットは横方向と違い、符頭の絶対描画位置
(`scoreGlyphYFix`で補正済みの位置)からの相対量なので、フォント内部の原点ズレの
影響を受けずスタッフスペース比をそのまま使える**(横方向の隙間バグのような
「アンカーの種類を間違えて外側に飛び出す」問題は起きにくい)。修正後、
`getImageData`のflood-fillで(4分音符2つ+8分音符ペアの連桁を含む)全ての符頭・
符幹・連桁が1つの連結図形になっている(どこにも隙間が無い)ことと、ズームした
スクリーンショットの目視の両方で確認済み。

## 意図的に本物の記譜と異なる簡略表現・既知の制約(2026-08-15時点)

- 3連符のタイミングが拍にきれいに揃っていない(例: 前の音符列の都合で3連符の
  開始位置が拍の境界からずれる)と、連桁が途中で切れてブラケットに切り替わる
  ことがある。実用上は3連符がその名のとおり「1拍」を3等分する形で入力される
  限り問題にならない(ヘッドレステストで確認済み)。
- タイが段(システム)をまたぐ場合、曲線は描かれない(上記参照)。
- 異なる音価が混ざる連桁で、副連桁(16分の連桁)が「隣接ペアをつなぐ/孤立は
  フック」という単純なルールで決まる。3つ以上の16分が連続する場合の内部の
  グルーピング(例: 16分が5つ連続する時にどこで区切るか)までは検証していない。
- スラー・強弱記号(クレッシェンドの線など)は音符列とは別に「テキストメモの
  スタンプ」(`DEFAULT_STAMPS`: p, mf, cresc. など)として譜面画像の上に別途貼る運用。
  楽譜エディタ自身の記譜要素としては持たない。
- 1つの楽譜につき拍子・調号は1つだけ(スコープの前提を参照)。

## 【続報・大きな刷新】2026-08-17: 音符入力をピアノ鍵盤タップ方式に変更(Flat参考)

「楽譜入力機能を充実させたい。[Flat](https://flat.io/ja)というアプリを真似て実装してほしい。
今は単音メロディーのみで良いが、将来和音を足せるようにしたい」という要望を受けて実施。

### データモデルの一般化(将来の和音対応に備えて)

`scoreElements`のnote要素を `{ step, accidental, ... }` から
`{ pitches: [{ step, accidental }], ... }` (配列、今は常に1要素)に変更した。
読み取りは`elFirstStep(el)`/`elFirstAccidental(el)`という2つのヘルパー関数を必ず
経由するようにし、`el.pitches[0].step`を直接あちこちに書かないようにした
(将来複数ピッチに対応する時の変更箇所を1つにまとめるため)。`buildScoreRenderQueue`の
タイ分割(小節線をまたぐ音を複数の断片に割る処理)も、accidentalのnull化(タイの
続きに臨時記号を再表示しない)をピッチごとに行うよう変更した。

`computeScoreLayout`の実効臨時記号(`effectiveAccidental`)は、今回は据え置き
(1音符=1ピッチなので配列のままでよい)。`explicitState`マップ(「letter+octave」→
直前に効いている臨時記号)は元々letter+octaveキーだったため、和音対応時もマップ
自体の作り替えは不要で、`el.pitches`をループするだけで済むはず。ただし
`effectiveAccidental`自体は「queueインデックスにつき1つ」の配列なので、和音対応時は
(queueIndex, pitchIndex)の2次元にする必要がある(TODOコメントを該当箇所に残した)。

`scoreBuildMeasureNotes`で`keys: [scoreStepToVexKey(...)]`(常に1要素)としていた
箇所を`keys: el.pitches.map(p => scoreStepToVexKey(p.step, effAcc))`に変更。
**和音対応時の注意点をコメントで明記**: VexFlowは2度音程が隣接する時のノートヘッド
左右オフセットを`keys`配列の並び順で決めるため、複数ピッチを渡す時は`step`昇順に
ソートしてから配列を作ること(未整列だと見た目が崩れる)。同一`step`の重複ピッチも
入力側で弾く必要がある(VexFlowは重複キーを正しく扱えない)。

### 音符入力: 「五線譜タップ」→「ピアノ鍵盤タップ」

これまでは五線譜を直接タップした位置(Y座標)がそのまま音高になる方式だったが、
実際の記譜ソフトの操作感とは異なっていた。楽譜エディタのツールバー下に、横スクロール
可能なピアノ鍵盤(`#scorePianoKeyboard`、C3〜C6の3オクターブ、白鍵の下に音名ラベル)を
新設し、**音符の新規入力は鍵盤タップで行う方式に変更した**。

- **何も選択していない時**、鍵盤タップ→末尾に新規追加(今までの「五線譜タップで追加」と
  全く同じ結果を、入力元だけ鍵盤に変えたもの)。
- **既存の音符を選択中**の時、鍵盤タップ→そのピッチ(step+accidental)をまるごと上書き。
  **選択は解除しない**(自動で次へ送らない設計。ドラッグでの音高修正が「直した後も
  選択したまま」なのと挙動を揃えた。「既存音を直す」と「連続で新規入力する」は別の
  意図なので、選択の有無だけで素直に分岐させる方が事故が少ないという判断)。
- 「五線譜の空白をタップして新規追加」は**廃止**(空タップは何もしない)。「既存音符を
  タップして選択」「選択中に別の場所をタップ/ドラッグで音高を変える」は**現状維持**
  (この2つは五線譜側の既存の当たり判定コードをそのまま使っており、今回ほぼ無変更)。

黒鍵タップ時の`{step, accidental}`決定は、白鍵の並び(1オクターブ12半音、白鍵は文字
そのまま・黒鍵は[下側の白鍵文字, 上側の白鍵文字]の組)を`SCORE_PIANO_OCTAVE`として
持ち、以下のルールで解決する:

- 保留中の♯/♭(ツールバーのトグルボタン)があればそちらを優先。**方向は理論に基づいて
  固定**: ♯なら下の白鍵の文字を上げる、♭なら上の白鍵の文字を下げる(「近い方」のような
  曖昧な判定はしない)。
- 保留中が♮(ナチュラル)の場合、黒鍵は理論上ナチュラルになり得ないため**非活性
  (タップ不可・薄く表示)**にする。
- 保留中の指定が無ければ、新設した「♭↔♯ 異名同音」トグル(`scoreEnharmonicMode`、
  `'auto'|'sharp'|'flat'`の3値)の実効モードに従い同じ規則を適用する。`'auto'`は
  調号の`fifths`から自動決定(`fifths>0`→♯系、`fifths<0`→♭系、`fifths===0`のハ長調は
  多くの記譜ソフトの慣習に合わせて♯をデフォルトにした)。単純なON/OFFではなく3値に
  したのは、一度手動で切り替えた後も「調号に自動追従する状態」へ明示的に戻せるように
  するため。

選択中の音符があれば、対応する鍵盤キーを青くハイライトする。ハイライトの一致判定は
**絶対半音位置**(`scoreAbsoluteSemitone(step, accidental)` = オクターブ*12+半音)で
行っており、異名同音表記(C#4とDb4等)によらず同じ物理鍵を正しく指せる。実際に
ヘッドレステストで、`B#4`(通常使われない異名同音表記だがデータとしては妥当)を
選択した時に、絶対半音が一致する`C5`の白鍵が正しくハイライトされることを確認した。

鍵盤のタップ/横スクロールの誤判定は、`overflow-x: auto`のネイティブスクロールと
各キーの`click`イベントの組み合わせに任せている(ブラウザ標準の「スクロール後は
clickが発火しない」挙動を利用。独自のpointerイベントしきい値判定は使っていない
= score note のドラッグや ToDo の並び替えとは異なる設計)。

### ツールバー拡張

音価ボタンに32分・64分を追加(それまで全/2分/4分/8分/16分の5段階だった)。
テキストラベル("32分","64分")のまま実装し、Unicode音楽記号(𝅘𝅥𝅯等)は使わなかった
(iOSでのフォント対応が不安定でグリフ欠けのリスクがあるため。既存の♯♭♮は一般的な
Unicode範囲で安全なのでそのまま)。

### 対象デバイスとレイアウト

「iPadやタブレットならFlat風のデザインに」という要望だったが、実装前に相談した結果
「ピアノ鍵盤は全端末共通にする(スマホは横スクロール・小さめの鍵盤で対応)」を選択した
(UA判定はせず、既存の`@media (max-width: 500px)`パターンで寸法だけ縮める。この
アプリで確立済みの「機種ではなく画面幅で判定する」方針を踏襲)。ヘッドレスChromeで
iPad縦(834×1194)・iPad横(1194×834)・スマホ(390×844)の3パターンを検証し、
どの幅でも譜面プレビュー領域が画面の47〜61%を確保できていることを確認した。
iPad横(1194px)では鍵盤の全音域(C3〜C6)がスクロール無しで収まる。

### テスト方法

このプロジェクトで確立済みの手法(`python -m http.server`+ヘッドレスChrome+CDP、
`window.__scoreTest`デバッグフックをテスト後に削除)で、以下を実機相当の操作
(`Input.dispatchMouseEvent`)で検証した: 白鍵/黒鍵タップでの新規追加、選択中の
上書き(選択維持)、異名同音のauto/sharp/flat切り替え、♮保留中の黒鍵非活性化、
五線譜空タップで音符が増えないこと、既存のタップ選択・ドラッグ音高修正が無変更で
動くこと、タイ分割・3連符・コード名を含む譜面のPNG書き出しが例外無く成功すること。
全項目パスし、実行時例外は0件だった。

## 【続報】2026-08-17: MIDI入力 + キーボードショートカット対応(PC向け)

「PC版はMIDI入力可能にしてほしい。ショートカットも実装できる?」という要望を受け、
[Flat.ioのショートカット一覧](https://help.flat.io/ja/music-notation-software/keyboard-shortcuts/)
を参考に、このアプリの単音・単一選択という現在のデータモデルに実際に対応するものだけを
実装した(和音入力・複数選択・小節ジャンプ・強弱記号・コピペ等、対応する機能が
そもそも存在しないショートカットは実装していない)。

### 唯一の合流点を使い回す設計

ピアノ鍵盤クリック用に既にあった`scoreApplyPitchFromKeyboard(step, accidental)`が
「新しい音符入力/選択中の上書き」の唯一の合流点になっていたので、これは変更せず、
文字キー入力・MIDIノートオンの両方をここに流し込むだけにした。undoスナップショットも
この関数の先頭1箇所に置くだけで、ピアノ鍵盤クリック・文字キー・MIDIの3経路すべてを
まとめてカバーできている。

### Undo/Redo(Ctrl+Z/Ctrl+Y)は本物の複数段履歴

既存の「✖ 直前を削除」ボタン(実装当初は「↩ 直前を削除」というラベルだったが、
本物のundo履歴と紛らわしいため今回アイコンを揃えた)は単発の削除操作であって
undo履歴ではない。今回`scoreUndoStack`/`scoreRedoStack`という別の本物の履歴を追加した。
`scoreElements`をJSON経由でディープクローンして積むだけの単純な方式(1曲ぶんの
譜面が対象で要素数が少ないため、差分管理は不要と判断)。

スナップショットは**実際に変更が起きる直前にだけ**呼ぶことを徹底した。当初の設計案では
「五線譜タップで選択中の音符の音高を書き換える分岐」に無条件でスナップショットを
置いていたが、その分岐はタップ位置が五線の許容範囲外だと何もせず終わることがあり、
無条件に置くとundoしても見た目が変わらない「ゴミ」スナップショットが積まれてしまう
バグになる(実装前のレビューで指摘され、実際に変更が起きるif文の内側に移した)。
同様に、ドラッグによる音高修正はドラッグ中の最初の実変化の瞬間だけ、コード名の
自由入力欄はフォーカスした後の最初のキー入力の瞬間だけ(1回の編集セッション=undo1手に
まとめる)スナップショットを取っている。

### 文字キー(A〜G)のオクターブ決定: 表示範囲にクランプする

文字キーで音符を入力する時、どのオクターブを選ぶかは「基準ピッチ(選択中の音、
無ければ直前の音、どちらも無ければC4)に一番近いオクターブ」という記譜ソフトで
一般的な最近傍方式にした。ただし候補オクターブは`SCORE_PIANO_FROM_OCTAVE`(3)〜
`SCORE_PIANO_TO_OCTAVE`(6)の範囲だけに絞っている。理由: オンスクリーン鍵盤も
その3〜6オクターブしか鍵盤DOM自体を持たないため、範囲外を選んでしまうと入力は
できても対応する鍵盤キーが無く選択ハイライトが光らない、という分かりにくい状態に
なってしまう(これも実装前のレビューで指摘された)。同距離の場合は探索順(低い
オクターブから高いオクターブへ)により自動的に低い方が優先される。

### MIDIは逆にオクターブを制限しない(非対称な判断)

MIDI入力は上記と逆に、オクターブ範囲を一切制限していない。物理鍵盤は実際に
オンスクリーン鍵盤の表示範囲の外を弾けるし、ピッチの正しさの方が鍵盤ハイライトの
演出よりも優先されるべきだと判断した。範囲外でもクラッシュはせず、単にハイライトが
付かないだけ(`querySelectorAll`のループが対象要素なしなら何もしないだけなので安全)。
ヘッドレスのテストでMIDIノート36(C2、表示範囲のC3より低い)を送っても例外なく
正しいピッチとして入力されることを確認済み。

MIDIノート番号→このアプリの絶対半音の変換は`note - 12`(MIDI60=ミドルC=このアプリの
C4=絶対半音48であることを確認して決めた)。新規ヘルパー`scoreResolveFromSemitone`は、
既存の`SCORE_PIANO_OCTAVE`配列のインデックスがそのままオクターブ内の半音位置
(0〜11)に対応していることを利用して白鍵/黒鍵の定義を引き、既存の`scoreResolveWhiteKey`/
`scoreResolveBlackKey`(異名同音の解決ロジック)をそのまま再利用している。

### MIDIリスナーは閉じたら必ずデタッチする

`closeScoreEditor()`で必ず`onmidimessage`をnullに戻す(`scoreDisableMidi()`)。
デタッチしないと、エディタを閉じた後に届いたノートオンが、既にPNG化されて捨てられる
はずの古い`scoreElements`を静かに書き換えてしまう実害がある(実装前のレビューで
指摘された)。ボタンをオンにした事実は`localStorage`に残すので、次に開いた時は
静かに再接続を試みる(初回許可済みなら許可ダイアログは再表示されない)。

### テスト方法

Web MIDI自体をheadless Chromeでエミュレートするのは困難(実機ハードウェアの許可
ダイアログが絡むため)なので、アプリ側の`scoreHandleMidiMessage`ハンドラを単体で
検証する方式にした。`window.__scoreTest`デバッグフックから直接
`[status, note, velocity]`相当の合成メッセージを渡してノートオン/オフを検証し、
MIDIリスナーのデタッチについてはダミーの`{onmidimessage: null}`オブジェクトを
`scoreAttachMidiInputs`に渡して「閉じたら実際に`onmidimessage`がnullに戻るか」を
直接確認した(最初、実際にはMIDIを一度も「接続」していない状態で「閉じた後に
メッセージを送ってもscoreElementsが変わらないこと」を確認しようとしたテストを
書いたが、これはハンドラを直接呼んでいるだけでデタッチの有無に関係なく無害だった
ため無意味なテストだと気づき、上記のダミー入力オブジェクト方式に書き直した)。
キーボードショートカットは`document.dispatchEvent(new KeyboardEvent(...))`で
文字キー・数字キー・矢印キー・Ctrl+Z/Y等を直接発火させて検証した(これらは
`setPointerCapture`のような信頼済みイベントを要求する処理ではないため、CDPの
`Input.dispatchKeyEvent`を使わず合成イベントで十分)。文字キーのオクターブ選択・
undo/redoの粒度(ゴミスナップショットが積まれないこと含む)・MIDIノートオン/オフ・
範囲外MIDIノート・既存のピアノ鍵盤クリックとPNG書き出しの回帰確認まで含めて
42項目のテストを書き、全項目パス・実行時例外0件だった。

## 【続報】2026-08-17: 文字キー入力を「入力→選択→調整→確定」の一連の流れに変更

キーボードショートカット実装直後、ユーザーから「直前に入力した音が選択状態になる
方が良い。↑↓で半音、Shift+↑↓でオクターブ移動。その後→で次の音を入力できるように」
という追加要望があった。単発のショートカット追加ではなく、既存の入力モデルに
新しい「編集セッション」概念を持ち込む変更だったので、設計判断を残しておく。

**選択の自動化は文字キー入力だけに限定した**: 当初「新規追加時は常に選択状態にする」
という素朴な実装(`scoreApplyPitchFromKeyboard`自体を変える案)も検討したが、これは
ピアノ鍵盤タップ・MIDIノートオンの「連続入力(タップ/演奏し続けるたびに次々と
音符が増える)」という既存の使い勝手を壊してしまう(2音目のタップが1音目を
上書きしてしまう)。そこで`scoreApplyPitchFromKeyboard(step, accidental, opts)`に
`opts.selectAfterAdd`という文字キー入力専用のオプトインフラグを追加し、
`scoreEnterNoteFromLetter`だけがこれを渡すようにした。合流点(唯一の入力経路)の
設計自体は変えず、末端の呼び出し側だけで挙動を分岐させている。

**↑↓の半音/オクターブ移動はMIDIと同じ解決ロジックを再利用**: 選択中の音符の
絶対半音を`scoreAbsoluteSemitone`で求め、±1(Shiftなら±12)してから
`scoreResolveFromSemitone`(MIDI入力実装時に作った、絶対半音→{step,accidental}の
逆変換ヘルパー)に渡すだけで実装できた。異名同音の表記(♯系/♭系)も調号や
異名同音トグルに自然に従う。MIDIと同様、オクターブ範囲の制限は無し
(♭↔♯異名同音のようにオンスクリーン鍵盤の表示に依存する話ではなく、
音高そのものの正しさが優先されるため)。

**→キーで末尾の音符から選択解除する設計**: 「入力→調整→確定」のサイクルを
文字キーだけで回せるようにするため、`scoreSelectRelative`の境界処理を
「末尾でクランプして留まる」から「末尾を超えたら選択解除する」に変更した。
これにより、選択解除後に押した次の文字キーが(非選択時の挙動である)新規追加
として働き、そのままサイクルが繰り返せる。既存の「タップで既存の音符間を移動する」
用途(曲の途中を直す時)はそのまま動作するため、既存の使い勝手を壊していない。

**検証**: 文字キー入力での自動選択、↑↓/Shift+↑↓の半音・オクターブ移動と選択の
保持、別の文字キーでの上書き(選択維持)、末尾での→キーによる選択解除、選択解除後の
次の文字キーでの新規追加、既存の音符間ナビゲーション(端でない場合)、選択が無い
状態での↑↓が安全に無視されること、ピアノ鍵盤タップの非選択維持(連続タップの回帰
確認)、の19項目をヘッドレスChromeで検証し全項目パス。

## 【続報】2026-08-17: 和音入力モードを追加

「モード分けのボタンで和音も入力できるようにしたい」という要望。実はこのデータ
モデルは同じ日の別のセッションで既に和音対応を見越して一般化済みで(note要素の
`pitches`が配列、今まで常に1要素)、コード中に`// TODO 和音対応時`という3箇所の
明示コメントが残されていた。今回はその宿題を実際に片付けた。

**設計の核**: 既存の`scoreApplyPitchFromKeyboard(step, accidental, opts)`が
ピアノ鍵盤クリック・文字キー・MIDIノートオンの唯一の合流点だったので、この関数の
先頭に和音モードの分岐を追加するだけで3つの入力経路すべてに和音入力が効くように
した(単音モード側のロジックは無変更)。

**上書きではなく足し引き(トグル)方式にした理由(実装前レビューで指摘)**: 当初案は
単音モードと同じ「選択中に別の音をタップ=上書き」を和音モードでも流用するつもり
だったが、Plan agentのレビューで「和音モード中に既存の1音を選択して別の音を
タップした時、`直す`のか`音を足す`のか曖昧で、データだけからは判別不能」と指摘された。
これは実装の細部ではなく設計そのものの欠陥だったので、**和音モードは常にタップ=
追加/既存音の再タップ=削除、というトグル方式に統一**し、単音モードの上書き挙動とは
意図的に違うものにした。音を間違えた時は「間違えた音を再タップで消す→正しい音を
足す」の2手順になるが、この違いを`#scoreEditorHint`とマニュアルに明記することで
解決した(silent defaultにせず、挙動差を明示するという判断)。

**MIDIでの離鍵による自動確定(レビューでの追加提案、低コストで採用)**: 和音モードは
入力元を問わず常に自動選択を維持するため、次の和音に移るには本来ArrowRight/Escape
等の明示操作が必要になる。これはマウス/文字キー入力では許容範囲だが、MIDI
キーボードで実際に和音を弾く場合は「離鍵で自動確定」の方が直感的なので、既存の
`scoreMidiHeldNotes`(ノートオフのたびに更新済みの、今物理的に押されているMIDI
ノート番号のSet)を流用し、和音モード中に全キーが離される(サイズ0になる)瞬間だけ
自動で選択解除するようにした。既存のインフラを流用しただけなので数行の追加で済んだ。

**`el.pitches`をstep昇順・重複無しに保つ不変条件をmutation側で保証する設計**:
VexFlowは2度音程が隣接する時のノートヘッドの左右オフセットをkeys配列の並び順で
決めるため、未整列だと見た目が崩れる(既存のTODOコメントが指摘していた問題)。
これをレンダリング時にソートする代わりに、`scoreToggleChordPitch`(声部の追加/削除
ヘルパー)側で常に挿入位置をstep昇順に保つよう設計した。これにより`buildScoreRenderQueue`
と`scoreBuildMeasureNotes`のkeys組み立てロジックは一切変更不要になった(元のTODO
コメントは削除)。同一stepの重複もこの関数内でトグル削除として処理されるため
発生しない。

**`effectiveAccidental`を音符単位→ピッチ単位の配列に変更**: `computeScoreLayout`が
計算する実効臨時記号(調号+小節内の直前の臨時記号を考慮した実際に鳴っている音)は、
今まで1音符につき1値だったが、和音では声部ごとに異なりうる。`explicitState`マップ
(letter+octaveキー)自体は変更不要で、`effectiveAccidental[i]`を単一値から
`el.pitches`と同じ並びの配列に変えるだけで対応できた(休符側は従来どおりnullのまま)。

**ドラッグ/↑↓キーでの移調は和音全体をまとめて動かす方式にした**: 和音の1声部だけを
個別につまんで動かす機能は、声部ごとの当たり判定が必要になり複雑になるため今回は
スコープ外とした。既存の音高調整(ドラッグ・↑↓/Shift+↑↓)は全て「新しいstepと
`elFirstStep(el)`(最低音、常にソート済みなので`pitches[0]`)との差分を全声部に
加算する」という単純な変更で、和音の形(声部間の相対関係)を保ったまま平行移動する
ようにした。

**ヒットテストも和音全体に対応**: `scoreLastNotePositions`が単一の`step`ではなく
`steps`(配列)を持つように変更し、`scoreHitTestNote`は候補の全声部とのY距離を見て
最小のものを採用するようにした(和音のどの音符頭をタップしても同じ音符が選択される)。

**検証**: モード切替ボタンの状態、文字キー/ピアノ鍵盤クリック双方での和音への
声部追加(自動選択・複数声部の積み上げ)、既存声部の再タップでの削除(トグル)、
最後の1声部は削除できないこと(no-op)、挿入順に関わらずstep昇順が保たれること、
2声部が異なる実効臨時記号を持つこと(`effectiveAccidental`が声部ごとに独立)、
↑↓キーでの和音全体移調で声部間の相対音程が保たれること、単音モードに戻した時に
既存フロー(上書き・ピアノ/MIDI非自動選択・文字キー自動選択)が無変更で動くこと、
MIDI合成メッセージでの複数ノートオン→和音構築→全ノートオフでの自動選択解除→
次のノートオンが新規音符として始まること、Ctrl+Z/Ctrl+Yで声部の追加/削除が
正しく取り消し/やり直しできること、PNG書き出しが壊れていないこと、の33項目を
ヘッドレスChromeで検証し全項目パス。

## 【続報】2026-08-17: 文字キー入力の仕様変更・コード名の漏れ修正・タイ機能を追加

和音入力モードを触った直後、実際にメロディーを打ち込んでみたユーザーから4件の
フィードバックがまとめて来た。

**①文字キー入力は常に新規追加に変更(仕様の反転)**: それまでは「文字キーで
入力→自動選択→別の文字キーで上書き→→キーで確定して次へ」という、直前に別の
要望を受けて作ったフローだったが、実際に使ってみると「入力して、次に音符キーを
押したらそれは次のノートの入力になってほしい」という逆方向の要望が来た。実際の
記譜ソフト(Finale/Sibelius/MuseScore等)の一般的な挙動(文字キーは常に新しい
音符を追加し、既存音符の修正は選択操作を介して行う)に寄せる形で、
`scoreApplyPitchFromKeyboard`に`opts.forceAppend`を追加し、文字キー入力
(`scoreEnterNoteFromLetter`)だけ選択中でも常に新規追加するようにした。
ピアノ鍵盤クリック/MIDIノートオンの「選択中なら上書き」は変更していない
(そちらは要望が無かったため)。和音モードの分岐はforceAppendチェックより前で
早期returnしているため、この変更の影響を受けない(和音モードの声部追加/削除は
従来どおり)。**教訓**: 一度実装した挙動でも、ユーザーが実際に使ってみて初めて
「思っていたのと違う」と気づくことがある。今回は前回の要望と正反対の内容だったが、
実際の使用感に基づく最新のフィードバックを優先した。

**②コード名が後続の音符に漏れる不具合を修正**: `scoreChordInput`の生のDOM値を
「次の新規音符に適用する保留値」としても「選択中の音符の編集値」としても
兼用していたため、コード名を入力→文字キーで音符確定(自動選択)→
`updateScoreToolbarUI`がその音符のchordを表示用に書き戻す→次の文字キーで
別の音符を追加、という流れで、直前の音符のコード名が次の音符にも付いてしまって
いた。`scorePendingChord`という別変数を新設し、「次の新規音符への保留値」と
「選択中の音符の表示/編集値」を完全に分離することで解決した(`scorePendingDot`/
`scorePendingAccidental`と同じパターンに揃えただけとも言える)。

**③「.」キーで付点をトグル**: 単純に`btnScoreDot.click()`を呼ぶだけ(既存の
`-`/`=`/`0`と同じ再利用パターン)。

**④タイでつなぐ機能を追加**: それまでは「小節に収まらない音符が自動分割される」
時にしかタイが発生しなかったが、ユーザーが同じ高さの2つの音符を手動でタイで
つなげる機能を要望。この単一選択の編集モデルでは2音符を同時に選べないため、
「選択中の音符→scoreElements上で直後の音符」という決め打ちの組にした
(`el.tiedToNext`フラグ)。高さが一致しない時はアラートで断る。
`buildScoreRenderQueue`の`tieToNext`/`tiedFromPrev`計算を、既存の自動分割
断片の判定に加えて`el.tiedToNext`由来のケースもORで拾うよう拡張しただけで、
実際の弧線描画ロジック(`scoreBuildMeasureNotes`後段のtie描画ループ)は
queueベースで汎用的に書かれていたため無変更で済んだ。

**おまけで見つけた既存バグの修正**: タイ機能を実装する過程で、`computeScoreLayout`
の実効臨時記号計算が、タイが小節線をまたぐ時に正しく音を継承できていない
(自動分割によるタイも含め、元からあった問題)ことに気付いた。`explicitState`
マップが小節ごとにリセットされるため、`tiedFromPrev`な音符が新しい小節の
先頭に来ると、本来継承すべき臨時記号ではなく調号のデフォルトにフォールバック
してしまっていた。修正: `effectiveAccidental`は先頭から順番に計算しているので、
`tiedFromPrev`の場合は`explicitState`を見る代わりに直前のqueueインデックス
(`effectiveAccidental[i-1]`)の同じピッチ位置を直接参照するようにした。これで
小節線をまたいでも正しい音が継承される。ヘッドレスChromeで実際に4/4拍子・
4分音符4つでちょうど1小節を埋め、4つ目(♯付き)を次の小節の頭にタイでつなぐ
テストケースを作り、`effectiveAccidental`が正しく`'sharp'`を引き継ぐことを
確認した(修正前ならnull=ナチュラルにフォールバックしていたはず)。

**検証**: 文字キーの常時新規追加(選択中でも上書きしないこと)・矢印キー調整を
挟んでも追加が続くこと・ピアノ鍵盤クリックの上書きは回帰していないこと、
コード名が選択解除後や連続入力後に後続の音符へ漏れないこと、「.」キーでの
付点トグル、タイの付与/解除・高さ不一致時のアラート・queueのtieToNext/
tiedFromPrevが正しく設定されること、和音モードが今回の変更の影響を受けて
いないこと、undo/redo・PNG書き出しの回帰確認、の32項目に加えて上記の
小節またぎタイの臨時記号継承を別途検証し、全項目パス。

---

## 【続報】2026-08-19: 入力音の再生とコード名ショートカット入力を追加

### 入力した音を鳴らす(`scorePlayPitches`)

「何の音か確認できないと不便」という要望への対応。音源の選択が最初の判断ポイント
だった。実楽器のサンプル音源(SoundFont等)を同梱すれば音は良くなるが、数MB単位で
アプリが重くなり、オフライン起動用のService Workerキャッシュも膨らむ。この機能の
目的は「打ち込んだ音程を耳で確認する」ことであって演奏用の音を出すことではないので、
**Web Audio APIのオシレータで合成する**方針にした。三角波+2倍音を薄く重ねただけの
簡素な音色だが、音程の確認という用途には十分。既存のメトロノーム用`AudioContext`
(`ensureAudioCtx`)をそのまま使い回している。

呼び出し位置は`scoreApplyPitchFromKeyboard`(ピアノ鍵盤クリック・文字キー・MIDIの
唯一の合流点)に加え、`scoreShiftSelectedPitch`(↑↓での高さ調整)と
`scoreSelectRelative`/`scoreSelectEdge`(←→/Home/Endでの選択移動)にも置いた。
後者2つは「入力時だけ鳴らす」より一歩踏み込んだ判断で、**音符をたどりながら音を
確認できる**方が実際の用途(打ち込んだメロディーが合っているかの確認)に合うため。
ただし末尾で→を押して選択が外れた時は鳴らさない(鳴らす対象の音符が無いため)。

和音モードでは、声部を足し引きするたびに**その時点の和音全体**を鳴らす(今押した
1音だけではなく)。和音の響きを確認したいという用途なので、部分ではなく全体を
鳴らす方が自然。声部数で音量を割って音割れを防いでいる。

エンベロープの立ち上がりを0.01秒かけているのは、いきなり最大音量にするとプツッと
クリックノイズが乗るため(Web Audioの定番の注意点)。iOSでは`AudioContext`が
suspendedで始まるが、音符入力は必ずタップ/キー操作を伴うのでその場でresumeして
問題ない。

オン/オフはlocalStorageに保存する(曲データには入れない)。電車内・楽屋・本番直前の
ステージ袖など「静かにすべきだが打ち込みはしたい」場面が実際にあるため。

### コード名のショートカット入力(`scoreChordShortcutTransform`)

ChordWikiのコード譜エディタ(chordlistener.com/editor/)と同じキー割り当てを実装した。
Shiftキーを押さずに`#`や`M7`を打てるのが利点。

実装で肝になったのは**位置によって解釈が変わる**点。`w`→`#`のような単純な文字置換
テーブルだけでは不十分で、`A`〜`G`は「コードの先頭」と「`/`の直後(分数コードの
ベース音)」でだけ音名として扱い、それ以外の位置では修飾子(`f`→`b`など)として
扱う必要がある。公式の例`gvlj[w9]/df`→`GbbaugM7(#9)/Db`がまさにこれを示していて、
最初の`g`は音名G、`/`の直後の`d`は音名D、しかし最後の`f`は♭になる。
`scoreChordExpectsNoteName()`で「カーソル直前の非空白文字が無いか`/`か」を見て
判定している。

入力欄の`keydown`を横取りして`preventDefault`+手動挿入する方式にした。
`preventDefault`すると`input`イベントが飛ばなくなるため、既存の保存処理
(選択中の音符への反映・`scorePendingChord`の更新)を動かすために
`dispatchEvent(new Event('input'))`を自分で発火している。

デフォルトはオフ。この割り当てを知らないと普通のコード名(`Cm7`の`m`など)が
打てなくなったように見えるため、明示的にオンにした人だけが使える状態にしている。
割り当ての無い文字(数字・`m`・`-`など)は素通しさせているので、オンのままでも
ある程度は普通に打てる。

**検証**: 変換ロジックの単体確認(公式例の完全一致を含む19項目)、実際のキー入力での
変換、オフ時に横取りしないこと、音の再生が入力/移調/選択移動/和音の各経路で正しい
回数・声部数で呼ばれること、音オフ時に鳴らないこと、コード名が音符に正しく保存され
入力欄がクリアされること、の36項目でヘッドレスChromeで検証し全項目パス。

---

## 【続報】2026-08-19: 転調機能とフレーズ集

### 転調(`scoreTransposeAll`)

「転調もボタンで一瞬。転調ボタンでは楽譜に振ってあるコードも転調する」という要望。
音符の高さだけを動かすのは既存の`scoreShiftSelectedPitch`とほぼ同じだが、
**コード名と調号も一緒に動かす**のが今回の肝。

**処理の順番が重要**: 先に移調先の調号(`scoreKeyFifths`)を決めてから、その結果に
応じて♯系/♭系(`preferFlat`)を確定し、音符とコード名の両方をその同じ基準で
書き換える。逆順にすると「調号は♭系なのに音符は♯で書かれている」という
食い違いが出る。

**調号の決定**: 主音の絶対半音→fifthsの対応表(`SCORE_FIFTHS_BY_SEMITONE`)を用意した。
臨時記号が少ない方が一意に決まるので基本は機械的に引けるが、半音6つ(F#/Gb)だけは
♯6つと♭6つで同数のため決め手が無い。ここは**元の調が♯系だったか♭系だったかを
引き継ぐ**ことにした。またこの穴を埋めるため`SCORE_KEY_OPTIONS`にF#(fifths 6)と
Gb(fifths -6)を追加している(これが無いと12音のうち半音6つぶんだけ移調先を
表現できなかった)。

**コード名の解析**: 先頭のA〜Gと、それに続く臨時記号を貪欲に読む。コードの語尾
(m7・aug・sus4など)が♯♭で始まることは無いので、貪欲でも誤読しない
(`Cb9`は「Cフラットの9」と解釈される。これは一般的なコード表記の慣習どおり)。
分数コードは`/`で分割して両方を移調する。全角の♯♭で書かれていた場合は移調後も
全角に揃える(このアプリはコード名に全角記号も打てるため、見た目の一貫性のため)。
ルートとして解釈できない文字列(`N.C.`など)はそのまま返す。

**専用のピッチ解決**: 既存の`scoreResolveFromSemitone`は「次に置く音符用の保留中♯♭」や
選択状態を見てしまうので、一括変換である転調には使えない。`preferFlat`を明示的に
受け取る`scoreResolvePitchForTranspose`を別に用意した。

**異名同音モードのリセット**: 手動で♯/♭に固定していた場合、転調後は`'auto'`に戻す。
そうしないと新しい調号と音符の表記が食い違って見えるため。

### フレーズ集

「楽譜エディタを立ち上げて楽譜を入力してそれを管理できる。画像で取り込んだ楽譜に
貼ることも可能」という要望。

**それまでの楽譜エディタは`scoreElements`を捨てていた**(完了時にPNG化して画像メモに
するだけ)。フレーズ集ではこのデータそのものを保存するので、後から開き直して
編集・転調できる。

**同じエディタUIを2つの出口で使い回す**: `scoreEditorMode`(`'memo'`|`'phrase'`)で
完了ボタンの動作だけを切り替える方式にした。入力まわりのロジック(ピアノ鍵盤・
文字キー・MIDI・和音・コード名・転調)は一切分岐させていないので、片方だけ機能が
足りない、という事態が起きない。完了ボタンのラベルも「完了」/「保存」と出し分けて、
貼るのか保存するのかを取り違えないようにしている。

**プレビュー画像**: 一覧でひと目で分かるよう、保存時にPNGのdata URLを1枚焼いて
レコードに持たせる。一覧表示のたびにVexFlowで再描画するのは重いため
(VexFlow本体は遅延読み込みなので、一覧を見るためだけに1MB超を読ませたくない)。

**曲の譜面への貼り付け**: 新しい配置フローを作るのではなく、既存の「+ 楽譜」で開いた
エディタに「🎵 フレーズ読込」ボタンを足す形にした。読み込む→(必要なら転調する)→
完了、で既存の画像メモ挿入の流れにそのまま乗る。読み込んだ内容はエディタ上の
作業コピーなので、**元のフレーズを壊さずにこの曲用だけ手直しして貼れる**。

**タグ**: カンマ・読点・空白のどれでも区切れるようにした(入力のたびに区切り文字を
気にしたくないため)。重複と空要素は取り込み時に潰す。絞り込みチップは、タグが
1つも登録されていないうちは表示しない(空の絞り込みUIが場所を取らないように)。

**検証**: コード名の転調(語尾の保持・分数コード・全角/半角の維持・オクターブ跨ぎ・
ダブルシャープ/フラット・解釈不能な文字列など25項目)、譜面全体の転調(音符+コード+
調号が揃って動くこと、±12で調号が変わらないこと、半音6つのF#へ行けること、
12回転調で元に戻ること)、タグ解析、エディタのモード切替とフレーズの復元、
一覧とタグ絞り込み、永続化、の52項目に加えて全角♯♭の維持を別途6項目検証し、
全項目パス。

---

## 【続報】2026-08-19(2): 黒鍵の♯/♭をモード理論で決める

### 従来の問題

黒鍵をタップした時にどちらの綴りにするかを、調号のfifthsの符号だけで決めていた
(`fifths >= 0`なら全部♯、`fifths < 0`なら全部♭)。これだとハ長調で黒鍵を押すと
`C#` `D#` `F#` `G#` `A#` と全て♯になるが、実際の記譜はそうならない。

### 正しい規則

アイオニアン(メジャースケール)を基準に、各モードの変化音を並べると:

| モード | 変化音 |
| --- | --- |
| リディアン | `♯4` |
| ミクソリディアン | `♭7` |
| ドリアン | `♭3` `♭7` |
| エオリアン | `♭3` `♭6` `♭7` |
| フリジアン | `♭2` `♭3` `♭6` `♭7` |
| ロクリアン | `♭2` `♭3` `♭5` `♭6` `♭7` |

**♯が付くのはリディアンの`♯4`だけで、他は全て♭**。つまり主音からの半音差で言うと
1・3・8・10(♭2・♭3・♭6・♭7)は♭、6(トライトーン)だけが♯になる。
ハ長調なら `Db` `Eb` `F#` `Ab` `Bb`。

トライトーンは`♯4`(リディアン)と`♭5`(ロクリアン)の両方の解釈がありうるが、
ユーザーの要望どおり`♯4`を採用した。

### 実装(`scoreBlackKeyAccidental`)

黒鍵の絶対音高クラスと調の主音の差(`offset`)で分岐する:

1. `offset === 6` → ♯(リディアンの`♯4`)
2. `offset ∈ {1,3,8,10}` → ♭(`♭2` `♭3` `♭6` `♭7`)
3. それ以外 → **その黒鍵はその調の音階内の音**(ト長調の`F#`、変ホ長調の`Eb`など)。
   調号マップ(`scoreKeySigAlterationMap`)を引いて、2つの候補文字のうち変化記号が
   付く方を採る。

3番目の分岐が必要なのがポイント。1と2だけだと、ト長調の`F#`(主音から11半音=長7度、
調号に含まれる音)が「変化音ではない」ため判定から漏れる。

主音の半音位置は、転調機能で追加済みの`SCORE_SEMITONE_BY_FIFTHS`をそのまま流用した。

**二重変化記号は原理的に発生しない**: 黒鍵の候補は「下の文字＋♯」か「上の文字＋♭」の
2つしかなく、どちらも単一の変化記号だから。度数から文字を導出する一般解にすると
Gbキーの`♭2`が`A♭♭`になるような二重変化記号が出うるが、黒鍵に限れば起きない。

手動の「♭↔♯ 異名同音」固定は従来どおり優先される(`scoreEnharmonicMode !== 'auto'`で
即座に返す)。MIDI入力も`scoreResolveFromSemitone`経由でこの関数を通るので同じ規則になる。

### 転調機能にも同じ規則を適用した(2026-08-19 追記)

当初は転調(`scoreTransposeAll`)側の綴りを「移調先の調号の♯系/♭系に倒す」従来方式の
ままにしていたが、それだと鍵盤入力と食い違う(ハ長調で鍵盤を押すと`Db`なのに、
ハ長調へ転調すると`C#`になる)。

そこで判定を`scorePreferFlatForPitchClass(pitchClass, keyFifths)`に一本化し、
**鍵盤タップ・転調時の音符・転調時のコード名の3箇所で共有**した。

重要なのは、コード名も音符と同じ規則にしたこと。片方だけモード規則にすると
「`Gb`のコードの下に`F#`の音符」という食い違いが起きる。両方揃えれば矛盾しないうえ、
コード名としても正しくなる(ハ長調の`bVII`は`A#`ではなく`Bb`、`#IV`は`Gb`ではなく`F#`)。

なお、この規則が実際に綴りを変えるのは黒鍵にあたる5つの音高クラスだけ。白鍵の音は
`SCORE_PITCH_NAME_*`テーブルで常に変化記号なしの素の音名になるため、
「変ニ長調の`♭2`は`E♭♭`」のような二重変化記号は出ない(実用上の綴りが優先される)。

**検証**: 13調 × 5黒鍵の全組み合わせを、度数から文字を導出する独立した参照実装と
突き合わせて全一致を確認。あわせて「変化音のうち♯になるのはトライトーンだけ」
「調号内の黒鍵は調号どおり」「手動固定は優先される」「綴りを変えても音の高さ自体は
ずれない」を検証し、全項目パス。


---

## 【続報】2026-08-19(3): 綴り規則を鍵盤・転調・コード名で一本化

上記の転調との不一致を解消し、`scorePreferFlatForPitchClass()`に判定を集約した。
`scoreBlackKeyAccidental()`もこの関数を呼ぶだけになり、調号マップを引く分岐は
`keyFifths < 0`で同じ結果になることを確認した上で削除した(黒鍵が調の音階内にある
場合、♯系の調ならその黒鍵は必ず調号の♯、♭系なら必ず調号の♭になるため)。

**検証**: 13調×5黒鍵の鍵盤綴りが共通化前と変わらないこと(参照実装と全一致)、
10通りの転調量それぞれについて12半音すべての音符とコード名の綴りが一致すること、
C→D転調で変化音コードが`C# Eb F# G# Bb`になること、分数コード・語尾・解釈不能な
文字列(`N.C.`)が保たれることを検証し、15項目全パス。

---

## 【続報】2026-08-19(4): スマホ入力の穴を埋める(取消・挿入・オクターブ移動・ヘ音記号)

他サービス(主に[Flat.io](https://flat.io/))と比較して洗い出した不足のうち、
スマホでの打ち込みに効く4点を実装した。

### 1. Undo/Redoボタン

`scoreUndo()`/`scoreRedo()`を呼んでいたのは**Ctrl+Z/Ctrl+Yのキーハンドラだけ**で、
スマホには取り消す手段が一切なかった。転調を誤爆しても全消去しても戻せない状態。

ツールバーの「✖ 直前を削除」(`btnScoreUndo`)は名前が紛らわしいが履歴とは無関係の
単発削除なので、これとは別に履歴用のボタンを2つ追加した。履歴の仕組み自体は既に
あったので、`updateScoreHistoryButtons()`で有効/無効を反映するだけで済んだ
(`pushScoreUndoSnapshot()`と`updateScoreToolbarUI()`の両方から呼ぶ)。

### 2. 挿入モード

**削除は途中でもできるのに、挿入だけ末尾限定**という非対称があった
(`scoreElements.push()`しか無く、中間に入れる経路が存在しなかった)。
1小節目のミスに気付くと後ろを全部消して打ち直しになり、スマホほど打ち直しコストが
効くので実用上の影響が一番大きかった。

Flat.ioの"Insert mode"と同じ「オンだと選択位置に挿し込んで後ろを押し出す」方式にした。
`scoreApplyPitchFromKeyboard`の和音モード分岐の直後に挿入分岐を置き、入力元
(鍵盤/文字キー/MIDI)を問わず効くようにしている。休符ボタンも同じ判定に従わせた。

**挿入後に選択を1つ後ろへずらす**のが要点。こうすると押し出された元の音符を選び
続けることになり、連続入力した時に挿し込んだ音符が左から右へ自然に並ぶ。

**和音モードとは排他**にした。和音モードの「選択中の音符に声部を足す」と挿入モードの
「選択位置に挿し込む」は、どちらも"選択中"という状態の意味を奪い合って両立しない。
片方をオンにするともう片方が切れる、という分かりやすい形にしている。

ショートカットはFlat.ioに合わせて`I`(A〜Gと衝突しない)。

### 3. 鍵盤のオクターブ移動ボタン

鍵盤は表示幅より横に長く(スマホ実測で780px vs 500px)、横スクロールでしか音域を
移動できなかった。白鍵7つぶんを単位にスクロールするボタンを鍵盤のすぐ上(親指の
届く位置)に置き、端まで来たら無効化する。指でのスクロールにも追従させている。

### 4. ヘ音記号

`addClef('treble')`決め打ちで、**ベースラインが書けなかった**。バンド用途としては痛い。

実装の肝は**stepの意味を変えないこと**。`step`は「E4を0とする絶対的な音の高さ」で
音部記号とは無関係だが、五線への描画位置は音部記号で変わる(第1線がト音記号ではE4、
ヘ音記号ではG2=step -12)。そこで描画・タップ・ドラッグの3箇所で
`step - scoreClefBottomStep()`という相対値を使うようにした。

この方式なら**音部記号を切り替えても音符データは一切変わらず**、同じ音が正しく別の
位置に描き直されるだけになる(検証で確認済み)。

あわせて鍵盤の音域も音部記号に追従させた(ト音記号 C3〜C6 / ヘ音記号 C1〜C4)。
文字キー入力の基準ピッチもヘ音記号ではC3にしている(C4のままだと第1加線より上
ばかりになり、毎回オクターブを下げる羽目になるため。テストで実際に見つけた不具合)。

フレーズ集には`clef`として保存する。古いフレーズには無いのでト音記号として開く。

**検証**: 取消/やり直しの有効状態と転調の取り消し、挿入位置と選択の追従・入力元
4種すべてでの挿入・和音モードとの排他・`I`キー、オクターブ移動と端での無効化、
音部記号を変えても音名とstepが変わらないこと・鍵盤音域の切り替え・フレーズへの
保存と復元・古いフレーズの後方互換、の49項目を検証し全項目パス。

---

## 【続報】2026-08-19(5): 弱起・強弱記号・奏法記号

### 弱起(アウフタクト)

実際の曲は1拍目から始まらないものが多いのに、小節容量を常に`scoreMeasureCapacity()`
固定で数えていたため、小節線が実際の曲とずれていた。

「1小節目だけ容量を減らす」という形で表現し、`scoreCapacityForMeasure(measureIndex)`
を新設した。小節分割は2箇所(`buildScoreRenderQueue`と`computeScoreLayout`)にあるので、
どちらも小節番号を数えながら容量を引き直すよう書き換えている。

`computeScoreLayout`では各小節の容量も配列で覚えるようにした。後段の
「1小節まるごと休み→全休符で描く」判定が容量と比較しているため、弱起の小節を
全休符扱いしてしまわないようにするため。

拍子より長い弱起を指定した場合は通常の小節として扱う(不正な譜面を作らない保険)。

### 強弱記号

**当初はBravuraのSMuFLグリフ(U+E520〜)で描こうとしたが、実測で描画されないことが
分かった**。`document.fonts.check('40px Bravura')`が`false`を返す状態で、canvasの
文字描画からはBravuraを確実には使えない(VexFlowは音符グリフを独自経路で描くので
譜面自体は正しく出るが、`Annotation`の文字描画はcanvasのフォント解決に乗る)。

そこで**太字イタリックのセリフ体**に倒した。強弱記号の慣用的な代替表現であり、
フォントの読み込み状態に依存せず常に出る。

もう1つ実測で見つかったのが**キャンバス下端での見切れ**。強弱記号は五線の下に
描かれるため、既存の下余白(20px)では収まらなかった。`scoreExtraBottomRoom()`で
「強弱記号が1つでもある譜面の時だけ」余白を足すようにしている(常に足すと、
強弱を使わない普通の譜面まで無駄に縦長になる)。

### 奏法記号

VexFlowの`Articulation`をそのまま使う(`a.`スタッカート/`a>`アクセント/
`a-`テヌート/`a^`マルカート)。上下の配置はVexFlowが符幹に応じて決めてくれる。

### 保留値を持たせなかった判断

強弱・奏法は**選択中の音符にだけ効く**仕様にした。コード名のような
「次に置く音符に付く保留値」の仕組みは持たせていない。

理由は2つ。①コード名で保留値と表示値を分けた時に、一度入力した値が後続の音符へ
漏れる不具合を出した経緯があり、同じ構造を増やしたくない。②強弱・奏法は普通
メロディーを書いた後から付けるものなので、保留の仕組みの利得が薄い。

タイで分割された時に最初の断片にだけ付ける処理はコード名と同じ扱いにしている
(伸びている先にもう一度出ると二重表示になるため)。

**検証**: 弱起の小節分割(なし/4分1つ/2分での区切り位置と、音符数が変わらないこと)、
強弱の設定・別の音符への非漏洩・選択解除時の無効化、奏法4種の設定と描画、
タイ分割時に二重表示しないこと、の22項目を検証し全項目パス。描画結果は
拡大スクリーンショットで目視確認した。
