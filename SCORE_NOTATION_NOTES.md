# 楽譜エディタ(スコアエディタ)の記譜法についての考え方

このファイルは `index.html` 内の「+ 楽譜」機能(`openScoreEditor` 以下)が
何を正しい記譜法として採用し、何を意図的に簡略化しているかを記録する開発メモ。
コード自体を読めば分かることは書かず、「なぜそうしたか」「どこまでが正式なルールで
どこからが割り切りか」を残す。

2026-08、ユーザーから「簡略化しないで、ちゃんとした楽譜を自動で作れるようにして」
という指示を受け、拍子・調号・小節線・段の自動折り返しに対応する大幅な書き換えを行った
(それ以前のv1は拍子・小節・調号を持たない単一行の五線だった)。

## スコープの前提

- 単音メロディー一本のみ(和音進行やポリフォニーは非対応)。コードネームは
  音符の上にテキストとして添えるだけで、和音として鳴らしたり複数符頭を積んだりはしない。
- 演奏(音再生)はしない。あくまで「後で人間が読んで弾ける・歌える」ための視覚表現。
- 1つの楽譜(1回のエディタ起動〜完了)につき、拍子・調号はそれぞれ1つだけ。
  曲の途中で拍子・調号が変わるような曲には対応しない(UIの複雑化を避けるための
  意図的な割り切り。単音メロディーの手早いメモという用途では十分と判断した)。
- 目的は彫刻譜(プロの版下)のような美しさではなく、素早く書けて誤読されない明瞭さ。
  そのため装飾的な要素(スラー、強弱記号との統合レイアウトなど)は非対応。

## データモデル

- 音高は MIDI 番号ではなく `step`(五線上の段階、整数、**E4 = 0**)で持つ。
  再生機能が無いため「実音高」ではなく「五線上のどこに描くか」だけが必要で、
  step だと `Y = baseY - step * 半行間隔` の単純な一次式でピクセル変換できる
  (`baseY` は段(システム)ごとのE4のY座標。段が複数あるので固定値ではなくなった)。
- 臨時記号(`accidental: 'sharp'|'flat'|'natural'|null`)は `step` とは独立に持つ。
  実際の記譜でも♯♭♮はそのstepの音を修飾するだけで、段位置自体は動かさない。
  `natural` は「調号で♯/♭が付く音を、この音だけ本位に戻す」ためのもの。
- 付点(`dotted`)・3連符(`triplet`/`tripletGroup`/`tripletFirst`)・音価(`duration`)は
  それぞれ独立フィールド。`tripletFirst` はグループの先頭要素だけに立てるフラグで、
  「3」ラベルを音符ごとではなくグループに1回だけ描くために使う。
- 拍子(`scoreTimeSigIdx` → `SCORE_TIME_SIGS`)と調号(`scoreKeyFifths`、五度圏の
  fifths値。+が♯系、-が♭系)はエディタ全体で1つずつのグローバル状態。

## レイアウト計算は `computeScoreLayout()` に一本化

小節分割・臨時記号の要否・連桁のグループ化・段(システム)割りは、バラバラに
その場で計算すると前提がズレて事故る(実際、旧v1はビームと小節を別々に考えていて
拍をまたぐビームが放置されていた)ので、1つの関数で順番に計算して1つのlayout
オブジェクトを返し、`drawScore()` はそれを描くだけにした。

1. **小節分割**: 拍子から「1小節の容量」(全音符=1単位として `beats/unit`)を求め、
   音価の合計がこの容量に達する/超えるまで貪欲に音符・休符を1小節に詰める。
   達した/超えた音符の直後で区切る。
2. **臨時記号の表示要否**: 明示的に♯♭♮を指定した音符は必ず表示し、以後その小節内で
   同じ段位置(＝同じ音)に効く。無指定の音符は、その小節内で初出であれば
   (＝調号どおりであれば)何も描かない。臨時記号は小節線を越えて持ち越さない
   (実際の記譜法どおり)。
3. **連桁のグループ化**: 「拍のまとまり」の境界(単純拍子は1拍=1/unitごと、
   6/8等の複合拍子は付点4分=3/8ごと)を`scoreBeatBoundaries()`で求め、これを
   跨がない・同音価が連続する8分/16分音符だけを連桁でまとめる。
4. **段(システム)割り**: 各小節の必要幅(音価比例幅の合計)を求め、段の内容幅
   (`scoreSystemContentWidth()`、コンテナ幅からト音記号・調号のぶんを引いた値)に
   収まるだけ貪欲に小節を詰めていく。段が変わるごとにト音記号・調号を描き直す
   (実際の楽譜と同じ)。

## 描画で守っている記譜法ルール

- **符幹の向き**: 五線中央(第3線, step=4)より下の音は符幹を上向き・右側に付け、
  上の音は下向き・左側に付ける。全音符には符幹を付けない。
- **連桁(beam)**: 上記の拍のまとまりを跨がない範囲で、連続する8分音符・16分音符は
  個別の旗ではなく符幹を繋ぐ横棒(連桁)でまとめる。16分音符は連桁2本。
- **休符の縦位置**: 全休符は第4線からぶら下がる、2分休符は第3線(中央線)の上に乗る。
  4分休符以下は独自のくねり形+旗で表現。
- **音価に比例した横幅**: `SCORE_DURATION_WIDTH_UNITS`で音価ごとの相対幅を持ち、
  `scoreElementWidth()`で実ピクセル幅に変換して各要素・各小節の幅を決める。
- **調号**: 五線冒頭にト音記号の右に描く。♯系は F,C,G,D,A,E,B の順、♭系は
  B,E,A,D,G,C,F の順で増える(五度圏どおり)。各文字をどの段位置(線/間)に描くかは
  実際の記譜法の慣習で決まっている固定パターンなので、計算式ではなく
  `SCORE_SHARP_STEPS`/`SCORE_FLAT_STEPS` にそのまま配列で持たせている。
- **小節線**: 小節の終わりに自動で挿入。曲全体の最後の小節だけ、太線を添えた
  終止線(double barline)にする。
- **臨時記号は符頭の左**、**コード名は五線の外側(上)**、**3連符ラベルはグループ先頭のみ**。

## タップ入力と複数段

エディタは常に末尾に音符を追記していく方式なので、タップされた段(システム)が
どれであるかは実は無視して良い。`scoreYToStepAnySystem()`は、タップY座標を
「段の高さ+段間隔」の周期で割った余りだけを見て段内の相対位置(＝音の高さ)を
求める。どの段をタップしても同じ相対位置なら同じ音高になり、常に末尾に追記される
ので、これで矛盾なく動く。

## 意図的に本物の記譜と異なる簡略表現・既知の制約

- ト音記号は専用フォント/SVGを使わず絵文字「🎼」で代用(専用フォント無しでも
  確実に表示されることを優先)。
- 連桁は音の高さに関係なく水平(フラット)に揃えて描く簡易版。本来は音型に合わせて
  斜めにするのが一般的だが、実装・見た目判定の複雑さに見合わないと判断。
- **異なる音価が混ざる連桁の部分連桁(secondary beam)は非対応**。例えば8分音符+
  16分音符2つが同じ拍内にある場合、実際の記譜では8分音符の連桁が2本のうち1本だけ
  16分音符側に伸びる「部分連桁」で表現するが、これは実装していない
  (同音価の連続のみを連桁化する)。混在パターンはそれぞれ個別の旗で描かれる。
- **小節をまたぐ音符の自動タイ分割は非対応**。音価の合計が拍子にぴったり合わない
  入力をした場合(例: 4/4の小節の途中に全音符を置いてしまった等)、実際の記譜法なら
  小節線をまたいでタイで2つに分割して描くべきだが、v1では「容量に達した/超えた
  音符の直後で区切る」だけで、その音符自体を分割することはしない。入力側(ユーザーが
  拍子に合った音価を選ぶこと)に依存する設計。
- 3連符は見た目のグループ化(`tripletGroup`・「3」ラベル)はできるが、時価の計算
  (`scoreElementDurationUnits()`で2/3倍している)は小節分割・連桁判定には反映される
  ものの、小節をまたぐ3連符の自動分割は上記のタイ分割非対応の制約を受ける。
- スラー・強弱記号(クレッシェンドの線など)は音符列とは別に「テキストメモの
  スタンプ」(`DEFAULT_STAMPS`: p, mf, cresc. など)として譜面画像の上に別途貼る運用。
  楽譜エディタ自身の記譜要素としては持たない。
- 1つの楽譜につき拍子・調号は1つだけ(スコープの前提を参照)。
