# ChordWikiライブビューア 使い方マニュアル

ライブ演奏中にiPadでコード譜(画像/PDF)を自動スクロール表示するためのアプリ。
**https://kentarousr.github.io/chordwiki-live-viewer/**

## 目次

1. [はじめの一歩(曲の登録)](#1-はじめの一歩曲の登録)
2. [演奏モード / 編集モード](#2-演奏モード--編集モード)
3. [自動スクロールの操作](#3-自動スクロールの操作)
4. [メモ機能(テキスト・画像・手書き)](#4-メモ機能テキスト画像手書き)
5. [BPM・メトロノーム](#5-bpmメトロノーム)
6. [YouTube参考動画パネル](#6-youtube参考動画パネル)
7. [セットリスト](#7-セットリスト)
8. [練習ToDo](#8-練習todo)
9. [音楽リンク集](#9-音楽リンク集)
10. [フレーズ集](#10-フレーズ集)
11. [フットコントローラー(譜めくりペダル)](#105-フットコントローラー譜めくりペダル)
12. [チューナー](#107-チューナー)
13. [連桁の切り方](#1075-連桁音符をつなぐ横棒の切り方)
14. [他のアプリから楽譜を読み込む(MusicXML)](#108-他のアプリから楽譜を読み込むmusicxml)
15. [クラウド保存(あなた用のURL)](#11-クラウド保存あなた用のurl)
16. [ZIPバックアップ](#12-zipバックアップ)
17. [オフライン対応・ホーム画面への追加](#13-オフライン対応ホーム画面への追加)
18. [困った時に](#14-困った時に)
19. [使い方のデータについて](#15-使い方のデータについて)

## 1. はじめの一歩(曲の登録)

1. ChordWikiでコード譜を**移調済みの状態**にし、フルページのスクリーンショット(画像)またはPDFとして保存する
   - **本番中の移調はできない**ので、キーは事前に確定させておくこと
2. 曲一覧画面の「＋ 曲を追加」から、保存した画像/PDFを選択する(曲一覧画面へのドラッグ&ドロップでも追加できる)
3. 曲名を聞かれるので入力する(BPMは聞かれない。設定したい場合はビューアの「BPM」ボタンから後で個別に設定する)
4. 曲一覧に追加され、タップすれば表示・自動スクロールできる

### 複数ファイルの一括登録

「＋ 曲を追加」で複数ファイルを選択する、または曲一覧画面に複数ファイルを一度にドラッグ&ドロップすると、**1件ずつ名前を聞かれることなく一括で登録される**(ファイル名がそのまま曲名になる)。曲名を変更したい場合は、曲一覧の各曲の「編集」ボタンから後で変更できる。画像/PDF以外のファイルが混ざっていた場合はスキップされ、完了時に件数がまとめて表示される。

### 曲一覧の並び替え

検索ボックスの右にあるプルダウンで並び順を切り替えられる(選んだ並び順は保存され、次にアプリを開いた時も維持される)。

- **更新が新しい順(デフォルト)**: メモ編集など最後に触った曲が上に来る
- **曲名順**: あいうえお順/アルファベット順
- **追加が新しい順 / 追加が古い順**: 曲を登録した日時基準

曲一覧が増えてきたら、上部の検索ボックスで曲名絞り込みができる。

## 2. 演奏モード / 編集モード

曲一覧画面の「✎ 編集モード」/「▶ 演奏モード」ボタンで切り替える。**この状態は保存され、次にアプリを開いた時も維持される。**

- **編集モード(デフォルト)**: メモ配置・手書き・動画パネル・BPM編集など、全ての機能が使える。普段の練習・準備用
- **演奏モード**: ツールバーが最小限(戻る/前後の曲送り/再生/クリック/速度/進捗バー)になり、編集系のボタンはツールバーから完全に消える。本番前にこちらへ切り替えておくと、誤操作の心配がなくなる

**本番に入る前に、曲一覧画面で演奏モードへの切り替えを忘れずに。**

## 3. 自動スクロールの操作

- 「▶ 再生」で自動スクロール開始、もう一度押すと停止
- 「－」「＋」でスクロール速度を1刻み、「－5」「＋5」で5刻みで調整(どちらも長押しで加速)。曲ごとに保存される。表示される数字は目安の値で、実際に動く速さはその半分(例: 「速度20」は実測10px/秒)
- 指でドラッグすると手動スクロールでき、再生中にドラッグすると自動で一時停止、離すと自動的に再開する
- 素早く指を払うようにドラッグすると、離した後も慣性でスーッと減速しながらスクロールする(Safariのスクロールに近い動き)。ゆっくり動かして止めた場合は今まで通りその場で止まる
- 末尾まで来ると自動で停止する
- **ズーム(編集モードのみ)**: 譜面の**右上に浮いている**「－ 100% ＋」ボタンで100%〜300%まで拡大できる(25%刻み。2026-08-19にツールバーの中から移動した。ツールバーは再生中に自動で隠れるうえボタン数も多く、ズームだけ使いたい時に遠かったため)。縦に長い譜面・ピアノ譜のように密度が高くて読みにくい譜面向け。ズームすると横方向にも見きれる部分が出るので、ドラッグで左右にも動かせる(慣性スクロールも縦横両方に効く)。ズーム率は曲ごとに保存され、次に開いた時も同じ倍率で表示される。演奏モードではロックされる(拡大縮小できない)。倍率の既定は**100%**。指でのピンチは既定では効かず、隣の「⤢」ボタンでONにした時だけ使える(後述)。ブラウザ標準のピンチズームは譜面エリア全体で無効化してある(ツールバーごと拡大されて操作しづらくなるため)
- 再生中はツールバーが自動的に隠れる(画面をもう一度タップすると再表示)
- 編集中などツールバーが譜面を隠してしまう時は、バー左端の折りたたみボタン(&#9207;)でバー自体を最小化できる。もう一度押すと元に戻る
- 画面消灯防止(Wake Lock)が働くので、ビューア画面を開いている間(再生中でなくても、MCメモ表示中でも)は画面が暗くならない

### 演奏時間から速度を自動計算する(2026-08-19〜)

**「⏱ 速度計算」ボタン**に曲の長さを入れると、譜面の最後までちょうど流れる速度を計算して設定する。

- YouTubeの再生時間や音源の長さをそのまま入れればよい。`3:45`でも`225`(秒)でもよい
- 「今の拡大率で、譜面の末尾が画面の下端に来るまで」を基準に計算する。**倍率を変えたら計算し直すこと**
- 速度は整数なので、狙った時間から数秒ずれることがある。実際に流して「−」「＋」で微調整する

## 4. メモ機能(テキスト・画像・手書き)

曲一覧で編集モードにした状態で、ビューア画面の「📝 メモ」ボタンを押すと、以下の操作ボタンが表示される(演奏モードでは「📝 メモ」ボタンごと表示されない)。

> **「+ テキスト」と「✎ ペン」は「📝 メモ」を押さなくても最初から出ている**(2026-08-19〜。この2つは使用頻度が段違いに高く、毎回1タップ挟むのが煩わしいため)。押すと自動でメモ編集モードに入るので、そのまま譜面をタップして配置・描画できる。「+ 画像」「+ 楽譜」「🔵 蛍光ペン」は従来どおり「📝 メモ」を押してから使う。

- **「+ テキスト」**: 押すとボタンがハイライトし、ラベルが「譜面をタップ」に変わって配置待ち状態になる(案内ダイアログは出ない)。譜面上の配置したい場所をタップ→その場に入力欄が出るので入力し、他の場所をタップ(またはEnter)すれば確定する(OKボタンは無い)。Shift+Enterで改行、Escapeでキャンセル。配置後はドラッグで移動、右下の丸ハンドルで幅をリサイズ(文字サイズも幅に比例して一緒に変わる)、タップすればまた内容を編集できる。もう一度「+ テキスト」を押すと配置待ちをキャンセルできる。左上の「✅」を押すとそのメモの内容を練習ToDoに追加できる(1曲で複数のメモを追加した場合、曲名のメインタスク1つの下に各メモがサブタスクとして束ねられていく。「1曲に練習項目がいくつも出てくる」ケースに合わせた形。→[8. 練習ToDo](#8-練習todo)参照)
  - **スタンプ機能**: テキスト入力中は画面上部に「スタンプバー」が出る。初回は`p / mp / mf / f / ff / cresc. / dim. / 〜(伸ばす) / ✂(ビートオフ・カット) / ♪3(3連ノリ) / Shuffle`がデフォルトで用意されている(1回だけ自動投入されるもので、全部消しても勝手には復活しない)。「＋ スタンプ追加」でよく使う定型文言を自由に追加できる。登録したスタンプはタップでカーソル位置にそのまま挿入される。「編集」ボタンを押すとスタンプが赤くなり、その状態でタップすると削除できる(確認ダイアログなし。個人用の定型文言なので気軽に消して作り直してよい)。スタンプは他の端末ともクラウド同期される
- **「+ 画像」**: 押すとボタンがハイライトし、ラベルが「譜面をタップ」に変わって配置待ち状態になる。配置したい場所をタップ→画像を選択、またはクリップボードから貼り付け(Ctrl+V)。配置後はドラッグで移動、右下の丸ハンドルでリサイズできる。もう一度「+ 画像」を押すと配置待ちをキャンセルできる
- **「+ 楽譜」**: アプリ内でメロディー(単音・和音どちらも)の五線譜を作成し、画像として譜面に貼り付けられる。押して配置したい場所をタップすると「楽譜作成」画面が開く(画面右上の「❓」から、この節の使い方を別タブで開ける。作成中の内容は消えない)
  - 冒頭の「拍子」(4/4・3/4・2/4・6/8・9/8・12/8)と「調号」(C・G・D…などキー文字で選ぶ。♯♭の本数は五線に描かれる調号を見れば分かる)を選ぶ。曲の途中でも変更できるが、譜面全体に1つずつ適用される(1曲=1拍子・1調のシンプルな運用)
  - **音符の入力は画面下のピアノ鍵盤で行う**(2026-08-17〜。それまでの「五線譜を直接タップ」方式から、実際の記譜アプリ([Flat](https://flat.io/ja)等)に近い操作感に変更した)。音価(全/2分/4分/8分/16分/32分/64分)を選んでから、鍵盤の**白鍵/黒鍵をそのままタップ**すれば、末尾に音符が1つ追加される(左から順に並んでいく)。黒鍵をタップすると自動で♯か♭が付く(既定はモード理論に沿った表記。下記参照。「♭↔♯ 異名同音」ボタンで全部♯/全部♭に手動固定もできる)。**小節線は拍子に応じて自動で挿入される**(音価の合計が1小節ぶんに達したところで自動的に区切られる)
  - **黒鍵の♯/♭はモード理論に沿って自動で決まる(2026-08-19〜)**。アイオニアン(メジャースケール)から見た各モードの変化音は`♭2`(フリジアン)・`♭3`(ドリアン等)・`♯4`(リディアン)・`♭6`(エオリアン等)・`♭7`(ミクソリディアン等)で、**♯になるのはリディアンの`♯4`だけ**、残りは全て♭になる。従来は「♯系の調は全部♯・♭系の調は全部♭」と倒していたが、実際の記譜に合わせた
    - 例: **Cキー**なら`Db` `Eb` `F#` `Ab` `Bb`(トライトーンのFだけが♯)
    - 例: **Gキー**なら`C#` `Eb` `F#` `Ab` `Bb`(`C#`が♯4。`F#`は調号の音なのでそのまま)
    - その調の音階に含まれる黒鍵(ト長調の`F#`、変ホ長調の`Eb`など)は調号どおりの表記になる
    - 調によっては黒鍵が全部♯/全部♭になる。E・B・F#キーは全部♯(調号の♯がちょうど黒鍵に重なり、♯4も黒鍵)、F・B♭・E♭・A♭・D♭・G♭キーは全部♭(トライトーンが白鍵なので♯が出てこない)
    - 「♭↔♯ 異名同音」ボタンを押すと、この自動判定をやめて全部♯/全部♭に手動固定できる(もう一度押すと自動に戻る)
  - **「⇥ 挿入」ボタン(2026-08-19〜)**: オフの間は音符が常に末尾に追加されるため、途中のミスに気付くと後ろを全部消して打ち直すしかなかった(削除は途中でもできるのに挿入だけ末尾限定、という非対称だった)。**オンにして音符を選択すると、その音符の直前に挿し込まれて後ろが押し出される**
    - 続けて入力すると、挿し込んだ音符が左から右へ順に並ぶ(選択は押し出された元の音符を追い続ける)
    - 鍵盤タップ・文字キー・MIDI・休符のどれでも挿入される。何も選択していない時は従来どおり末尾に追加
    - PCでは`I`キーで切り替えられる
    - **和音モードとは同時にオンにできない**(どちらも「選択中の音符」の意味を奪い合うため、片方をオンにするともう片方が切れる)
  - **「↶ 取消」「↷ やり直し」ボタン(2026-08-19〜)**: 操作を取り消す/やり直す。**転調や全消去も取り消せる**
    - 「✖ 直前を削除」は名前が似ているが別物で、履歴とは関係なく音符を1つ消すだけの操作
    - PCでは`Ctrl+Z`/`Ctrl+Y`も使える(以前はこちらしか無く、スマホでは取り消す手段が一切なかった)
  - **「音部」で音部記号を選べる(2026-08-19〜)**: ト音記号/ヘ音記号。**ヘ音記号にするとベースラインが書ける**
    - 切り替えても**音の高さ(データ)は変わらない**。同じ音が五線の正しい位置に描き直されるだけ
    - 鍵盤の音域も一緒に切り替わる(ト音記号はド3〜ド6、ヘ音記号はド1〜ド4)
    - フレーズ集に保存すると音部記号も一緒に保存される(音部記号を指定していない古いフレーズはト音記号として開く)
  - **鍵盤の「◀ 低音」「高音 ▶」ボタン(2026-08-19〜)**: 鍵盤を1オクターブ単位で移動する。鍵盤は画面幅より横に長く、それまでは横スクロールでしか音域を移動できずスマホで操作しづらかったための対応。中央のラベルに今見えている一番低い音が出る(端まで来るとボタンが無効になる)
  - **「弱起」で1小節目だけ短くできる(2026-08-19〜)**: 実際の曲は1拍目から始まらないものが多いが、それまでは常に満杯で数えていたため小節線が実際の曲とずれていた。8分1つ/4分1つ/付点4分/2分/4分3つから選べる。**音符データは変わらず、小節の区切り位置だけが変わる**
  - **「強弱」「奏法」で選択中の音符に記号を付けられる(2026-08-19〜)**
    - 強弱: `pp` `p` `mp` `mf` `f` `ff` `sf` `fp`(五線の下に太字イタリックで表示)
    - 奏法: スタッカート/アクセント/テヌート/マルカート(上下の位置は符幹に合わせて自動)
    - **選択中の音符にだけ効く**(コード名のような「次の音符に付く」保留の仕組みは持たせていない。これらは普通メロディーを書いた後から付けるため)。音符を選んでいない時と休符を選んでいる時は選べない
  - **既に置いた音符は、そのまま上下にドラッグすると音の高さを直せる**(掴んだ瞬間から動く。長押しで待つ必要はない)。ドラッグ中は青くハイライトされ、指を離した後もその音符が選択されたままになるので、続けて音価や♯♭も直せる
  - **既に置いた音符をタップすると(指を動かさずに離すと)、その音符が青くハイライトされて選択状態になる**(段や順番は問わず、どの音符でも選べる。対応するピアノ鍵盤のキーも青くハイライトされる)。選択中は「付点」「3連」以外のツールバー操作(音価ボタン・コード名欄)がその音符に直接反映される。**選択中にピアノ鍵盤の別のキーをタップすると、その音符の音高がまるごと上書きされる**(選択は解除されない。五線譜の何もない場所をタップして音高を変える方法もそのまま使える)。選択中の音符をもう一度タップすると選択だけ解除する。「↩」ボタンは選択中は「選択中の音符を削除」に変わる
  - 「付点」「3連」「♯」「♭」「♮(ナチュラル)」は、何も選択していない時はその場でON/OFFを切り替えてからピアノ鍵盤で音符を置くと反映される(白鍵をタップした場合に適用される。黒鍵は既に♯/♭を含むため、代わりに保留中の指定がそちらを上書きする)。「3連」はONにしてから3つ続けて置くと自動でOFFに戻る(3連符は既に置いた音符への後編集は非対応)。調号で♯/♭が付く音を元に戻したい時は「♮」を使う(♮を保留中はピアノ鍵盤の黒鍵が一時的に押せなくなる。ナチュラルは理論上白鍵の音にしかならないため)
  - **「⋃ タイ」ボタン(2026-08-17〜。2026-08-20に向きを反転)**: 選択中の音符と、その**1つ前**の音符を弧線でつなぐ(2つの音符が同じ高さの時だけ有効。違う高さの時は押してもアラートが出るだけ)。打ち込みは左から右へ進むので、音を置いた直後にそのまま「今の音を前の音とつなぐ」と押せる向きにしてある(以前は「次の音符とつなぐ」だったので、前の音を選び直す手間があった)。もう一度押すと解除できる。小節をまたいでつないでも正しく音の高さが引き継がれる。**ショートカット: T**
  - **「↵ 改行」ボタン(2026-08-19〜)**: 選択中の音符が入っている小節の後ろで段を変える。**どこで改行するかを自分で決められる**(それまでは幅に収まらなくなったところで自動的に折り返すだけだった)。自動の折り返しも残してあるので、指定しなくても画面外に流れることはない。もう一度押すと解除
  - **「𝄞 冒頭記号」ボタン(2026-08-19〜)**: 各段の頭に出る音部記号と調号を消せる。コード譜に貼る短い断片では場所を食ううえ情報としても冗長なことが多いため。**表示だけの切り替えで、音の高さの解釈は変わらない**(調号を消しても♯♭は正しく反映されたまま)。この設定は端末に記憶される
  - **「8va」ボタン(2026-08-20〜)**: 選んだ範囲を**オクターブ記号**で書く。押すたび `8va`(1オクターブ上) → `8vb`(1オクターブ下) → 解除、と一巡する。Shiftを押しながらで範囲を選んでから押すと、その範囲全部に付く
    - 加線だらけで読みにくい高音・低音を「1オクターブずらして書き、記号でそのぶんを示す」ための記譜法。譜面が一気に読みやすくなる
    - **鳴る音の高さは変わらない**(変わるのは書く位置だけ)。音を鳴らす機能も移調も従来どおり正しく動く
  - **「⌣ スラー」ボタン(2026-08-19〜)**: 選択中の音符と次の音符をスラー(フレーズのまとまりを示す弧線)で結ぶ。**タイと違って音の高さが同じである必要は無い**(タイは同じ音の継続、スラーはフレーズのまとまりを示すもので意味が違うため)。もう一度押すと解除
  - **「|: 反復開始」「:| 反復終了」ボタン(2026-08-19〜)**: 繰り返し記号を付ける。反復記号は本来「小節」に付くものだが、このエディタで選べるのは音符なので**選択中の音符が入っている小節に付く**形にしている。同じ小節に開始と終了の両方を付けることもできる。もう一度押すと解除
  - **「♪ 単音」/「🎶 和音」ボタンで和音入力に切り替えられる**(2026-08-17〜)。単音モードは「選択中に別の音をタップ=音高を直す(上書き)」だが、**和音モードは常に「タップした音を追加、既に入っている音をもう一度タップすると削除」という足し引き(トグル)方式**になる(1つの音符の中で「直す」と「足す」は両立できないため、和音モードでは意図的に上書きをしない設計。音を間違えた時は、間違えた音をもう一度タップして消してから正しい音を足す)。和音モードは入力元(ピアノ鍵盤/文字キー/MIDI)を問わず、音符を置くと自動的に選択状態が続くので、続けてタップするだけで声部を積み重ねられる。次の和音に移る時は→キーかEscape(またはピアノ鍵盤以外の場所をタップ)で選択を外す。MIDIキーボードの場合は和音の鍵盤を全部離すと自動的に選択が外れる。ドラッグや↑↓キーでの音高調整は、和音全体が同じ音程でまとめて動く(声部ごとの個別調整は非対応)
  - 「コード名」欄に入力してから音符を置くと、その音符の上にコード名が表示される。コード名は「ルート音は通常サイズ、それ以外(m7・△9・7♭9等)は上付き文字」という実際のコード譜と同じ表記で描かれる(例: 「E♭△9」→Eフラットが通常サイズ、△9が小さく上付き)
  - **「⚡ コード略記」ボタン(2026-08-19〜。2026-08-20から既定でオン)**: オンにすると、コード名欄が[ChordWikiのコード譜エディタ](https://www.chordlistener.com/editor/)と同じキー割り当てになり、**Shiftキーを押さずにコード名を打てる**。コードの先頭(と`/`の直後)だけは`A`〜`G`が音名として扱われ、それ以外の位置では下の表の修飾子になる(例: `gvlj[w9]/df` と打つと `GbbaugM7(#9)/Db` になる)。割り当ての無い文字(数字・`m`など)はそのまま入力される。この設定は端末ごとに記憶される

    | キー | 出力 | | キー | 出力 |
    | --- | --- | --- | --- | --- |
    | `w` | `#` | | `z` | `sus` |
    | `f` | `b` | | `y` | `add` |
    | `x` | `##` | | `q` | `omit` |
    | `v` | `bb` | | `p` | `+` |
    | `l` | `aug` | | `[` | `(` |
    | `h` | `dim` | | `]` | `)` |
    | `j` | `M7` | | `\` | `/` |
    | `r` | `m7` | | | |
    | `k` | `m7-5` | | | |

  - **入力した音は実際に鳴る(2026-08-19〜)**: ピアノ鍵盤タップ・文字キー・MIDIのどれで入力しても、その音が鳴って高さを耳で確認できる。`←`/`→`で音符をたどる時や`↑`/`↓`で高さを直す時も鳴る。和音モードでは声部を足すたびに和音全体が鳴る。**「🔊 音あり」ボタンでオフにできる**(電車内・楽屋など静かな場所向け。この設定は端末ごとに記憶される)
  - **「転調」の−1/+1(半音)・−12/+12(オクターブ)ボタン(2026-08-19〜)**: 譜面全体を移調する。音符の高さだけでなく、**音符に振ってあるコード名と調号もまとめて動く**(詳細は[10. フレーズ集](#10-フレーズ集)の「転調」を参照)
  - **「🎵 フレーズ読込」ボタン(2026-08-19〜)**: [フレーズ集](#10-フレーズ集)に保存した譜面を読み込む。貯めたフレーズをそのまま曲の譜面に貼れる(読み込んでから転調して貼ることもできる)
  - 「休符」ボタンで選択中の音価の休符を直接追加できる(タップ不要)
  - 描画は記譜ライブラリ[VexFlow](https://github.com/0xfe/vexflow)(MIT、内部でBravuraフォントを使用)に任せている。連続する8分/16分音符は、**拍のまとまりを跨がずに**自動で連桁(連なった旗)にまとめて表示され(6/8等の複合拍子では付点4分ごとにまとまる)、**音価が混ざっていても**(8分+16分など)正しく主連桁+副連桁(または内向きのフック)に分けて描かれる。連桁は音の高さの流れに沿って自然に傾く。音符の横幅は音価に応じて変わる(全音符は8分音符よりも幅を取る)
  - 臨時記号(♯♭♮)は、調号どおりの音や同じ小節内で既に表示済みの音には自動で省略される(実際の楽譜と同じ、小節をまたぐと調号どおりの状態にリセットされる)
  - **音価の合計が小節にぴったり収まらない音符を置くと、自動的にタイでつないだ2つ以上の音符に分割される**(例: 4/4の小節末ギリギリに4分音符を置くと、小節末までの8分音符+次の小節頭の残りの音符がタイで結ばれる)
  - **小節の真ん中をまたぐ短い音符は、自動的にそこで分割してタイで結ぶ**(2026-08-20〜)。例: 4/4で `16分 → 付点8分 → 16分 → 16分 → 付点8分 → 16分` と置くと、2つ目の付点8分が小節の真ん中(3拍目の頭)をまたぐので、`8分 + 16分` に切ってタイで結ぶ
    - **分割するのは小節線と小節の真ん中だけ**。4/4で3拍目を隠す音符は読みにくいというのが記譜法の定番の禁則なので、そこだけ守る。普通の拍はまたいでよい(シンコペーションはそのまま書いた方が読みやすい)
    - 1拍以上の音符(4分・付点4分・2分など)と3連符は分割しない
    - 3/4・9/8のように2等分が拍の境界に来ない拍子では、小節線でしか分割しない
    - **連桁(はたをつなぐ横棒)は分割とは別に、常に拍のまとまりで揃う**。音符が拍をまたいだ位置で連桁を切っているため、分割しなくても拍の区切りは読める
  - **1小節がまるごと休みの時は、選んだ音価に関わらず全休符の記号で表示される**(3/4拍子でも同様。実際の楽譜の慣習どおり)
  - 3連符のうち、4分音符など旗を持たない音価が混ざって連桁でまとめられない場合は、連桁の代わりに角カッコ(ブラケット)+「3」で表示される
  - 長い曲は五線が自動で複数段に折り返される(段が変わるたびにト音記号・調号を描き直す。実際の楽譜のような見た目になる)
  - 「✖ 直前を削除」(音符選択中は「✖ 選択中の音符を削除」)「全消去」で編集、「完了」を押すと画像化されて画像メモと同じ扱いで譜面に挿入される(ドラッグ移動・リサイズも画像メモと同様に可能)。「キャンセル」で破棄
  - **「▾ ツール」ボタンでツールバーをまるごと畳める(2026-08-19〜)**: スマホだとボタン群が画面の大半を占めて譜面がほとんど見えないため。畳むと五線譜とピアノ鍵盤だけになる。この状態も端末に記憶される
  - **画面内の操作説明は「❓ 使い方」ボタンにまとめてある(2026-08-19〜)**。以前は編集画面の下に長い説明文を常時表示していたが、譜面が見えなくなるほど場所を取っていたため削除した。このボタンを押すとこのマニュアルの該当箇所が別タブで開く(作成中の内容は消えない)
  - **鍵盤をタップすると一瞬光る(2026-08-19〜)**。押せたかどうかが分かりにくかったための対応。MIDIキーボードで物理的に押している間光る緑のリングとは別の表示(背景色)なので、同時に光っても区別できる
  - **キーボードショートカット一覧パネル(PCのみ、2026-08-19〜)**: ヘッダーの「⌨ キー」ボタンで、よく使うショートカットの一覧を画面上に出せる。**ヘッダー部分をドラッグして好きな位置に動かせる**(位置と開閉状態は端末に記憶され、次に開いた時も同じ状態になる)。「×」または「⌨ キー」で閉じる
    - **スマホには表示されない**(物理キーボードが無くショートカットを使えないため)。マウスを持つ端末でだけ出る仕組みなので、キーボードを付けたiPadでは表示される
  - **キーボードショートカット(PC、2026-08-17〜)**: コード名入力欄や拍子/調号のセレクトにフォーカスが無い時だけ効く。`A`〜`G`は**常に新しい音符を末尾に追加する**(選択中の音符があっても上書きしない。「入力したら次に押した音は次の音符になってほしい」という運用に合わせている)。追加した音符は自動的に選択状態になるので、続けて`↑`/`↓`(半音)や`Shift+↑`/`Shift+↓`(オクターブ)でその場で高さを微調整できる。既存の音符の高さを後から直したい時は、ピアノ鍵盤のタップ(選択中に別のキーを押すと上書きされる)か、五線譜上でのドラッグ/タップを使う。

    **左手だけで完結する配置(2026-08-20〜)**。MIDIキーボードでステップ入力する時は右手が鍵盤に乗りっぱなしになるので、編集操作は全部キーボードの左側だけで届く位置にまとめてある。使っているのは音名キー`A`〜`G`と重ならない`Q` `W` `S` `Z` `X` `V` `R` `T`と`Tab`だけ。

    | キー(左手) | 動作 |
    | --- | --- |
    | `1`〜`5` | 音価を切替(全/2分/4分/8分/16分) |
    | `Q` | 付点をトグル |
    | `W` | 3連符をトグル |
    | `R` | 休符を追加 |
    | `T` | 1つ前の音符とタイで結ぶ |
    | `Z` / `X` / `V` | ♭ / ♯ / ♮ をトグル |
    | `S` | 挿入モードの切り替え |
    | `Tab` | 次の音符を選択 |
    | `Shift`+`Tab` | 前の音符を選択 |
    | `` ` ``(キーボード左上の角) | 選択中(無ければ直前)の音符を削除 |
    | `Escape` | 選択解除(画面は閉じない) |
    | `Ctrl+Z`(Macは`Cmd+Z`) | 元に戻す |
    | `Ctrl+Y` / `Ctrl+Shift+Z`(Macは`Cmd`) | やり直す(「✖ 直前を削除」ボタンとは別物の、本物のundo/redo) |

    | その他のキー | 動作 |
    | --- | --- |
    | `A`〜`G` | 新しい音符を追加(常に追加。上書きはしない) |
    | `6` `7` | 32分 / 64分 |
    | `↑` / `↓` | 選択中の音を半音上げる / 下げる |
    | `Shift`+`↑` / `Shift`+`↓` | 選択中の音をオクターブ上げる / 下げる |
    | `Home` / `End` | 最初 / 最後の音符を選択 |
    | `←` / `→` | 前 / 次の音符を選択(`Tab`と同じ) |
    | `Delete` / `Backspace` | 削除(`` ` ``と同じ) |
    | `.` `-` `=` `0` `I` | 付点 / ♭ / ♯ / ♮ / 挿入モード(旧割り当て。そのまま使える) |

    なお、ピアノ鍵盤のタップやMIDIキーボードでの入力は従来どおり非選択のまま追加される(連続タップ/演奏でどんどん音符が増えていく方を優先しているため。文字キーだけが「入力→調整→次へ」の流れ用に選択状態を保つ)。
  - **MIDIキーボード入力(対応ブラウザのみ、2026-08-17〜。2026-08-20から既定でオン)**: Web MIDI APIに対応したブラウザ(Chrome/Edge等のPC版。Safari/iOSは非対応のため、対応環境でのみツールバーに「🎹 MIDI入力」ボタンが表示される)では、**エディタを開くと自動的に接続を試みる**。USB/Bluetooth MIDIキーボードを繋いでおけば、弾いた音がそのままピアノ鍵盤タップと同じ扱いで入力される(何も選択していなければ末尾に追加、選択中ならそのピッチを上書き)。音の長さは鍵盤の押している時間ではなく、その時点で選んでいる音価ボタンに従う。ボタンで明示的にオフにした時だけその設定が記憶される(MIDI機器を繋いでいない・許可がまだ無いといった理由で自動接続に失敗しても、勝手にオフ扱いにはならないし、アラートも出ない)
  - **ヘッダーの「⏺ 録音」でクリックに合わせて弾くと、そのまま楽譜になる(2026-08-20〜。MIDI対応ブラウザのみ)**
    - ボタンを押すと録音パネルが開く。BPM・カウントイン・量子化・入れ方を決めて「録音開始」
    - カウントインのクリックが鳴ってから録音が始まる。弾き終わったら「停止して記譜」
    - **音の長さは押している時間から取る**ので、切って弾けば休符が入る。次の音に重なるまで伸ばした場合(レガート)は次の音の直前で切られる
    - **同時に押した音は和音として1つの音符にまとまる**
    - **3連符を自動で聞き分ける。** 1拍ごとに「直線のグリッド」と「3連のグリッド」の両方に当てはめてみて、ズレが小さい方を採用する。曲の途中で3連と直線が混ざっていても書き分けられる。迷う程度の差なら直線を採る(誤判定で読めない譜面になるのを避けるため)
    - 頭の音を少し走って弾いてもちゃんと拾う(グリッド半分ぶんまでは「1拍目が少し早かっただけ」とみなす)
    - **6/8・9/8・12/8ではBPMは付点4分を指す**(記譜の慣習どおり)。クリックも付点4分ごとに鳴る
    - 「入れ方」を「末尾に追記」にしておけば、既に打ち込んだ譜面を消さずに足せる。既定は追記
    - 録音した結果は普通の音符なので、そのあと五線譜でもピアノロールでも直せる
  - **画面上のヘッダーにある「▦ ロール」でピアノロールに切り替えられる(2026-08-20〜)**
    - ヘッダー(「キャンセル」「完了」と同じ行)にあるので、**ツールバーを畳んでいても押せる**
    - 横軸が時間、縦軸が音の高さ。**左端の鍵盤は音名(C・C#・D…)つきで、横にスクロールしても貼り付いたまま残る**(音名が無いと何の音か数えることになって使い物にならないため)。黒鍵の行には薄く色が敷いてある
    - **空いているところをタップすると音符が増え、音符をタップすると消える**
    - **音符をドラッグすると、時間の位置と高さを動かせる**(動かした先の音が鳴るので耳で確認できる)。**掴んだ位置を基準に「動かした分だけ」動く**ので、指が1〜2行ずれた所を掴んでも音符が飛ばない
    - **音符の右端(10px幅)を掴んで左右にドラッグすると長さを変えられる**。空いているところを押してそのまま右へ引けば、置くと同時に長さを決められる
    - **タイでつながれた音符は1つの音として表示される**(小節線をまたぐ音や3連符の分割で譜面上は複数に割れていても、鳴っているのは1つの音なので分断して見せない)
    - グリッドは4分/8分/16分/32分に加えて**8分3連・16分3連**が選べる。3連グリッドで置いた音符は、五線譜に戻ると3連符として書かれる
    - **五線譜と同じデータを別の形で見ているだけ**なので、どちらで直しても即座にもう一方に反映される(ピアノロール専用のデータは持たない)
    - 五線譜とは**切り替え**で表示する(同時に出すと両方の高さが半分になり、スマホやタブレットでどちらも操作しづらくなるため)
  - **段に入る小節数を「＋」「−」で調整できる(2026-08-20〜)**
    - 各段の左端に出る小さな「−」「＋」で、その段に入る小節数を手で増減できる
      - **−**: この段の最後の小節を次の段へ送る
      - **＋**: 次の段の最初の小節をこの段へ引き上げる(自動折り返しの幅を超えてでも引き上げる)
    - 自動折り返しだけだと「2段目だけ極端に短い」ような座りの悪い組み方になることがあるので、その調整用
    - **音符データは一切変わらない**(見た目の組み方だけが変わる)。Ctrl+Zで取り消せる
  - **「◑ 範囲選択」ボタンで、スマホ・タブレットでも範囲を選べる(2026-08-20〜)**
    - 手順: **範囲の始めの音符をタップ → 「範囲選択」を押す → 終わりの音符をタップ**。その間の音符がまとめて選ばれる
    - 選び終わると自動でモードが外れる(付けっぱなしだと次の1音で意図しない範囲になるため)
    - 選んだ範囲は `Ctrl+C`/`Ctrl+X`/`Ctrl+V` やボタンでコピー・切り取り・貼り付けできる(コード名も一緒に動く)
    - PCでは従来どおり **Shift+タップ** でも範囲を選べる。以前は Shift しか手段が無く、**物理キーボードの無い端末では範囲をまったく選べなかった**
  - **既知の制約**: タイが段(五線の行)をまたぐ場合、曲線は表示されない(音の長さ自体は正しく分割されている)。既に置いた音符の3連符所属(グループ化)は後から編集できない
- **「+ スタンプ」(2026-08-20〜)**: ツールバーの「+ テキスト」の隣にある。よく使う記号を、譜面の好きな場所にワンタップで貼れる。ボタンを押すとパレットが開くので、**スタンプを選んでから譜面の置きたい場所をタップ**する。パレットは開いたままなので、同じ記号を続けて何枚でも貼れる
  - 見た目は意味で2系統に分けてある。**記譜の記号(強弱・白玉)は黒インクのセリフ体で背景なし**、**進行や編成の指示(MC/TC・Beat・Oct・楽器)は色付きの札**。ライブ中に一瞬見るものなので、「音の弾き方の話か / 進行の話か」がパッと区別できることを優先した
  - 収録スタンプ(全59種)
    - **強弱**: `pp p mp mf f ff fff sfz fp cresc. dim.`
    - **奏法**: 白玉(放る) / フェルマータ / ブレス / `Fill` `Break` `Stop`
    - **進行**: `MC` `TC` `MC/TC` `Intro` `A` `B` `サビ` `Solo` `Outro`
    - **反復**: `D.S.` `D.C.` コーダ / セーニョ / `Fine`(記譜の記号なので札にせず黒インク)
    - **ノリ**: `4Beat` `8Beat` `16Beat` `HalfBeat` `Shuffle` `Swing` `Bossa`
    - **音域**: `Oct4`〜`Oct8`
    - **楽器**: `Gt Ba Key Dr Vo Cho Syn Per` の`only`(塗り)と`in`(白抜き)
    - **only は塗りつぶし、in は白抜き**。「他が抜ける=重い」「加わる=軽い」という差を見た目に持たせている
  - 右下の角をドラッグすると大きさを変えられる。**SVG(ベクター)なので拡大しても輪郭がぼけない**。データは記号のIDだけなので同期も一瞬で終わる
  - スタンプは今後も追加していく予定。増えても既に貼ってあるものは影響を受けない
- **ツールバーの「⋯」と表示のカスタマイズ(2026-08-20〜)**: スマホではツールバーが画面の3〜5割を占めて譜面がほとんど見えなかったため、**よく使うものだけを常時出し、残りは**「⋯ その他」**の中に畳む**ようにした
  - 既定で常時出るもの — ビューア: 再生 / クリック / 速度 / ＋テキスト / ＋スタンプ / ペン。楽譜エディタ: 音価 / 付点・3連 / 休符 / 改行 / 取消・やり直し / 単音・和音 / 挿入 / 直前を削除
  - **「⋯」→「⚙ 表示を選ぶ」で、どれを常時出すかを自分で決められる**。項目を押すたびに常時表示⇔「⋯」の中が入れ替わり、設定は端末に記憶される
  - しまったボタンは**複製ではなく本体をパネルに移している**ので、押した時の動作も拍子・調号などの選択状態もそのまま生きている(複製にすると二重管理になって必ず食い違うため)
  - **「← 曲一覧」だけは畳めない**(しまうと画面から抜けられなくなるため)
  - **「＋画像」「＋楽譜」「蛍光ペン」「BPM」は、以前は「📝 メモ」を押している間しか出なかったが、常時表示に選べるようにした**(自分で表示に選んだのに出てこないのは筋が通らないため)。押すと自分でメモ編集モードに入る
  - この整理で、390px幅の実測でビューアのツールバーは290→153px、楽譜エディタは468→149pxになり、**楽譜エディタの譜面が232→520pxと2倍以上**になった
- **「✎ ペン」「🔵 蛍光ペン」**: 押すと手書きモードに入り、譜面上に直接線を描ける。色・太さは各ツールごとに保存される。線をタップすると削除確認が出る。「↩ 元に戻す」で直前の一筆だけを即座に取り消せる
- メモ・線はすべて譜面と一緒にスクロールする(位置は相対座標で保存されるので、画面サイズが変わってもズレない)
- **テキストメモの右下をドラッグすると文字サイズが変わる**(2026-08-20〜)。箱は常に文字にぴったり吸い付くので、短い文言の右に余白が残らない。長い文言はこれまでどおり折り返す。
- メモの大きさ(文字サイズ・幅)は、画面の狭い端末ほど譜面に対して相対的に大きく見えてしまう問題があるため、「どの端末でメモを作った/調整したか」を基準にスケールする仕組みになっている。**新規に置いたメモ・リサイズしたメモは自動でその端末が基準になる**ので通常は意識しなくてよい

## 5. BPM・メトロノーム

### 譜面からBPMを読み取る(2026-08-19〜)

ChordWikiの譜面には先頭に`BPM=84`のように書かれているので、**「🔍 BPM読取」ボタンでそれを読み取って自動設定できる**(編集モードで「📝 メモ」を押すと出る)。

- 画像・PDFのどちらでも使える。譜面の上部だけを見るので数秒で終わる
- **初回だけ文字認識エンジン(約6MB)の読み込みが入る**ので時間がかかる。2回目以降はブラウザのキャッシュから読むので速い。**このボタンを押さない限り読み込まれない**ので、普段のアプリの動作やオフライン用データの容量には影響しない
- 読み取れなかった場合はその旨が出るだけなので、「BPM」ボタンから手で入力すればよい
- 読み取った文字は開発者コンソールに出るので、うまくいかない時の手がかりになる

### ピンチでの拡大縮小(2026-08-19〜。既定はOFF)

**右上の「⤢」ボタンを押してONにした時だけ**、譜面の上で2本指を広げる/狭めて拡大縮小できる(編集モードのみ。演奏モードでは従来どおり倍率が固定される)。

- **既定はOFF。** 当初は常時ピンチできるようにしていたが、譜面をなぞる時に指が2本触れやすく、そのたびにスクロールが打ち切られて倍率まで動いてしまい、まともにスクロールできなかったため、ボタンで切り替える方式に改めた
- OFFの時は2本目の指を完全に無視するので、指が何本触れてもスクロールが途切れない
- ONにするとボタンが緑に点灯する。設定は端末に保存されるので、次に開いた時も同じ状態
- 倍率は「指を置いた時点の間隔」が基準。間隔を2倍に広げれば200%、半分に縮めれば50%になる(右上のボタンと同じ25%刻み、50%〜300%の範囲)

- 「📝 メモ」を押した状態で表示される「🎼 BPM: --」ボタンで、曲ごとのBPMを設定できる
- 「🥁 クリック」ボタンでメトロノームを再生/停止(演奏モードでも使える)。BPM未設定の場合は先に設定するよう案内される

## 6. YouTube参考動画パネル

編集モード中のみ、ツールバーの「🎬 動画」ボタン(「📝 メモ」の隣)で開閉できる。

- **URL未設定の曲でパネルを開くと、曲名でYouTube検索を自動で新しいタブに開く**(検索結果ページはこのアプリの中には埋め込めない仕様のため別タブ)。同じ曲で開き直しても連発はしない(URLを設定するか別の曲に切り替えるまで1回だけ)
- 見つかった動画のURLは「YouTubeのURLを設定」から貼り付ける(以後その曲を開くたびに自動で呼び出される)
- パネルは指でドラッグして好きな位置に動かせる(ヘッダーの「ここをドラッグで移動」の部分をつかむ)
- **右下の角(&#10530;)をドラッグすると幅を変更できる**(動画は縦横比を保ったまま追従する)。選んだ幅は端末に記憶され、次回も同じ幅で開く
- 「戻る」「進む」ボタンで巻き戻し/早送り(秒数は－/＋で調整可能、練習しやすいように)
- 再生/一時停止ボタンあり
- **A-Bリピート(2026-08-19〜)**: 難所だけを繰り返し聴ける。再生しながら「A」を押すとそこが開始点、「B」を押すとそこが終了点になり、以後その区間を自動で繰り返す。「✕」で解除
  - BはAより後でないと設定できない。区間の外へシークしても自動で区間内に引き戻される
  - 曲を切り替えたり動画URLを変えたりすると区間は自動で解除される(前の曲の区間が残ると混乱するため)
- **オンライン専用機能。**機内モードでは動画は読み込めない(それ以外の全機能はオフラインで動く)
- 別の曲に切り替えると自動的に動画は止まる

## 7. 音源ファイル(端末内)

ビューア画面の「🎵 音源」ボタン(編集モードのみ)から、**その曲の音源ファイルを端末内に取り込んで再生できる**(2026-08-19〜)。

YouTubeは広告が入るうえ、埋め込み再生ではログイン状態を引き継げず、規約上も音声だけの再生やダウンロードはできない。自分で用意した音源(購入した音源やCDから取り込んだもの)を入れておけば、**広告なし・オフライン・任意区間のリピート**まで自由にできる。

- 「音源ファイルを選ぶ」から端末内の音声ファイルを選ぶ。よくある形式(mp3/m4a/wav等)はブラウザがそのまま再生できる
- 再生/一時停止・シークバー・「◀ 戻る」「進む ▶」(秒数は動画パネルの設定を共用)
- **速さ**を0.5〜1.5倍に変えられる(音程は変わらない)。難所をゆっくり確認するのに使える
- **A-Bリピート**: 「A」「B」で区間を決めると、その間を繰り返す。「✕」で解除
- 「差し替え」で入れ替え、「🗑 削除」で消せる(曲やメモは消えない)
- パネルはヘッダーをドラッグして好きな位置に動かせる
- 曲を切り替えると自動的に停止する

> **この音源はクラウド同期されない**(端末内のみ)。1曲5〜10MBと大きく、同期に時間がかかるうえ保存容量も圧迫するため。端末を変えた時は入れ直すことになる。

## 7. セットリスト

1. 曲一覧の「📋 セットリスト」から「＋ 作成」
2. セットリスト編集画面で「＋ 曲を追加」「＋ メモを追加」(MCの内容などを挟める)
3. 曲名・メモをタップして直接その項目から演奏を開始できる(セットリストの途中からでもOK)
4. 演奏中は「前」「次」ボタンでセットリスト内を移動できる。ボタンの間に「今何曲目/全何曲」が表示される
5. メモ項目は演奏中の画面ではタップしても何も起きない(誤操作防止)。ただし編集画面ではタップで直接編集できる
6. 曲順の入れ替えは3通り: ①↑↓ボタンで1つずつ、②行の左端の「⠿」をドラッグして好きな位置へ、③番号(1,2,3…)をタップして移動先の番号を直接入力(曲数が多い時に↑連打を避けられる)

## 8. 練習ToDo

曲一覧の「✅ 練習ToDo」から開く、**アプリ全体で1つ**の練習用チェックリスト(特定の曲やセットリストには紐付かない)。本番に向けて練習すべきこと・覚えたいリフやフレーズなどを管理するためのもの。

- 上部の入力欄に入力してEnter(または「＋ 追加」)で新しいToDoを追加
- チェックボックスでON/OFF。**チェックしても自動では消えない**(手動で外すまで残る)
- 項目のテキストをタップすると直接書き換えられる(他の場所をタップかEnterで確定、Escapeでキャンセル)
- 「＋ サブタスク」で、その項目の下に1段階だけサブタスクを追加できる(例:「Bメロのリフを覚える」の下に「ゆっくり8分音符で弾く」「テンポを上げる」など)
- 「×」で項目を削除(サブタスクも一緒に消える。確認あり)
- 曲一覧・セットリストと同様、クラウド同期の対象(新しい方が勝つ方式)
- 各項目には「+優先度」「+コスト」の2つのチップが付いていて、**タップするたびに 未設定→低(軽)→中→高(重)→未設定 と切り替わる**。優先度は普段は緑/黄/赤で重要度を表示、コストは**数値が高いほど時間がかかるタスク**という意味で、薄い青→濃い青で軽い/重いを表す(色の意味は違うが、どちらも「濃い=気にすべき」という向きで揃えてある)
- 項目の左端の「⠿」を**ドラッグすると並び替えられる**(新しく追加した項目は一番下に付くので、優先したい時にここで上に動かせる)。サブタスクの並び替えは未対応

### 曲のテキストメモとToDoを紐付ける

編集モードで曲を開き、テキストメモの左上に出る「✅」ボタンを押すと、そのメモの内容が練習ToDoに追加され、**「どの曲の・どの場所の話か」も一緒に記録される**。

- **曲ごとに1つのメインタスク(曲名)にまとめられ、各メモはその下のサブタスクとして追加される**(2026-08-17〜。1曲で練習項目がいくつも出てくるため、曲名の下に束ねて見通しを良くしている。同じ曲から2つ目以降のメモを追加しても新しいメインタスクは増えず、既存の曲名タスクにサブタスクが増えていく)
- サブタスクには「🎵」ボタンが表示される。押すとその曲を開き、該当メモの位置まで自動でスクロールして一瞬ハイライトする
- その画面から「← 曲一覧」で戻ると、曲一覧ではなく練習ToDo画面に戻る
- 紐付け先のメモや曲を削除した場合、ToDo自体は消えない(曲が無ければ案内が出る。メモだけ無ければ曲だけ開く)

## 9. 音楽リンク集

曲一覧画面の「🔗 リンク集」ボタンから開く。ChordWiki・U-FRET・YouTubeなど、練習中によく開く音楽系サイトを名前付きで貯めておける(2026-08-19〜)。

- 上の欄に**名前**と**URL**を入れて「＋ 追加」で登録する。名前欄・URL欄のどちらでEnterを押しても追加できる
- **名前は省略してもよい**(省略した場合はURLがそのまま名前になる)
- **`https://`は省略できる**(`chordwiki.jp`と打てば`https://chordwiki.jp/`として登録される)
- 各行をタップすると新しいタブでそのサイトが開く
- 「✎」で名前とURLを編集、「🗑」で削除
- 登録したリンクは**他の端末ともクラウド同期される**(→[11. クラウド同期](#11-クラウド同期複数端末で共有))。ZIPバックアップにも含まれる
- `http://`と`https://`のURLだけ登録できる(それ以外は弾かれる。同期を通じて他の端末にも配られるデータなので、タップした瞬間に何かが動くような形式は受け付けない)

デフォルトでは何も登録されていないので、自分がよく使うサイトを入れて使う。

## 10. フレーズ集

曲一覧画面の「🎵 フレーズ集」ボタンから開く(2026-08-19〜)。楽譜エディタで作った譜面を**データのまま**保存しておき、後から開き直して編集・転調したり、曲の譜面に貼ったりできる。

「+ 楽譜」で作る譜面メモは画像として貼るだけで音符データは残らないが、フレーズ集は音符・コード名・拍子・調号をそのまま保持するので、**作ったフレーズを資産として使い回せる**のが違い。

### フレーズを作る・編集する

- 「＋ 新しいフレーズ」で楽譜エディタが開く。入力方法は[「+ 楽譜」](#4-メモ機能テキスト画像手書き)と全く同じ(ピアノ鍵盤・文字キー・MIDI・和音・コード名など全部使える)
- 「保存」を押すと**フレーズ名**と**タグ**を聞かれる。タグはカンマ・読点・空白のどれで区切ってもよい(例: `ジャズ, リフ, 練習中`)。タグ不要なら空のままでよい
- 一覧の「✎ 編集」で開き直して直せる(保存すると上書きされる。名前とタグは聞かれない)
- 「名前/タグ」ボタンで名前とタグだけを変更できる
- 「🗑」で削除

### タグで絞り込む

タグを1つでも登録すると、一覧の上にタグのチップが並ぶ。タップするとそのタグのフレーズだけに絞り込める。「すべて」で解除。

### 転調

楽譜エディタのツールバーに「転調」グループがある(フレーズ集・「+ 楽譜」のどちらでも使える)。

- **−1 / +1**: 半音ずつ移調
- **−12 / +12**: オクターブ単位で移調
- **音符の高さ・音符に振ってあるコード名・調号がまとめて動く**。コード名は`Cm7`→`Dm7`のように語尾を保ったまま、分数コード(`C/G`→`D/A`)もルートとベースの両方が動く
- **綴りは鍵盤入力と同じモード理論の規則で決まる**(→[黒鍵の♯/♭](#4-メモ機能テキスト画像手書き))。音符もコード名も同じ規則なので表記が食い違わない(例: ハ長調では`bVII`が`A#`ではなく`Bb`、`#IV`が`Gb`ではなく`F#`になる)
- 「♭↔♯ 異名同音」を手動で固定していた場合は、転調時に調号追従に戻る(調号と音符の表記が食い違わないようにするため)

### 練習ToDoと連動する

一覧の「✅」ボタンで、そのフレーズを練習ToDoに追加できる(→[8. 練習ToDo](#8-練習todo))。

### 曲の譜面に貼る

保存したフレーズは、曲の譜面に画像として貼れる。

1. 曲を開いて編集モードにし、「📝 メモ」→「+ 楽譜」→貼りたい場所をタップ
2. 楽譜エディタが開いたら「🎵 フレーズ読込」を押し、番号でフレーズを選ぶ
3. 必要なら転調してから「完了」を押すと、その場に画像として貼られる

読み込んだ内容はエディタ上でそのまま編集できるので、**元のフレーズを壊さずにこの曲用だけ手直しして貼る**こともできる(フレーズ集側は「保存」を押さない限り変わらない)。

### コードからフレーズを逆引きする(2026-08-20〜)

**「今このコードが来た。何が弾ける?」を引ける。** フレーズ集の上の帯で、キーを選んでコード名かディグリーを入れる。

- **コード名で引く**: `Dm7` と打つと、選んだキーでそれが何度に当たるか(キーCなら`IIm7`)を判定して、その度数を含むフレーズを出す
- **進行で引く**: `2-5-1` や `IIm7 V7 IM7` と打つと、その並びが**連続して**出てくるフレーズを出す
- **チップで引く**: 手持ちのフレーズに実際に入っている度数が、出現回数の多い順に並ぶ。押すだけで絞れる
- **出てきたフレーズは、選んだキーに移調したコード進行で表示される**。元のキーが違う場合は「(元: D)」のように添える
  - 例: キーDで保存した `Em7 - A7` のフレーズは、キーCで引くと `Dm7 - G7` として出る。**そのまま弾けないと意味がないため**
- **一致の判定は「そのコードで使えるスケールが同じか」で行う**(2026-08-20〜)。コードの綴りではなく音楽的な意味で束ねる
  - `I` `I6` `IM7` はどれもアイオニアンなので**同じ扱い**
  - `V` と `V7` はどちらもミクソリディアンなので**同じ**。`VM7` は7thが半音上でミクソリディアンにならないので**別物**
  - `IIm7` と `IIm9` は同じ(ドリアン/エオリアン)。`IIm7b5` はロクリアンなので別物
  - **テンション(9th・11th・13thとその変化)は種類に含めない。** `V7` `V9` `V13` `V7b9` `V7(9,13)` `Valt` は全部同じ「V7」
    - 「上に乗せる音」であって、どのスケールを使うかを決める骨組みではないため。**V7が来た時にオルタードのリックも出てほしい**という考え方
    - ただし `b5` `#5` は5度そのものを変える = コードの種類が変わるので落とさない(`m7b5` は `m7` と別物のまま)
    - `add9` はトライアドに9thを足しただけなのでドミナントにはしない(`Cadd9` → `I`)
  - `m7` と `m6` は既定で同じ扱い(「まとめ方」で分けられる)
  - **無印のコードは、その度数の「本来の姿」として読む**。`I`→`IM7`、`V`→`V7`、`II`→`IIm7`
  - **チップは続けて押すと進行になる**(`IIm7` → `V7` と押せば 2-5 の検索)。末尾をもう一度押すと取り消せる
- **束ね方は「⚙ コードのまとめ方」で変えられる**。族ごとに「まとめない」にすると、コードの書き方が完全に一致した時だけ同じ扱いになる。族はメジャー/ドミナント/マイナー7/マイナー6/マイナーメジャー7/ハーフディミニッシュ/ディミニッシュ/オーグメントの8つ

**仕組み**: フレーズにはコード名と調(キー)が保存されているので、**ディグリーはそこから計算して索引として持つ**。ディグリーの方を本体にすると、`A7` を `VI7` と持つか `bVII7` と持つかで綴りの情報が落ちて元のコード名に戻せなくなるため、あくまで導出値として扱っている。以前に作ったフレーズは、フレーズ集を開いた時に一度だけ自動で計算される。

11. [クラウド同期(複数端末で共有)](#11-クラウド同期複数端末で共有)
12. [ZIPバックアップ](#12-zipバックアップ)
13. [オフライン対応・ホーム画面への追加](#13-オフライン対応ホーム画面への追加)
14. [困った時に](#14-困った時に)

---


## 10.5 フットコントローラー(譜めくりペダル)

**ペダルや外付けキーボードのキーに、譜めくりなどを割り当てられる(2026-08-20〜)。** 譜めくりペダルの多くはBluetoothキーボードとして振る舞い、キーを送ってくるだけなので、そのキーを拾って動かす仕組み。

設定は「⚙ 設定・バックアップ」→「🦶 フットコントローラー / キー操作」。**使う人が少ない設定なので既定では畳んであり、この見出しを押すと開く**(一度開けば次回も開いた状態で始まる)。畳んでいる間もペダルは効く。

| 動作 | 既定のキー | 何が起きるか |
|---|---|---|
| 次のページ | `↓` / `PageDown` | **PDFなら次のページの先頭へ飛ぶ**。画像1枚の曲は1画面ぶん送る |
| 前のページ | `↑` / `PageUp` | 同上の逆 |
| 再生 / 停止 | `Space` | 自動スクロールの開始・停止 |
| 速度を上げる | `→` | 自動スクロールの速度 +1 |
| 速度を下げる | `←` | 自動スクロールの速度 −1 |

- **どのキーを送るかは機種によって違う**ので、「変更」を押してから実際にペダルを踏めば、そのキーが登録される
- 同じキーを2つの動作に割り当てると事故になるので、**他の動作に付いていたら自動で外れる**
- 「既定に戻す」で全部やり直せる
- **譜面を見ている時だけ効く**。曲一覧・楽譜エディタ・文字入力中は邪魔にならないよう無効
- 画像1枚の曲では、1画面ぶんより少し少なく送る(前の行が少し残るので、どこを見ていたか見失わない)

## 10.7 チューナー

**曲一覧画面の「🎤 チューナー」から使える(2026-08-20〜)。**声にも楽器にも使える、いわゆるボーカルチューナー。

- マイクで拾った音の**いちばん近い音の名前**と、**そこからどれだけずれているか**だけを大きく出す。本番前に慌てて使うものなので、情報はこの2つに絞ってある
- まん中の緑(±5セント以内)に入れば合っている。それ以外は「◯ 高い(少し下げる)」「◯ 低い(少し上げる)」と出る
- 読める範囲は **65Hz〜1200Hz**(男性の低い声から女性の高い声、ギター・ピアノの大半)
- **A = 440Hz** の基準は ＋− で変えられる(415〜466)。変えた値は覚える
- **マイクはこの画面を開いている間だけ使う。**「閉じる」を押すと必ず止まる
- 音を出していない時・雑音だけの時は何も出さない(適当な音名を出さない)

## 10.75 連桁(音符をつなぐ横棒)の切り方

**楽譜エディターのツールバーの「連桁」で、2つのやり方を選べる(2026-08-20〜)。**

| | 何をするか | 向いている用途 |
|---|---|---|
| **拍で切る**(既定) | 拍をまたぐ音符を**タイで切って**、連桁を必ず1拍ごとに揃える | **本番で読む譜面。**連桁を見れば拍が分かる |
| 音符の形を残す | 小節線と小節の真ん中でだけ切る。それ以外は音符の形をそのまま残す | リックを書き留める時。**付点がタイに化けない** |

- **この2つは両立しない。**拍をまたぐ音符は「切って拍を見せる」か「切らずに形を残す」かのどちらかしかない
- 「拍で切る」だと、たとえば**付点8分を4つ並べると2つ目と3つ目が `16分〜8分` のタイになる**。見た目は変わるが、**元のデータ(音符そのもの)は変わらない**ので、切り替えれば元の形に戻る
- 設定は端末に覚える。全部の譜面に効く

## 10.8 他のアプリから楽譜を読み込む(MusicXML)

**楽譜エディターの「📤 読み込む」から、他のアプリが書き出した MusicXML を取り込める(2026-08-20〜)。** 対応するのは `.musicxml` / `.xml` / `.mxl`。

### 何に使うか

1つは**譜面の写真から音符を読み取る(OMR)**ため。読み取りそのものはブラウザでは動かないので、**このアプリの外でやってもらって、結果だけ持ってくる**形にしている。**画像はどこにも送られない。**

- [Audiveris](https://audiveris.com/) — 無料。パソコンに入れて使う。いちばん精度がいい
- [QuickStave](https://www.quickstave.com/optical-music-recognition) — 無料。ブラウザで動くので入れなくていい

もう1つは、**MuseScore・Sibelius・Finale・Guitar Pro などで打ち込んだ譜面を持ち込む**ため。これらは全部 MusicXML を書き出せる。

### 取り込めるもの / 取り込めないもの

- 取り込めるのは **1パート・第1譜表・最初の声部**だけ。この楽譜エディターは1本の旋律と和音のためのもので、ピアノの左右手や複数声部を表現できないため
- 読むもの: 音符・休符・和音・音価と付点・タイ・3連符・臨時記号・調号・拍子・音部記号・**コード名(`<harmony>`)**
- 落とすもの: 装飾音符・2声目以降・2段目の譜表・他のパート。**落としたものは必ず件数で報告する**(黙って消さない)
- 途中で拍子や調号が変わる曲は、**最初の値だけ**を使う。その旨も報告する
- 5/4 のような対応していない拍子は 4/4 に倒す

### 使い方

1. 楽譜エディターで「📤 読み込む」
2. 案内が出るのでファイルを選ぶ
3. **何が取り込めて何が落ちたかが出る**ので、確認してから「この内容で置き換える」
4. **読み取りは必ず間違いが混ざる。**保存する前に見直すこと
5. 気に入らなければ **Ctrl+Z(取消)で読み込む前に戻せる**

> **「置き換える」は、いま作りかけの譜面を消す。**追記ではないので注意。

## 11. クラウド保存(あなた用のURL)

**初めて開いた時点で、あなた専用のURLが自動で用意される。**いつものURLの末尾に `?key=...` というパラメータが付いた形で、アドレスバーを見ると分かる。設定も登録も要らない。

**このURLで曲やメモを作ると、データは端末の中とクラウド(Cloudflare Worker)の両方に保存され、同じURLを開いた他の端末とも中身がそろう。**曲一覧画面の「☁ クラウド保存(あなた用のURL)」グループにまとまっている。

- **見に来ただけの人はクラウド側に何も作らない。**実際に中身を変える操作(曲の追加・メモ・ToDoなど)をした時に初めてクラウド側の置き場所が用意される
- その瞬間に一度だけ「🔑 このURLがあなたのデータの鍵です」という案内が出る。**ここでブックマークするか、ホーム画面に追加しておくこと。**このURLを失くすと、クラウド側のデータには二度と入れない
- 自動で用意された置き場所には上限がある(**150曲 / 合計200MB**)。超えると保存時にその旨が出るので、不要な曲を削除するかZIPバックアップに移す
- 通信できなかった等でクラウド側の用意に失敗しても、**端末の中には普通に保存される。**次に何か変えた時に自動で再挑戦する

> **スマホの場合**: 保存・バックアップ・オフラインの各ボタンは、画面が狭いと曲一覧を押し下げてしまうため、「⚙ 設定・バックアップ」ボタンの中に畳まれている。押すと開く(iPadやPCでは最初から開いた状態で表示される)。

- **開いた瞬間に確認ダイアログは出ない。**以前は「同期キーがまだ設定されていません」と出していたが、アプリを一度も見ていない人に知らない言葉を突きつけていたので、2026-08-20にやめた
- 「今すぐ同期」ボタンで即座にやり取りする
- **あなた用のURLで開いている端末では、アプリを開いた時点で自動的にも同期する**(失敗しても画面には出さず静かに諦めるので、機内モードで開いても邪魔にならない)。手動で押す必要は基本的に無い
- 「あなた用のURLをコピー」で、`?key=...` 付きのURLをそのままコピーできる。LINEやメモで自分の他の端末に送り、そこで開くだけでセットアップが済む
- 「別のURLに切り替える」で、あなた用のURLを確認・貼り替えできる。**URLをまるごと貼っても、`key=` の後ろの部分だけを貼っても、どちらでも通る**。空にすればこの端末だけの利用に戻る(端末内のデータはそのまま残る)
  - 切り替えると**アドレスバーのURLにも自動で反映される**ので、そのまま「ホーム画面に追加」すれば次回からそのURLで起動する
- やり取りは「新しい方が勝つ(updatedAt-wins)」方式。片方の端末でしか変更していない項目は、もう片方に正しく反映される
- 対象は、曲(画像/PDF・メモ・BPM・速度・動画URL)・セットリスト・練習ToDo・メモのスタンプ・[音楽リンク集](#9-音楽リンク集)・[フレーズ集](#10-フレーズ集)
- **削除も反映される**。ある端末で曲/セットリストを削除すると、他の端末で次回同期した時にそちらでも自動的に削除される(削除後に別端末で編集していた場合は、編集の方が新しければ削除されず残る)
- **URL = 本棚**。URLごとに完全に別のデータを持つので、友達に別のURLを渡せば、お互いの曲・セットリスト・練習ToDoは見えない。逆に**同じURLを渡した相手とは1つの本棚を共有する**(バンドで曲やセトリを共有したい場合はこちら)
  - 同じ本棚を共有している場合、**誰か1人が曲を削除すると全員の端末からも消える**(上記の削除の反映のため)。共有する相手が増えるほど、定期的なZIPバックアップの重要度が上がる
  - 許可されていないURLでは保存できない(第三者に勝手にストレージを使われないための仕組み)。アプリが自分で用意したURLは自動で許可されるが、**同じ回線からは1日5個まで**
- **あなた用のURLは、それを知っている人なら誰でもデータを読み書きできる。**共有する相手以外には教えないこと。SNSなどに貼らないこと
- このアプリは個人の練習・準備用。取り込んだ譜面は私的な利用の範囲にとどめること
- 電波状況が悪い等で通信が固まった場合、20秒でタイムアウトしてエラー表示に戻る(「同期中...」のまま固まり続けることはない)。タイムアウトしたら電波状況を確認してもう一度試す

> **アプリが最新版にならない時**: 「⚙ 設定・バックアップ」→「オフライン」の**「データを更新」**を押すと、保存済みのファイルをサーバーから取り直して画面を読み込み直す。ホーム画面から起動したPWAは、アプリを閉じても裏で動いたままになることがあり、新しい版に自動で入れ替わらないことがあるため(2026-08-19にこのボタンの不具合を修正した。それ以前は押しても何も起きなかった)。

## 12. ZIPバックアップ

曲一覧画面の「💾 バックアップ」グループにまとまっている。

- 「書き出す」で全データ(曲・画像・メモ・セットリスト・練習ToDo・スタンプ・リンク集・フレーズ集)をZIPファイルとして端末のFiles/ダウンロードフォルダに保存できる
  - **練習ToDo・スタンプ・リンク集・フレーズ集がZIPに含まれるようになったのは2026-08-19〜**。それ以前のバックアップには曲とセットリストしか入っていない(古いZIPを読み込んでも、これらの項目は今の内容がそのまま残るだけで消えることはない)
- 「読み込む」で復元(同じ曲がある場合は新しい方が優先される)
- 最終バックアップ日時がグループ内に表示され、7日以上更新が無いと警告色になる
- **クラウド同期があっても、念のため定期的にZIPバックアップも取っておくことを推奨**(仕様変更等でクラウド側のデータが消えるリスクへの保険)

### 自動ローカルバックアップ(2026-08-17〜)

上記の「書き出す」は手動でファイルに保存する方式だが、それとは別に**アプリを開くたびに自動でバックアップが更新される**仕組みもある(アプリ自身のストレージ内に1件だけ保存し、常に最新版で上書きしていく方式。過去分は残らない)。

- 「自動バックアップ: ○日前(サイズ)」の表示で、いつの時点のものかと使用容量が分かる
- 「この端末から復元」で、ファイルを探さずワンタップでこのバックアップから復元できる(手動の「読み込む」と同じく、今のデータより新しい曲は上書きされない)
- 「🗑 削除」で、この自動バックアップが使っている分の容量を解放できる(クラウド同期や手動の「書き出す」には影響しない)
- 実際の更新は前回から24時間以上経っている時だけ(毎回作り直すと重いため)。曲が1つも無い時は作られない

### 端末をまたいだ時のメモの見え方(2026-08-19に改善)

メモは**作った時の画面幅を基準**にして、開いた端末の幅に合わせて自動で拡大縮小される。タブレットで作ったメモをスマホで開いても、譜面に対する位置・大きさの関係が保たれる。

- 文字は小さくなりすぎないよう下限(7px)でクランプされる。**クランプされた分は箱の幅も一緒に広げる**ので、「箱は縮んだのに文字だけ元の大きさ」で意図しない折り返しが起き、メモ同士が重なる、という問題は起きない(2026-08-19に修正。それ以前はタブレットで作った譜面をスマホで開くとメモが重なっていた)
- それでも文字が小さくて読みにくい時は、**右上のズームボタンで拡大するとメモも一緒に拡大される**

## 13. オフライン対応・ホーム画面への追加

1. Safariでアプリを開き、共有ボタン→「ホーム画面に追加」
2. ホーム画面のアイコンから起動すると、アドレスバーの無い全画面アプリとして開く
3. 一度オンラインで開いておけば、以後は**機内モードでも起動・全機能(YouTube以外)が使える**

### アプリの更新について(2026-08-06〜完全自動)

このアプリのコードが更新されると、**オンライン時は自動で最新版に切り替わる**(バックグラウンドでダウンロードが完了すると、その場で自動的にリロードされる。演奏中の画面でも例外なく自動リロードされる仕様)。以前は「データを更新」ボタンを手動で押さないと反映されない方式だったが、F5でリロードしても古いキャッシュのままで分かりにくいという問題があったため、2026-08-06に完全自動更新へ変更した。
「📷 オフライン」グループの「データを更新」ボタンは、明示的に今すぐ最新版を取得したい時向けに残っている(基本的には押さなくても自動で更新される)。

## 14. 困った時に

- **エラーが出た**: そのままの文言をメモしておいてもらえれば調査できる
- **あなた用のURLが分からなくなった**: 既に使えている別の端末があれば「あなた用のURLをコピー」で取り出せる。全端末で分からなくなった場合はCloudflare側での再設定が必要
- **データが全部消えた/壊れた**: 直近のZIPバックアップから復元する。バックアップが無い場合は[クラウド保存](#11-クラウド保存あなた用のurl)からの復旧を試みる
- **不具合を見つけた・こうしてほしいことがある**: 「⚙ 設定・バックアップ」→「💬 不具合・要望を送る」から直接送れる([使い方のデータについて](#15-使い方のデータについて))

## 15. 使い方のデータについて

- **不具合・要望の送信**: 「⚙ 設定・バックアップ」→「💬 不具合・要望を送る」から、不具合か要望かを選んで本文を書いて送る。送信すると完了画面が出る。返信はできないが全部読んでいる
  - 一緒に送られるのは**アプリの版とブラウザの種類だけ**。曲・譜面・メモなどの中身は一切送られない
  - あなた用のURLで使っている場合は、そのパラメータの**末尾4文字だけ**が一緒に記録される(同じ人からの続きの報告だと分かる程度)
  - **あなた用のURLが無くても送れる。**荒らし対策として、同じ回線からは1時間に10件までに制限している
- **使われ方の記録**: どれくらい使われているかを知るため、**日ごとに何人が開いたか**だけを数えている
  - 送っているのは端末ごとのランダムなIDとアプリの版だけで、誰かは分からない。同じ日は1回しか送らない
  - 曲名・譜面・メモなど、アプリの中身は一切送っていない

---

このマニュアルは`E:\chordwiki-live-viewer\MANUAL.md`にあります。機能を追加・変更した際は、このファイルも一緒に更新すること。
