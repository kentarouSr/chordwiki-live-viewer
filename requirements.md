# 要件定義書 v0.2(再構成版)

> **注記:** claude.ai側のオリジナル requirements.md v0.2 は本セッションに引き継がれておらず、
> CLAUDE.md 内で言及されている章番号(14〜17章)を手がかりに再構成した。
> 1〜13章は CLAUDE.md の記述から妥当な構成を組み立てた**推定**であり、
> 原本と食い違う可能性がある。内容を確認し、必要に応じて修正すること。

## 1. 背景

ライブで約20曲を、iPadに表示したChordWikiのコード譜を見ながら演奏する。
既存の楽譜アプリ(複数試用)は速度調整が粗い・スクロール中に画像描画が乱れる、
という理由ですべて不合格だった。

## 2. 目的

ChordWikiと同等の操作感を持つ自動スクロールを、オフライン環境でも
確実に再現する自作PWAを開発する。

## 3. 対象ユーザー・利用シーン

- 開発者本人がライブ本番でiPadに表示して演奏しながら使用
- 事前準備はWindows PC(Chrome/Edge)で行う

## 4. 核心要件(最重要)

「ChordWikiと同等の操作感の自動スクロール再現」。
メモ機能・画像機能はその上に載せる二次的機能であり、優先度は自動スクロールの完成度より低い。

## 5. 経緯・却下した方式

1. Safari拡張機能でChordWiki実ページにオーバーレイ表示する方式(v0.1) → 却下
   - 開発者はMacを持たずXcodeビルドが不可能
   - 本番会場はネット接続が保証されない
   - SafariのITP(7日未訪問でサイトデータ自動削除)によるデータ消失リスク
2. PWA方式(v0.2)に転換 → 現行方針

## 6. コンテンツ登録方式

事前にChordWikiを移調済み状態でフルページスクショ(画像/PDF)し、PWAに登録して表示する。
**本番中の移調は非対応**(キーは事前に確定させておくことが前提)。

## 7. スクロールエンジン要件

- rAF + translate3d による一定速度スクロール
- 縦1024pxタイル分割Canvas描画(iPad Safariの巨大画像描画問題対策)
- 1px/秒刻みの速度調整(長押しで刻み加速)、localStorageまたはIndexedDBに保存
- 即時停止・同位置再開
- 末尾到達で自動停止
- 手動ドラッグ操作(再生中はドラッグ中一時停止、離すと自動再開)

## 8. 表示対応フォーマット

- 画像(フルページスクショ)
- PDF(pdf.js使用。本実装ではリポジトリに同梱してオフライン化する)

## 9. 端末・環境要件

- Wake Lockで画面消灯を防止
- 進捗バー表示
- 再生中はツールバーを自動的に隠す

## 10. 対象デバイス・ブラウザ

- 本番: iPad Safari(ホーム画面追加PWA)
- 事前編集: Windows Chrome/Edge

## 11. 技術方針・制約

- 素のHTML/CSS/JS。フレームワークなし。ビルド工程なし(誰でも触れる状態を維持)
- 外部依存は pdf.js のみ。本実装ではリポジトリに同梱する
- localStorage は画像を扱えないため使わない。曲データはIndexedDBに保存する
- ホーム画面追加PWAはITPの7日削除の対象外(WebKit公式見解)だが、
  仕様変更リスクに備えZIPバックアップを必須機能とする

## 12. データ保持方針

- 全データ(画像、曲名、スクロール速度、メモ、BPM)をIndexedDBに保存
- 全データのZIP書き出し/読み込み機能を必須とする(バックアップ)
- 最終バックアップ日時を表示し、一定期間更新がなければ警告する

## 13. 開発プロセス

1. スクロールエンジンのプロトタイプ(scroll-prototype.html)をPCブラウザで検証
2. iPad実機検証(下記14章のチェック項目)
3. 合格後、MVP実装(15章)に着手

## 14. MVP後のスコープ(将来拡張)

- 曲一覧表示
- セットリスト作成・セットリスト順の曲切り替え(前後曲送り)
- メトロノーム(Web Audio、曲ごとにBPMを設定可能)
- Windows(Chrome/Edge)での編集対応の強化

## 15. MVP実装のスコープ

プロトタイプをベースに以下を追加する:

- Service Worker によるオフライン起動(pdf.js同梱含む)
- IndexedDB への曲保存(画像、曲名、スクロール速度、メモ、BPM)
- 曲ごとのスクロール速度保存(現在はグローバル1値)
- テキストメモ・画像メモの配置(譜面画像上の相対座標、編集モードで操作)
- 演奏モード(デフォルト)/編集モードの切り替え(鍵アイコン長押し等の誤操作対策)
- 全データのZIP書き出し/読み込み(バックアップ必須。最終バックアップ日時の表示と警告)

## 16. 受け入れ条件

- **最重要:** 機内モード(完全オフライン)で全機能が動作すること
- 実際のChordWikiフルページスクショでスクロールが滑らかであること
- スクロール中に画像の白抜け・描画乱れが発生しないこと(長い曲を含む)
- 全データのZIPバックアップ/リストアが正しく機能すること
- iPadホーム画面追加PWAとして起動できること

## 17. 検証が必要な未決事項

- 本番中に移調が必要な場面が本当にないか(本方式では不可のため重要)
- 自動スクロール中の手動操作の挙動(ドラッグ→一時停止→自動再開案の是非を実機で確認)
- メトロノームの音色
- 譜面画像の推奨解像度・圧縮率(iPad実機で実測)
- ホーム画面追加PWAが7日放置でデータ保持されるかの実測(開発と並行して放置試験)

## iPad実機検証のチェック項目(14章と対応)

1. 実際のChordWikiフルページスクショで、スクロールが滑らかか
2. 1px/秒刻みの速度粒度が適切か(実際に使う速度域もメモする)
3. スクロール中に画像の白抜け・描画乱れが出ないか(長い曲で)
4. ドラッグ→自動再開の挙動が演奏時の感覚に合うか
