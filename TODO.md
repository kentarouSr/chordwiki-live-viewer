# 未対応の改善要望・不具合

2026-08-19 時点。着手する時はここから拾う。

## 不具合

### 楽譜を「更新」するとパーミッションエラーのようなものが出る

楽譜メモの「♪」から開き直して更新した時に発生。未再現・未調査。

疑わしい順:

1. **IndexedDBのバージョン上げが別タブに阻まれている。** 2026-08-19に音源ストア追加で
   DB_VERSION を 7→8 に上げた。古い版を開いたままのタブ/ホーム画面PWAが残っていると
   `versionchange` がブロックされ、以後の書き込みが失敗する。全部閉じてから開き直すと
   直るはず。だとすれば根本対処は「blocked/versionchange をハンドリングして、
   古いタブに再読み込みを促す」こと。
2. Safari のストレージ制限(容量超過)。音源を入れた直後だと `QuotaExceededError` が
   出ることがあり、文言が「permission」寄りに見える可能性がある。
3. `replaceScoreMemo` → `saveCurrentMemos` → `dbPut` の経路で、曲レコードが
   別経路で更新されていて衝突している。

まずは**エラーの正確な文言**を控えてもらうこと。console にも出しているはず。

## 楽譜エディタの機能追加

### 休符のショートカットキー

今は「休符」ボタンのみ。PCのキーボードから打てるようにする。
`R` は既に音名(A〜G)と衝突しないので使える見込み。

### オクターブ記号(8va / 8vb)

「オクターブ上/下で書く」記号。VexFlow には `TextBracket` があるのでそれで引ける。
範囲指定が要るので、複数選択(実装済み)と組み合わせるのが自然。

### タイボタンの向きを逆にする

現状は「選択中の音符 → **次**の音符」を結ぶ。
要望は「選択中の音符 → **1つ前**の音符」を結ぶ挙動。
打ち込んだ直後に「今の音と前の音をつなぐ」流れの方が自然、という理由。

`btnScoreTie` と `scoreCanTieToNext` の向きを入れ替える。
`el.tiedToNext` というデータの持ち方自体は変えなくてよい
(選択中の1つ前の要素に `tiedToNext` を立てればよい)。

### 拍をまたぐ音符を自動でタイに分割する ★これが本命(原因を再現・特定済み)

**2026-08-19 追試で原因を確定した。**

再現手順: 4/4で `8分 → 付点4分 → 8分 × 4` を置く。
付点4分が拍0.5〜2.0にかかり、**拍の境界をまたぐ**。

結果: その後ろの8分4つが、本来なら「2つずつ2組」で連桁になるべきところ、
「単独・連桁ペア・単独」とばらばらになった。

理由: `VF.Beam.generateBeams` に渡している groups(4/4なら1拍=1/4を4つ)は、
音符を**順番に音価で消費**して区切りを決める。1つでも拍をまたぐ音符が挟まると
以後の消費位置が拍とずれ続けるため、後続の連桁が総崩れになる。
グループ指定(`scoreVexBeamGroups`)自体は正しく、そこは原因ではない。

対策: 拍の境界で音符を切ってタイで結ぶ。そうすれば「拍をまたぐ音符」が
存在しなくなり、消費位置が常に拍と一致するので連桁が自動的に揃う。

**今**: 小節線をまたぐ時だけタイで分割している。
**要望**: **拍**をまたぐ時も分割して、連桁が拍のまとまりを壊さないようにする。

添付画像の例で言うと:

- 付点8分などが拍をまたいでいて、そこで連桁が途切れて読みにくい
- 期待する形: 「ミ」は1つ前の「レ」と同じ連桁に入れ、タイで16分同士をつなぎ、
  その先を次の「ファ」と同じ連桁に入れる

つまり「音符を拍の境界で切って、切った断片同士をタイで結ぶ」。実際の記譜法どおりの
処理で、これをやると連桁が拍単位で綺麗に揃う。

実装の勘所:

- 分割位置の計算は既にある `scoreBeatBoundaries()`(拍のまとまりの境界)を使う
- 今の `buildScoreRenderQueue()` は「小節の残り容量」だけを見て切っているので、
  ここに「次の拍境界まで」も条件として足す
- 連桁のグループ化(`scoreVexBeamGroups`)は既に拍単位なので、分割さえ正しくすれば
  連桁は自動的に揃うはず
- タイのフラグ(`tieToNext` / `tiedFromPrev`)は既存の自動分割と同じ仕組みに乗せられる

## 保留中(前回の質問への回答待ち)

### 段の幅 / 改行時の「＋と−」

「改行するたびに＋と−が出て**高さ**調整」という要望。解釈が2通りあって未確定:

- **A**: その段に入る**小節数**を増減する(＋で次の段から引き上げ、−で次の段へ送る)。
  「2段目だけ短い」問題が直接解消する
- **B**: 段と段の**縦の間隔**を変える(譜面全体を縦に詰める/広げる)
