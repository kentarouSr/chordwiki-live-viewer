# 未対応の改善要望・不具合

2026-08-20 更新。着手する時はここから拾う。

## 公開前に必ずやること

### 「データを更新」ボタンを消す

「⚙ 設定・バックアップ」→「オフライン」の**「データを更新」ボタンは開発中の
テスト用**。2026-08-06に自動更新へ切り替えたので、普通の利用者は押す必要が無く、
押しても何が起きるか分からないボタンが1つ増えているだけ。**一般公開の前に削除する。**

- 消す時は `#btnUpdateOfflineCache` とそのハンドラ、`MANUAL.md` / `manual.html` の
  「アプリが最新版にならない時」の注記も一緒に。「困った時に」へ移すのではなく消す
- 隣の「オフライン用データ: 未更新」「版: ...」の表示は残す(切り分けに使うため)

### `testcases.html` は機能追加のたびに足す(運用ルール)

**機能を追加・変更したら、同じコミットの中で `testcases.html` にも項目を足すこと。**

最初に作った時、正常系(押したら動く)ばかり並べて「テストケースぬるいな。
今の内容は自分自身で既に通ってるケーズばかり」と指摘された。
持ち主が普段の開発で通る道は既に何度も通っていて壊れていないので、
人手をかけて確かめる価値が低い。並べるべきなのは**持ち主が通らない道**:

- **過去に実際に直した不具合の再発チェック**(このファイルの「完了済み」と
  `SCORE_NOTATION_NOTES.md` に履歴があるので、そこから拾える)
- **異常系**: 空入力・通信断・機内モード・2つ同時に開く・閉じて復帰・
  2台での競合・上限を超えた時
- **端末をまたぐ動き**: 同期の競合、削除の反映、ホーム画面から起動

優先度は3段階:

| | 意味 |
| --- | --- |
| **A** | 本番で使う中核と、過去に壊れた所。**必ず通す** |
| **B** | 使うが致命的ではない |
| **C** | 余裕があれば / 特殊な環境が要る |

現在 20章113項目(A: 53 / B: 45 / C: 15)。

### `testcases.html` を公開時にどうするか

テスト担当者にスマホで試してもらうための項目表(2026-08-20 追加)。
`https://kentarousr.github.io/chordwiki-live-viewer/testcases.html`

アプリの中からはリンクしていない(普通の利用者には関係ないため)。URLを直接渡す。
一般公開の時に**消すか残すかを決める**。残す場合でも導線は作らない方がよさそう。

---

## これから作る

### 譜面の画像から読み取る(OCR) ※2026-08-20 決定・未着手

「コード譜からBPMを一括取得」「リックはOCRで読取出来るといいな。コードと音符」。
**取得元はOCR(画像から読む)で確定。**ただし読む対象によって難易度が段違いなので、
**3つに分けて、できるものから作る。**

#### A. BPM (やる。ただし当たりは少ない見込み)

譜面の中の `♩=120` `BPM 120` `Tempo 120` を探して数字を取る。

- 数字と少数の記号しか読まないので、**OCRとしては一番簡単**
- 探す場所を絞れる: テンポ表記は**1ページ目の上の方**にある。
  そこを先に見て、無ければ全体を見る。一括処理でも現実的な速さになる
- **ただしChordWikiのスクショにはテンポ表記が基本無い。**
  当たるのは市販の譜面や自分で書いた楽譜のPDFくらい。
  「一括で取れた曲だけ入る」という前提で作る(取れなかった曲は今まで通り手入力)
- 読み取った値は**確認画面を挟んでから入れる。**勝手に上書きしない
  (`♩=120` の120と、小節番号の120を見間違える可能性があるため)

#### B. コード名 (やる。これが一番おいしい)

譜面の上に並んだ `C` `Am7` `F#m7-5` `D♭` を読む。

- **語彙が決まっている**のが効く。ルート音(A〜G)＋変化記号＋種類、という
  形しか出てこないので、OCRの候補を絞れるし、**読み間違いを形から直せる**
  (`Am7` を `Arn7` と読んでも、コードの形に合わないので直せる)
- 取れたら[フレーズ集のディグリー](#)の仕組みにそのまま乗る。
  進行をディグリーに直す・移調する・「この進行のフレーズ」を引く、が全部つながる
- ChordWikiのスクショは**文字がきれいにレンダリングされた画像**なので、
  紙をカメラで撮った物より遥かに読みやすい。ここは勝算がある
- 位置も取れるので「何小節目のコードか」まで持てる可能性があるが、
  **まずは出てくる順番のリストだけでよい**

#### C. 音符 = OMR (外部の力を借りればできる。ただし順番がある)

五線譜の音符を読むのは **OMR(Optical Music Recognition)** といって、
文字のOCRとはまったく別の技術。五線を検出して消し、符頭・符幹・連桁・臨時記号を
それぞれ見つけて組み立てる必要があり、**Tesseractのような文字OCRでは1音も読めない。**

**ブラウザの中だけで完結させるのは無理**だが、外に出せば道はある(2026-08-20 調査):

| | 何 | どう使う | 注意 |
| --- | --- | --- | --- |
| [Audiveris](https://audiveris.com/) | 無料・オープンソース。Java製のデスクトップアプリ | 手元のPCで動かしてMusicXMLを書き出す | ブラウザでは動かない。サーバーに置くならコンテナが要る |
| [oemer](https://github.com/BreezeWhite/oemer) | 無料・オープンソース。機械学習。**スマホで撮った写真**が対象 | Python。サーバー側で動かす | 同上。モデルが重い |
| [Klangio (Scan2Notes)](https://klang.io/scan2notes/) | 商用。**REST APIあり**。MusicXML/MIDI/PDF/Guitar Pro | 画像を送ると譜面が返る | 有料。**音源→楽譜のAPIも同社**にある |
| [Halbestunde](https://halbestunde.com/optical-music-recognition/) | 商用。**モバイル/Web向けAPI**。写真の傾き・照明・切れを想定した学習 | 同上 | 有料 |
| [Sheet Music Manager](https://sheetmusictranscriber.com/api-docs.html) | 商用。HTTPで画像/PDF→MusicXML・MIDI | 同上 | 無料枠は月10件。公開アプリでは即枯れる |
| [QuickStave](https://www.quickstave.com/optical-music-recognition) | 無料のブラウザ上の道具。MusicXML/MIDIで書き出せる | 人が手で使う | APIではない |

**どれを選んでも出口はMusicXML。**つまり詰まっているのはOMRエンジンではなく、
**MusicXMLをこのアプリの楽譜データ(`elements`)に取り込む口が無いこと。**

##### 順番

1. **MusicXMLの取り込みを作る。** ここが本体。
   - 端末の中だけで完結する。**お金もかからず、どこにも何も送らない**
   - **どのOMRを選んでも無駄にならない。**あとから乗り換えても取り込み口は同じ
   - それ自体で役に立つ: MuseScore・Sibelius・Finale・Guitar Proは全部MusicXMLを吐く。
     人が打ち込んだ譜面をそのまま持ち込めるようになる
2. **手元のAudiverisでMusicXMLにする流れをマニュアルに書く。**
   無料・無制限・外に何も出ない。これで「本のリックを取り込む」は実現する
3. **APIを繋ぐのは、1と2をやってから判断する。**

##### APIを繋ぐ時に効いてくること

- **お金。**一般公開すると、知らない人がアップした画像のぶんまで課金される。
  無料枠(月10件など)は初日で枯れる。やるなら
  **持ち主だけが使える**か、**利用者が自分のAPIキーを入れる**形にする
  (「みんなのフレーズ」と同じ非対称の考え方)
- **外に出すということ。**譜面の画像を第三者のサーバーに送ることになる。
  いまは全部端末の中で完結しているので、方針が変わる
- **精度。**きれいに印刷されたPDFなら実用になるが、**紙をスマホで撮った写真は
  かなり落ちる。**取り込んだ後に必ず直す前提で作ること
- **ChordWikiのスクショにはそもそも五線譜が無い**ので、OMRが効くのは
  「本や市販PDFからリックを取る」場面だけ。**主目的はリック収集**と割り切る

##### 「自分のサーバーで処理する」案について(2026-08-20 検討)

「サーバで処理にすればよい？そうすると著作権てきにまずい？」

**技術的にはできる。ただし今の構成から一段増える。**

- Cloudflare Workersでは動かない。V8のisolateなのでJVMもPythonも動かせない。
  **Cloud Run / Fly.io / VPS のような、コンテナを動かす場所が別に要る**
- Audiverisは1ページ数秒〜数十秒のCPU仕事。JVMなので冷えた状態からの起動も遅い
- 今は「静的なページ(GitHub Pages)＋サーバーレス(Workers)」で、
  **面倒を見るものが無い**構成になっている。ここに常時動く箱が入るのは、
  このプロジェクトで一番大きな構成変更になる

**著作権の方が本題。**(以下は法律の専門家の判断ではない。考え方の整理として)

論点は「**誰が複製したことになるか**」に尽きる。

- **端末の中で処理する場合**: 自分が持っている譜面を自分の道具で写しているだけなので、
  私的使用のための複製(30条)の範囲で説明がつく
- **自分のサーバーで他人のぶんを処理する場合**: ここが変わる。
  2026-08-16に整理した通り、まねきTV・ロクラクII(最高裁 2011)の考え方では、
  **利用者が操作していても、設備を用意して管理している側が複製の主体と
  みなされうる。**「利用者が勝手にやったこと」で切り離せるとは限らない
- 30条には「**公衆の用に供する自動複製機器**を用いた複製」を除く、という
  但し書き(1号)もある。コピー機については当面適用しない附則があるが、
  サーバー上のOMRがそれに当たるとは言いにくい
- 30条の4(享受を目的としない利用)は使えなさそう。**画像を解析する過程は
  非享受と言えても、出てくるMusicXMLはまさに演奏して享受するためのもの**で、
  それが目的物になっている
- **商用のOMRサービスは実在して普通に営業している。**ただし利用規約と
  削除対応の窓口と、必要なら弁護士を持っている会社の話。
  **個人が趣味で公開する物とは、法律ではなく体力の問題として別**

**効いてくる違い: 溜めないこと。**
R2に譜面を保管する話と違って、**処理したら即捨てて何も残さない**なら、
公衆送信も蓄積も起きない。同じ「サーバーで扱う」でも risk の性質はかなり違う。

##### 結論: 危険度の低い順に3段階。1と2は同じコードで済む

| | 何 | 費用 / 手間 | 著作権上の立場 |
| --- | --- | --- | --- |
| **1** | **MusicXMLの取り込みだけ作る。**OMRは各自が手元のAudiverisで | 0円 / 0 | **何も変わらない。**端末内で完結 |
| **2** | サーバーでOMR。**ただし持ち主だけが使える** | 箱代のみ | 自分の譜面を自分のサーバーで処理するだけ。手元でやるのと実質同じ |
| **3** | サーバーでOMR。**誰でも使える** | 箱代＋負荷 | **ここで立場が変わる。**他人の譜面の複製主体になりうる。窓口も要る |

**1から作る。**2をやりたくなっても取り込み口は同じものを使うので、1は無駄にならない。
3をやるなら、少なくとも「処理したら即捨てる」「削除の申し出を受ける窓口」
(既にある `POST /api/feedback` を流用できる)は先に用意すること。

**そもそもの目的を思い出すと、リック収集は自分のための作業**なので、
1と2で必要十分な可能性が高い。3は「みんなのフレーズ」を投稿開放する時に
まとめて考えればよい。

##### 3(誰でも使えるサーバーOMR)を将来やる場合: 費用と規約と権利(2026-08-20 調査)

「3じゃだめかな？将来的に。コストを知りたい。規約と権利も」

結論を先に書くと、**費用は問題にならない。詰まるのは権利の方。**
そして**3の使い心地を、権利の問題を抱えずに出す道がある**(下の「2′」)。

###### 費用: 自前で動かせばほぼ0円。APIを買うと死ぬ

Audiverisは1ページおよそ10〜20秒のCPU仕事。2 vCPU / 2 GiB で15秒として計算する。

**Cloud Run(使った時だけ動かす)**
[料金](https://cloud.google.com/run/pricing)は vCPU $0.000024/秒、メモリ $0.0000025/GiB秒。
毎月 **180,000 vCPU秒・360,000 GiB秒・200万リクエスト**が無料。

| | 計算 |
| --- | --- |
| 1ページ | 30 vCPU秒 + 30 GiB秒 = **$0.0008(約0.12円)** |
| 無料枠に収まる量 | **月およそ6,000ページ** |
| 数十人 × 各20ページ | 月400〜1,000ページ。**無料枠の中** |

- **必ず「使わない時は0台」にすること。**常時1台にすると 2vCPU×1か月 =
  520万 vCPU秒で**月1万円を軽く超える**
- 代償はJVMの起動待ち。使った瞬間は20〜30秒待たされる。
  起動中のCPUも課金されるが、それを足しても無料枠には収まる

**VPSを借りる(ずっと動かす)**
月700〜1,800円くらい(2vCPU/2〜4GB)。**起動待ちが無い**のが利点。
2コアならAudiverisで日に1万ページ以上さばけるので、性能は完全に過剰。

**商用APIを買う**
これは無理。相場感で1ページ0.05〜0.5ドル。月1,000ページで**50〜500ドル**。
無料枠(月10件など)は初日で枯れる。**一般公開して他人のぶんを肩代わりする形とは相性が最悪。**

→ **費用の結論: 自前で動かすなら実質0円。だから費用は3を諦める理由にならない。**

###### 権利: ここが本当の壁。近すぎる判例がある

**自炊代行事件**(東京地判 2013-09-30 → [知財高判 2014-10-22](https://www.nikkei.com/article/DGXLASDG22H1Q_S4A021C1CC1000/))。
本を裁断してスキャンする代行業者について、

- 業者の「**顧客が複製の主体で、業者は手足にすぎない**」という主張は**通らなかった**
- 「電子データ化などの作業をしている**業者が複製の主体**」
- 「顧客は送付後の作業に全く関与せず、業者が主体となって複製しており、**私的複製とはいえない**」
- 差止め＋損害賠償70万円

**これは3とほぼ同じ形。**利用者がアップロードして待っているだけ、
実際の変換は運営者の機械がやっている、という構造が一致する。

こちら側に有利な違いもある: 自炊代行と違って**最初のデジタル化(撮影・スキャン)は
利用者が自分でやっている。**サーバーがやるのは形式の変換だけ。
ただし出てくるMusicXMLはやはりその曲の複製・翻案であって、
それを作っているのは運営者の機械。**判決の理屈はそのまま当てはまりそう。**

**プロバイダ責任制限法(2025年4月から情報流通プラットフォーム対処法)は助けにならない。**
あれは「**公衆**が受信する通信」=掲示板やSNSの投稿が対象。
OMRの結果はアップロードした本人にしか返さないので公衆送信ではなく、
問題になるのは送信ではなく**複製**の方。だから枠の外。

###### 規約でできること・できないこと

**できること**(訴えられる確率は下げられる)

1. **利用者が権利を持っていることの表明**「あなたが権利を持つ譜面だけを入れてください」
2. **処理の目的を限定した許諾**「変換してあなたに返すためだけに使います。
   保存しません。学習に使いません。他の人に見せません」
3. **保存しない、を実装で守る**(処理が終わったら即削除。ログにも画像を残さない)
4. **削除・停止の申し出を受ける窓口**(既にある `POST /api/feedback` を流用できる)
5. **精度の免責**「OMRの結果は必ず間違えます。確認してから使ってください」
6. 無保証・責任の上限・準拠法と管轄

**できないこと**

- **規約で複製主体の判断は変えられない。**自炊代行の業者も規約は書いていた。
  「顧客が主体」と契約書に書いても、裁判所は実態を見る
- 日本には米国のDMCAのようなセーフハーバーが**この類型には無い**

###### 2′: 3の使い心地を、権利を抱えずに出す道

**利用者が自分のAPIキーを入れる方式。**

- 画像はブラウザから**直接** Klangio / Halbestunde 等に飛ぶ。**こちらのサーバーは一切触らない**
- 利用者はその会社と直接の契約者になる。権利の表明もその会社に対して行う
- **こちらの費用は0円。複製主体にもならない。**運用する箱も要らない
- 利用者から見た操作は「ボタンを押す」だけで、3とほぼ同じ
- 弱点: APIキーの取得というひと手間が要る。**本気の人しか越えない壁**だが、
  そもそもOMRを欲しがるのは本気の人しかいない

もう一つの理想は**端末の中で動かすこと**(WASM)。これなら問題は消える。
Audiverisは Java なので不可。oemer を ONNX にする道はあるが、
モデルが100〜500MBあって現時点では割に合わない。**モデルは年々小さくなるので、
将来の選択肢としては生きている。**

###### まとめ

| | 費用 | 権利の位置 | 手間 |
| --- | --- | --- | --- |
| 1. MusicXML取り込みだけ | 0円 | **問題なし** | 小 |
| 2. サーバーOMR・持ち主だけ | 0円〜月1,800円 | **問題なし**(自分の物を自分で処理) | 中 |
| **2′. 利用者が自分のAPIキー** | **0円** | **問題なし**(こちらは画像に触らない) | 中 |
| 3. サーバーOMR・誰でも | 0円〜月1,800円 | **自炊代行判例が近すぎる** | 中＋窓口対応 |

**3をやらなくても 2′ で同じ体験が出せる。**順番は変わらず 1 →(2 か 2′)。
3は「みんなのフレーズ」の投稿開放と同じ議論なので、そちらとまとめて判断すればよい。

#### 共通の作り

- **Tesseract.js を同梱する。**ただし `sw.js` の precache には**入れない**
  (wasmと学習データで十数MBある。オフライン起動のたびに落とすのは重すぎる)。
  ボタンを押した時に初めて取りに行き、以後はブラウザのキャッシュに任せる
- 端末の中だけで完結する。**画像はどこにも送らない**
- 一括処理は**進み具合を出して途中で止められる**ようにする(何十曲もあると長い)
- 結果は必ず**確認画面**を挟む。OCRは必ず間違えるので、勝手に書き換えない

## 保留中(前回の質問への回答待ち)

### 段の幅 / 改行時の「＋と−」

「改行するたびに＋と−が出て**高さ**調整」という要望。解釈が2通りあって未確定:

- **A**: その段に入る**小節数**を増減する(＋で次の段から引き上げ、−で次の段へ送る)。
  「2段目だけ短い」問題が直接解消する
- **B**: 段と段の**縦の間隔**を変える(譜面全体を縦に詰める/広げる)

---

## 完了済み(2026-08-20)

以下は全て実装・検証済み。記録として残す。

### 楽譜を「更新」するとパーミッションエラーのようなものが出る → 修正

**原因は当初の推測(IndexedDBのバージョン競合)ではなかった。**
`replaceScoreMemo` が存在しない関数 `saveMemos()` を呼んでおり、楽譜メモを開き直して
「更新」するたびに `ReferenceError` で失敗していた。正しくは `saveCurrentMemos()`。

ついでに、実際に起きうる保存失敗を切り分けられるようにした:

- `openDb()` に `onblocked` / `onversionchange` / `onclose` を追加。
  古いタブがDBを掴んだままだとPromiseが永久に決まらず「黙って固まる」状態だったのを、
  原因が分かる文言を出して再読み込みを促すようにした
- `describeDbError()` を追加。`QuotaExceededError`(容量不足)・`InvalidStateError`
  (接続切れ)を名前つきで見分けられる

### 拍をまたぐ音符を自動でタイに分割する + 連桁の総崩れ

**TODOに書いていた原因の推定は半分外れていた。** 実測したところ、
`VF.Beam.generateBeams` の「音価を順に消費してグループを切る」方式は、
拍をまたぐ音符が挟まっても**次の拍には正しく復帰する**(Fractionが破壊的更新なので
剰余がちゃんと繰り上がる)。実際の崩れ方は違った。

実測(4/4、`8分 + 付点4分 + 8分x4`):

- 修正前の連桁: `[[3,4]]` … 拍をまたぐ位置で1組だけ張られ、前後は単独
- 修正後の連桁: `[[2,3],[4,5]]` … 拍どおり2組

対策は2つ入れた。どちらも必要だった:

1. **連桁のグループ化を自前にした**(`scoreMakeBeams` / `scoreBeamRuns`)。
   各音符が小節のどこから始まるかを数え、同じ拍のまとまりに入る連続した8分以下だけを
   1組にする。グループ内の連桁作成自体はVexFlowに渡す(符幹の向き・3連符の括弧処理を
   任せたいため)
2. **1拍に満たない音符が拍をまたぐ時はタイで分割する**(`scoreBeatChunkLimit`)。
   1拍以上の音符と3連符は対象外

### タイボタンの向きを逆にする → 実装

「選択中の音符 → **1つ前**」に反転。データの持ち方(`prev.tiedToNext`)は変えていない。

### 休符のショートカットキー → 実装

`R`=休符、`T`=タイ。どちらも音名A〜Gと衝突しない。

### オクターブ記号(8va / 8vb) → 実装

`el.oct8` に `'up'|'down'|null` を持たせ、同じ値が連続する範囲に VexFlow の
`TextBracket` を引く。「開始/終了に印を付ける」方式にしなかったのは、途中の音符を
削除・挿入した時に印の対応が壊れるため。

**鳴る音(pitches)は変えず、書く位置だけ7段ずらす。** 移調も再生も影響を受けない。

副産物として、**五線から大きくはみ出す音符が canvas の上下で切れていた既存の不具合**も
直した(`scoreExtraTopRoom` / `scoreExtraBottomRoom` を実際の音符位置から計算)。
それまでは上マージンが20px固定で、高い音は問答無用で切れていた。
